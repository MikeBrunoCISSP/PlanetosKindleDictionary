## 1. Fix the enqueue mechanism

- [x] 1.1 In `apps/api/src/jobs/sweep.ts`, remove the `jobId: \`${series.id}-${hash}\`` option from the `queue.add()` call and add `deduplication: { id: series.id, keepLastIfActive: true }` instead. Verify `pnpm --filter @planetos/api exec tsc --noEmit` passes. (Typecheck clean.)
- [x] 1.2 In the same file, rewrite the doc comments (the function-level comment describing "the deterministic jobId makes repeated enqueues... a no-op" and the inline comment above the `add()` call about BullMQ's jobId character restrictions) to describe the `deduplication`-based mechanism instead, explicitly noting why the content hash must not go back into `jobId` (BullMQ's jobId-existence check short-circuits before `deduplication` is ever consulted — see design.md Context). Verify by reading the comments back against design.md for accuracy. (Both comments rewritten; consistent with design.md Decisions 1-2.)

## 2. Update existing test coverage

- [x] 2.1 In `apps/api/tests/sweep.test.ts`, update the `"multiple content changes between sweeps still enqueue exactly one build reflecting the final state"` test: remove the literal-jobId assertion (`expect(jobIds[0]).toBe(...)`) and the now-unneeded `finalHash` computation it depends on, keeping the existing `expect(jobIds).toHaveLength(1)` assertion. Verify the test passes. (Passing, live against real Redis.)
- [x] 2.2 Re-run `"running the sweep twice in a row with no intervening change enqueues nothing the second time"` and confirm it still passes unmodified (dedup against a waiting job should still resolve to that job's own auto-generated ID across both calls). If it needs a change, make the smallest one that preserves its intent and note why. (Passes with only its explanatory comment reworded — mechanism changed from jobId-based to deduplication-based, assertions unchanged.)

## 3. Add the required new test coverage

- [x] 3.1 In `apps/api/tests/sweep.test.ts` (or a new file if it reads more naturally separately — decide based on how large the file gets), add an A→B→A end-to-end test: create a series with content state A, run the sweep, start a real `Worker` (using `processDictionaryBuild` and the real `storage.ts` module, matching `build.test.ts`'s convention) and let it complete; assert `Series.contentHash` equals hashA. Edit content to state B, sweep again, let the worker complete it; assert `Series.contentHash` equals hashB. Edit content back to state A, sweep again, and assert a new job is actually created and processed to completion; assert `Series.contentHash` returns to hashA. Verify the test passes against real Postgres/Redis (note in the task's completion whether this sandbox has reachable infrastructure at the time — see task 5.2). (Written as new file `apps/api/tests/sweepRebuild.test.ts`, since `sweep.test.ts` already had 5 tests and this needed a real `Worker`/`Redis` lifecycle distinct from that file's queue-only setup. Verified live against real Postgres/Redis/MinIO — passes, confirming the exact revert-to-A scenario from the finding now produces a genuine third build.)
- [x] 3.2 In the same file, add a test for the active-job dedup branch: start a real `Worker` whose processor is held from completing momentarily (e.g. via a controllable promise/latch), trigger a sweep so a job becomes active, trigger a second sweep while it's still active, and assert exactly one job total exists (waiting + active) for that series. Let the processor complete and confirm no unexpected extra job appears afterward. Verify the test passes against real Postgres/Redis. (Refined during implementation: BullMQ's `keepLastIfActive` doesn't discard the second sweep's request when the identical content is already active — it stashes it and auto-promotes it to a real second job once the first finalizes, per design.md Decision 3. So "no unexpected extra job appears" would be the wrong assertion; the test instead asserts exactly one *visible* job immediately after the second sweep fires, then confirms the deferred request is honored — not lost — once the first job completes. Verified live — passes.)

## 4. Documentation

- [x] 4.1 Check `SPEC.md` for any prose describing the sweep's jobId-based dedup mechanism (e.g. near §7's build-automation description) and update it to match the `deduplication`-based mechanism if it exists; if `SPEC.md` doesn't describe the mechanism at this level of detail, no change is needed — confirm which is the case. (SPEC.md did describe it, at "### The hourly sweep" and the "Sweep logic" test-coverage bullet — both updated to describe `deduplication` and explain why the old deterministic-`jobId` approach was replaced.)

## 5. Verification

- [x] 5.1 `pnpm --filter @planetos/api exec tsc --noEmit` passes with all changes in place. (Clean.)
- [x] 5.2 `pnpm --filter @planetos/api test sweep` passes in full, including the two new tests. If this sandbox lacks reachable Postgres/Redis/MinIO, confirm any failure is a pure connection error, not a logic error, and note the limitation rather than claiming a live pass — check first whether the user's local `docker compose -f infra/docker-compose.yml up -d` from earlier in this session means infrastructure is now reachable. (Docker Desktop was live-verified: full suite 317/317 pass. Found and fixed a genuine bug in the new active-job dedup test itself — `waitFor` checked `Build` row *count* reaching 2, not that both had reached a terminal state, so the assertion could fire while the second build was still `RUNNING`. Not a bug in the fix; fixed the test's wait predicate and re-ran 5x consecutively with no flakiness before the full-suite run.)
- [x] 5.3 `openspec validate fix-sweep-job-dedup --type change --strict` passes. (Passed: "Change 'fix-sweep-job-dedup' is valid".)
- [x] 5.4 Read-through: confirm the rewritten doc comments in `sweep.ts` accurately describe the shipped mechanism and don't contradict design.md. (Confirmed accurate against design.md Decisions 1-2; polished one sentence for clarity.)
