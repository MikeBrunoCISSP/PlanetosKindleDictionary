## 1. Data model and migration

- [x] 1.1 In `apps/api/prisma/schema.prisma`, attempt to declare a GIN trigram index on `SeriesWord.normalizedWord` natively (`type: Gin`, `ops: raw("gin_trgm_ops")`); verify with `pnpm --filter api prisma validate`. Native syntax validated cleanly on Prisma 6 with no preview flag needed — `@@index([normalizedWord(ops: raw("gin_trgm_ops"))], type: Gin)`.
- [x] 1.2 Create the migration (`pnpm --filter api prisma migrate dev --name add_search_trgm_index`), including `CREATE EXTENSION IF NOT EXISTS pg_trgm;` and the GIN index on `SeriesWord.normalizedWord`. If the native schema syntax from 1.1 doesn't produce the right SQL or doesn't validate, hand-author `migration.sql` instead (via `--create-only`) and leave an explanatory comment in `schema.prisma`. Verify via `prisma migrate status` ("Database schema is up to date!") and a direct query confirming the index exists (`\d SeriesWord` or `pg_indexes`). Prisma's auto-generated migration omitted `CREATE EXTENSION IF NOT EXISTS pg_trgm;` (it doesn't manage extensions automatically), so the first `migrate dev` failed with "operator class gin_trgm_ops does not exist" (P3018); resolved via `prisma migrate resolve --rolled-back`, hand-added the `CREATE EXTENSION` statement to the generated `migration.sql`, then applied via `prisma migrate deploy`. Verified via `pg_indexes`: `SeriesWord_normalizedWord_idx` exists using GIN with `gin_trgm_ops`. Ran `prisma generate` afterward.

## 2. Shared package

- [x] 2.1 Add `packages/shared/src/search.ts`: `searchResultItemSchema` (`{ entryId, headword, headwordMatched, definitionExcerpt, inflections: { value, matched }[], seriesId, seriesSlug, seriesTitle }`), `searchResultsSchema` (`{ query, page, limit, totalCount, totalPages, items }`), and their inferred `SearchResultItemDto`/`SearchResultsDto` types. Export from `packages/shared/src/index.ts`.
- [x] 2.2 Add `definitionExcerpt(definitionHtml: string, maxLength = 256): string` to `packages/shared/src/sanitize.ts` (server-only, not the main barrel): strip all HTML tags via `sanitize-html` with an empty allowlist, trim, then truncate to `maxLength` characters + `"..."` if longer. Add unit tests: definition longer than 256 chars is truncated with `...`; definition ≤256 chars is returned in full with no `...`; entities (`&amp;` etc.) decode correctly after stripping. Correction to design.md's assumption: `sanitize-html` does NOT decode entities on its own — it re-escapes text nodes since its output is still HTML, not plain text (a stripped `&amp;` came back as literal `&amp;`, not `&`). Added an explicit small entity-decode step (`&amp; &lt; &gt; &quot; &#39; &apos;`) after stripping.
- [x] 2.3 Run `pnpm --filter shared build && pnpm --filter shared test`.

## 3. Backend: search endpoint

- [x] 3.1 Add `apps/api/src/routes/search.ts` exporting a `FastifyPluginAsync<{ prisma: PrismaClient }>` with `GET /api/search` (public, no `preHandler`): query schema `{ q: z.string().trim().min(1).max(200), page: z.coerce.number().int().min(1).default(1) }`, `PAGE_SIZE = 50` hardcoded (not client-adjustable).
- [x] 3.2 Implement query-word processing: lowercase `q`, split on whitespace, dedupe, cap at 10 words, escape LIKE metacharacters (`%`, `_`, `\`) per word for literal-substring semantics.
- [x] 3.3 Implement the ranked search query as a parameterized `$queryRaw` (via `Prisma.sql`/`Prisma.join` template interpolation only — never `$queryRawUnsafe`, never string concatenation) over `SeriesWord` joined to `Entry`/`Series`, filtered to `status = 'PUBLISHED' AND approvalStatus = 'APPROVED'`, ranked by `MIN(word ordinal) GROUP BY entryId`, ordered by rank then `sortKey` then `id`, with `LIMIT`/`OFFSET` for the requested page and `COUNT(*) OVER()` for the total match count.
- [x] 3.4 After the ranked query, fetch each returned entry's full inflection list via `prisma.inflection.findMany({ where: { entryId: { in: pageEntryIds } } })`; in TypeScript, mark `headwordMatched` and each inflection's `matched` flag via a case-insensitive substring check against the same word list from 3.2.
- [x] 3.5 Compute `definitionExcerpt` per result via the helper from task 2.2; assemble and return the `SearchResultsDto` response shape from task 2.1.
- [x] 3.6 Add `SEARCH_RATE_LIMIT = { rateLimit: { max: 60, timeWindow: "1 minute" } }` to `apps/api/src/plugins/rateLimit.ts`; attach via `{ config: SEARCH_RATE_LIMIT }` on the route.
- [x] 3.7 Register `searchRoutes` in `apps/api/src/index.ts` and `apps/api/tests/helpers.ts`.
- [x] 3.8 Add `apps/api/tests/search.test.ts` covering: case-insensitive substring match on headword; match via an inflection (not the headword); no match when the term only appears in the definition; multi-word query returns entries matching either word; entries matching the first word rank above entries matching only the second; an entry matching both words ranks by its earliest match (appears once, not twice); Pending/Rejected/Deleted entries are excluded even on an exact substring match; pagination returns exactly 50 results on a full page and the correct remainder on the last page; `totalCount`/`totalPages` are correct; a literal `%`/`_` in the query is treated literally, not as a wildcard; empty-results shape (`items: []`, no error). 16 tests, all passing (also added a "lists all inflections even when only the headword matched" test).
- [x] 3.9 Run `pnpm --filter api typecheck` and `pnpm --filter api lint`; confirm both pass. Full `pnpm --filter api test` also passes (147/147; one run hit transient flakiness from killing leftover zombie dev-server processes mid-session, a clean re-run passed fully).

## 4. Frontend: homepage and search UI

- [x] 4.1 Update `apps/web/src/routes/index.tsx`: remove the `beforeLoad` auth redirect entirely; add `validateSearch: z.object({ q: z.string().trim().max(200).optional(), page: z.coerce.number().int().min(1).default(1) })`; remove the "Signed in as X / Log out" content and its `useMe`/`apiLogout` usage (moved to `AppHeader.tsx` in task 4.5).
- [x] 4.2 Implement the two homepage layout states in `index.tsx`: no `q` → centered hero (reuse the existing `flex min-h-svh items-center justify-center` wrapper) with just a search `Input`/`form`; `q` present → a top-anchored search bar plus the `SearchResults` component from task 4.3. Enter in the search box navigates immediately (`search: { q: value, page: 1 }`).
- [x] 4.3 Superseded per direction: search is submit-driven, not search-as-you-type — no debounce hook. Added an explicit submit button (`ArrowRightIcon` from `lucide-react`, `aria-label="Search"`, no text label) directly to the right of the input in both layout states; Enter and clicking the button both submit the same `<form onSubmit>` handler, which navigates immediately (`search: { q: trimmed, page: 1 }`). Local input state still syncs from `Route.useSearch().q` on external URL changes (e.g. browser back). Deleted the now-unused `apps/web/src/lib/useDebouncedValue.ts` created under the original debounce approach. `design.md`'s "Search box behavior" decision updated to match.
- [x] 4.4 Add `apps/web/src/lib/api.ts`'s `apiSearchEntries(q: string, page = 1): Promise<SearchResultsDto>` following the existing `fetch → handleResponse<T>` pattern.
- [x] 4.5 Add `apps/web/src/components/SearchResults.tsx`: fetches via `useQuery({ queryKey: ["search", q, page], queryFn: () => apiSearchEntries(q, page), enabled: !!q })`; renders the results using the existing `Table`/`TableHeader`/`TableBody`/`TableRow`/`TableHead`/`TableCell` primitives with two columns ("Dictionary", "Word"); the word cell shows the headword in `<strong>` only when `headwordMatched`, the definition excerpt beneath in muted small text, and the inflection list beneath that with each `matched` inflection in `<strong>`. Loading/error/empty states follow house copy conventions ("Loading search results…" / "Failed to load search results." / `No results for "{q}".`).
- [x] 4.6 Add pagination controls to `SearchResults.tsx` (or a small extracted component): Previous/Next `Button`s (disabled at the boundaries) plus a "Page X of Y" indicator, driven by `navigate({ search: (p) => ({ ...p, page: p.page ± 1 }) })`; hidden entirely when there are no results. (Pagination controls live in `SearchResults.tsx` itself, since it's already the component that owns the fetched data and page state.)

## 5. Frontend: move identity/log-out into the header

- [x] 5.1 Update `apps/web/src/components/AppHeader.tsx`: pass the full `me` object to `AppMenu` (not just a derived `isAdmin` boolean); add a non-interactive username/email block at the top of the dropdown content (before "Dictionaries", not inside a collapsible section), followed by a `DropdownMenuSeparator`.
- [x] 5.2 Add a "Log out" `DropdownMenuItem` inside the existing "Settings" collapsible section, next to "Preferences"; move the logout handler (`apiLogout` → clear `ME_QUERY_KEY` → navigate to `/`) from `index.tsx` into `AppHeader.tsx`.

## 6. Documentation

- [x] 6.1 Update `SPEC.md` §6 "Public reads": add `GET /api/search ?q=&page=` with a short description of the matching/ranking/pagination behavior. Also added a "60 searches/min/IP" line to §6 Conventions' rate-limit list.
- [x] 6.2 Update `SPEC.md` §8 "Frontend > Routes": update the `/` line (currently "Series index, search + cards") to describe the search-grid homepage this change ships, superseding the "cards" phrasing.

## 7. End-to-end verification

- [x] 7.1 Run `pnpm --recursive typecheck`, `pnpm --filter api lint`, `pnpm --filter web lint`, and `pnpm --filter web build`; confirm all succeed. All clean (one stray `eslint-disable-next-line react-hooks/exhaustive-deps` comment in `index.tsx` was removed — that plugin/rule isn't configured in this project at all, so the disable directive itself was the only lint error).
- [x] 7.2 Run `pnpm --filter api test` and `pnpm --filter shared test`; confirm all new and existing tests pass. 147/147 and 41/41.
- [x] 7.3 Manually run the full app (real Postgres, both dev servers, driven via a real browser, logged out): confirm `/` shows only a centered search box with no redirect; search a headword substring and confirm the right entry appears with the headword bolded; search an inflection substring and confirm the same entry appears with the headword unbolded and the matching inflection bolded, alongside the entry's other (unbolded) inflections; search a two-word query and confirm first-word-matching entries rank above second-word-only matches; page through more than 50 results using Previous/Next; confirm a Pending entry never appears in results even on an exact substring match; log in, confirm "Signed in as X / Log out" now appears in the header dropdown and no longer appears on `/`, and that `/` still searches identically while logged in. All confirmed via Playwright against the real dev servers: logged out and stayed on `/` with the centered search box (no redirect); "sedai" matched headword "Aes Sedai" (bolded) with its full definition and bolded inflection "Aes Sedai's"; "dai's" matched only the inflection (headword left unbolded, inflection bolded); "dragon fire" ranked "Dragonmount" (first-word match) above "Wildfire" (second-word-only match); 55 test entries paginated correctly (Page 1 of 2 → 50 results, Page 2 of 2 → 5, Previous/Next enabled/disabled correctly at each boundary); a Pending entry never appeared in results (verified via the UI query and a direct API call); clearing the search box returned to the hero layout; logged in as admin and confirmed the identity block (username/email) and "Log out" now live in the header dropdown's Settings section, `/` shows pure search with no dashboard content, and search behaves identically while logged in. Test data cleaned up afterward via `DELETE /api/series/:slug` (cascades).
