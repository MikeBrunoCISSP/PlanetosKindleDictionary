## 1. Backend validation and creation

- [x] 1.1 In `apps/api/src/routes/entryImports.ts`, replace the `typeof rawValue !== "string"` check with the new per-row shape checks per design.md Decision 1: `rawValue` is a non-null, non-array object (else skip, "Value is not an object."); `rawValue.Definition` is a string (else skip, "Definition is not a string."); `rawValue.Inflections`, if present, is an array (else skip, "Inflections is not an array."). Verify `pnpm --filter @planetos/api exec tsc --noEmit` passes.
- [x] 1.2 Clean each row's `Inflections` list per design.md Decision 1: filter out any value that fails `singleWordText`'s predicate (empty, whitespace-only, contains whitespace, or exceeds 200 chars) or matches the row's own headword case-insensitively, then de-duplicate what's left case-insensitively (keep first occurrence). This is a plain filter, not a row-skipping validation - nothing here produces a `skippedInvalid` entry. Only *after* cleanup, check the remaining count against `MAX_INFLECTIONS`; exceeding it still skips the whole row via the existing `skippedInvalid` path. Verify with new test cases in task 3.1.
- [x] 1.3 Inside the existing per-row `$transaction`, create the row's inflections and their `SeriesWord` rows following `apps/api/src/routes/entries.ts`'s exact pattern (`tx.inflection.createManyAndReturn` then one `tx.seriesWord.createMany` combining the headword's row with one per inflection), so a `P2002` from any of them surfaces as today's duplicate-skip path. Verify with new test cases in task 3.1.
- [x] 1.4 Update the CREATE `Revision` snapshot (currently hardcoded `inflections: []`) to record the row's actual created inflection values. Verify with a new test case in task 3.1.
- [x] 1.5 Add `droppedInflectionCount: z.number().int()` to `importEntriesResultDtoSchema` in `packages/shared/src/entryImport.ts`, and accumulate it in `entryImports.ts` across the whole file per design.md Decision 7 (each row's cleanup step in task 1.2 adds however many values it removed to a running total, included in the final `result`). Verify `pnpm --filter @planetos/shared exec tsc --noEmit` and `pnpm --filter @planetos/api exec tsc --noEmit` pass. Implementation note beyond the literal design text: the running total is only incremented once a row is confirmed created (after the transaction succeeds), not at cleanup time - otherwise a row later skipped for exceeding `MAX_INFLECTIONS` or as a duplicate would incorrectly contribute to the count (task 3.2 requires it stays 0 for those cases).

## 2. Shared/client types

- [x] 2.1 In `apps/web/src/lib/api.ts`, change `apiImportEntries`'s `entries` parameter type from `Record<string, string>` to `Record<string, { Definition: string; Inflections: string[] }>`. Verify `pnpm --filter @planetos/web exec tsc --noEmit` passes. (Web typecheck won't pass in full until `import.tsx` itself is updated in task 4.1 - this task's own scope, the exported function's signature, is done.)

## 3. Backend tests

- [x] 3.1 In `apps/api/tests/entryImports.test.ts`, rewrite every existing test payload from the old flat-string shape to the new `{ Definition, Inflections }` object shape (all currently use the old shape - this is a full rewrite of the file's fixtures, not additive). Add new cases covering: a row is created with its inflections and they appear on the resulting entry; a row whose `Inflections` contains a value with a space still creates the entry, with that inflection dropped and the row's other valid inflections kept; a row whose `Inflections` duplicates its own headword still creates the entry, with that inflection dropped; a row whose `Inflections` contains a same-row duplicate (case-insensitive) still creates the entry, keeping only the first occurrence; a row whose `Inflections` contains an empty/whitespace-only or oversized (>200 char) value still creates the entry, with that inflection dropped; a row whose `Inflections` exceeds `MAX_INFLECTIONS` *after* cleanup is skipped entirely, while a row that only exceeds it *before* cleanup (e.g. due to duplicates) is NOT skipped once cleaned; a row whose inflection collides (case-insensitively) with an existing headword/inflection elsewhere in the dictionary is still skipped as a duplicate (unchanged, cross-entry case); a value that is a plain string (old shape) is skipped with a clear reason; a value missing `Definition` is skipped; a value whose `Inflections` is not an array is skipped; the CREATE revision snapshot for an imported entry includes its (cleaned) inflections. Verified: `pnpm --filter @planetos/api exec vitest run tests/entryImports.test.ts` - 21/21 pass.
- [x] 3.2 Add test cases asserting `droppedInflectionCount`: it is `0` when nothing was dropped anywhere in the file; it correctly totals the number of dropped values (not the number of affected rows) when multiple rows each drop a different number of inflections; it stays `0` for a row skipped entirely as a duplicate or as malformed (a skipped row's would-be inflections were never "created and then cleaned," so they don't count as dropped). Verified as part of the same 21/21 passing run above.

## 4. Frontend page

- [x] 4.1 In `apps/web/src/routes/entries/import.tsx`, update `handleFileChange`'s client-side shape check per design.md Decision 4 (every value must be a non-null, non-array object with a string `Definition` and, if present, an array `Inflections`), updating `parsedEntries`'s state type to match. Verify `pnpm --filter @planetos/web exec tsc --noEmit` passes. Also updated the bottom explanatory paragraph to mention inflection cleanup, beyond the task's literal text, for copy accuracy/coherence with the new behavior.
- [x] 4.2 Rebuild the "Expected format" `<pre>` sample to the new nested shape (Abelon with empty `Inflections`, Braavos with two: `["Braavosi", "Braavosian"]` - the exact sample already shown to and approved by the user), and add a third legend dot/label for "inflections" alongside the existing headword/definition dots. Update the page's intro paragraph to mention inflections. Typecheck passes; visual confirmation still pending in task 6.
- [x] 4.3 Restack the file-format section from its current side-by-side two-column row to the approved "Option B" layout: the file-picker block as a compact row on top, the sample block full-width below it. Typecheck passes; visual confirmation still pending in task 6.
- [x] 4.4 In the mutation's `onSuccess` handler, branch per design.md Decision 7: when `result.droppedInflectionCount > 0`, call `toast.warning(...)` with the existing counts message plus an appended "Not all inflections could be included." sentence; otherwise call `toast.success(...)` exactly as today. Typecheck passes; browser verification still pending in task 6.

## 5. Documentation

- [x] 5.1 If `SPEC.md` or any other repo-level doc describes the bulk-import file format, update it to match the new shape. Search first to confirm whether such a reference exists before editing. Confirmed via `grep -i "import" SPEC.md`: no reference to bulk import exists anywhere in `SPEC.md` - nothing to update.

## 6. Browser verification

- [x] 6.1 Start the web + API dev servers as an admin. Confirm the "Expected format" sample renders correctly in the new stacked layout, at both a normal desktop width and a narrow (~400px) width. Verified via Playwright screenshots at 900px and 400px viewports - layout matches the approved "Option B" mockup; the sample scrolls horizontally within its own box at narrow width rather than breaking the page.
- [x] 6.2 Upload a JSON file using the old flat-string shape; confirm it's rejected client-side with a clear inline error and the Import button stays disabled. Verified: inline error "Every value in the file must be an object like..." shown, Import stayed disabled.
- [x] 6.3 Upload a JSON file using the new shape with a mix of valid rows (some with inflections, some without), a row with an invalid/duplicate inflection (confirm the entry is still created, with only that inflection missing), and a row whose inflection duplicates an existing entry elsewhere in the dictionary (confirm that whole row is skipped); select a dictionary; import; confirm the result toast's counts match, and confirm in the UI that the created entries show their imported (cleaned) inflections. Verified against a dedicated "Import Verify Dictionary" test series with a pre-existing "Griffin" entry: Wolf (dup+multi-word inflections) created with only ["Wolves"]; Dragon created cleanly; Roc (inflection "Griffin" collides cross-entry) fully skipped, no entry created - confirmed directly via Prisma query.
- [x] 6.4 Confirm the completion toast from 6.3 (which included a dropped inflection) renders as a warning, not a plain success, and mentions that not all inflections could be included. Then import a second, "clean" file with no invalid/duplicate inflections and confirm that one shows the ordinary plain success toast. Verified via a DOM-level check of the live toast element (`[data-sonner-toast]`): a drop-affected import rendered `data-type="warning"` with text "1 created. Not all inflections could be included."; a clean import rendered `data-type="success"` with text "1 created." Test fixture data cleaned up afterward.

## 7. Final verification

- [x] 7.1 `pnpm run typecheck` passes in full. Verified.
- [x] 7.2 `pnpm --filter @planetos/api test` passes in full - no regressions to any other existing suite. Verified: 42/42 test files, 446/446 tests pass.
- [x] 7.3 `openspec validate update-import-json-shape --type change --strict` passes. Verified: "Change 'update-import-json-shape' is valid".
