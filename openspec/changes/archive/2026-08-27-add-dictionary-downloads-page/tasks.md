## 1. Backend: filename helper

- [x] 1.1 Create `apps/api/src/lib/filename.ts` exporting `sanitizeForFilename(input: string): string` (replaces runs of non-`[A-Za-z0-9]` characters with a single `-`, trims leading/trailing `-`, preserves case) and `buildDictionaryFilename(title: string, buildDate: Date): string` (returns `<sanitized-title>_<ddMMMyyyyhhmm>.epub` using local server time, 24-hour hour, 3-letter capitalized month abbreviation). Add unit tests (`apps/api/tests/lib/filename.test.ts`) covering: a plain alphanumeric title is unchanged; spaces/punctuation collapse to single hyphens with case preserved; leading/trailing special characters are trimmed; day/hour/minute are zero-padded; all 12 months produce the correct abbreviation; two different `buildDate`s for the same title produce different filenames. Verify with `pnpm --filter api test`. 18 new tests passing (including all 12 month abbreviations parameterized); full API suite 227/227.

## 2. Backend: presigned URL filename support

- [x] 2.1 Update `getPresignedDownloadUrl` in `apps/api/src/lib/storage.ts` to accept an optional third parameter `downloadFilename?: string`, setting `ResponseContentDisposition: \`attachment; filename="${downloadFilename}"\`` on the `GetObjectCommand` when provided (omitted entirely when not, preserving current behavior for existing callers). Add/extend a test in `apps/api/tests/storage.test.ts` asserting a presigned URL generated with a filename actually serves that filename (fetch the URL, check the `content-disposition` response header) against the real dev MinIO. Verify with `pnpm --filter api test`. Verified against real running MinIO: fetched the presigned URL and asserted the exact `content-disposition` header value. 4/4 storage tests passing.

## 3. Backend: apply filename to existing per-series download route

- [x] 3.1 Update `GET /api/series/:slug/download` in `apps/api/src/routes/downloads.ts` to select the series' `title` alongside the build lookup, compute the filename via `buildDictionaryFilename(series.title, build.createdAt)`, and pass it to `getPresignedDownloadUrl`. Requires selecting `createdAt` on the build query (currently only `epubKey` is selected). Update `apps/api/tests/downloads.test.ts`'s existing download tests to assert the redirect target's presigned URL includes the expected `response-content-disposition` query parameter with the correct filename for that series/build. Verify with `pnpm --filter api test`. Extended the existing "302s to a presigned URL..." test to assert the redirect target's real `content-disposition` header matches `buildDictionaryFilename(series.title, build.createdAt)` exactly. 11/11 downloads tests passing.

## 4. Backend: all-dictionaries listing endpoint

- [x] 4.1 Implement `GET /api/downloads` (public, no preHandler) in `downloads.ts`: `prisma.series.findMany({ where: { builds: { some: { status: "SUCCESS" } } }, select: { slug: true, title: true }, orderBy: { title: "asc" } })`, return as `{ slug: string; title: string }[]`. Add tests: a series with a SUCCESS build appears; a series with only FAILED/no builds does not appear; results are ordered by title; response contains no `error`/`log`/build-internal fields. Verify with `pnpm --filter api test`. 2 new tests: SUCCESS-build series included and title-ordered while no-build/only-FAILED series are omitted; no-auth-required. 13/13 downloads tests, 230/230 full suite.
- [x] 4.2 Register any newly-needed route wiring (should already be covered since `downloadsRoutes` is registered in both `apps/api/src/index.ts` and `apps/api/tests/helpers.ts` — confirm no additional registration is needed). Run the full `pnpm --filter api test` suite and confirm no regressions. Confirmed via grep: already registered in both places, no change needed. Full suite 230/230 passing, no regressions.

## 5. Frontend: API client and downloads page

- [x] 5.1 Add `apiGetDownloads(): Promise<{ slug: string; title: string }[]>` to `apps/web/src/lib/api.ts` following the established `fetch → handleResponse<T>` pattern, hitting `GET /api/downloads`.
- [x] 5.2 Create `apps/web/src/routes/downloads.tsx`: fetches the list via `apiGetDownloads`, renders each dictionary's title with a `<a href="/api/series/{slug}/download">Download .epub</a>` (real navigation, not fetch-wrapped, matching the existing per-series page's pattern), an empty-state message when the list is empty, no `beforeLoad` guard (public route). Verify by visiting the page while logged out and confirming it renders without redirect and lists at least one seeded dictionary. Verified live via Playwright: `/downloads` renders logged-out with no redirect, listing "ASOIAF" with a working `/api/series/asoiaf/download` link.
- [x] 5.3 Run `pnpm --filter web exec vite build` to regenerate `apps/web/src/routeTree.gen.ts` for the new `/downloads` route; confirm the new route appears in the generated file. Confirmed via grep: `DownloadsRoute`/`/downloads` present in the generated route tree.

## 6. Frontend: homepage entry point

- [x] 6.1 In `apps/web/src/routes/index.tsx`, add a `Link to="/downloads"` reading "Download the latest dictionaries" beneath the search form in the landing (`!hasQuery`) view only. Verify by visiting `/` logged out and confirming the link is visible and navigates to `/downloads`. Verified live via Playwright: link renders beneath the search box logged-out, `href="/downloads"`.

## 7. Frontend: menu entry point and anonymous menu support

- [x] 7.1 Rework `apps/web/src/components/AppHeader.tsx`: change `AppMenu`'s prop type from `{ me: UserDto }` to `{ me: UserDto | null }`, derive `isAdmin = me?.role === "ADMIN"`, remove the `{me && <AppMenu me={me} />}` guard in `AppHeader` so `AppMenu` always renders (pass `me` as `null` when logged out). Gate the username/email header row and the "Entries"/"Administration" sections on `me` being non-null. Keep the "Dictionaries" section always rendered; keep "Create"/"Update"/"Delete" gated on `isAdmin` as today. Verify `pnpm --filter web exec tsc --noEmit` passes (the `UserDto | null` propagation is fully typed, no `any`). `AppHeader` now passes `me ?? null` (loading state treated as anonymous, matching the existing Log In link's own falsy check). `tsc --noEmit` clean, no `any` introduced.
- [x] 7.2 Add a "Download" `DropdownMenuItem` to the Dictionaries shelf, visible unconditionally (not gated on `isAdmin` or `me`), navigating to `/downloads` and closing the menu. Verify by opening the hamburger menu both logged out and logged in, expanding Dictionaries, and confirming "Download" is present and navigates correctly in both states. Verified live via Playwright in both states: anonymous menu → Dictionaries → "Download" click navigated to `/downloads`; admin menu → Dictionaries shows Create/Update/Delete/Download together.
- [x] 7.3 Confirm the anonymous hamburger menu shows only the Dictionaries section (no Entries, no Administration, no username/email row) and that the Dictionaries shelf for an anonymous visitor contains only "Download" (no Create/Update/Delete). Verify live: log out, open the hamburger menu, expand Dictionaries, confirm only "Download" is listed. Verified live via Playwright: anonymous menu shows exactly one top-level item ("Dictionaries", no Entries/Administration, no username/email row); expanding it shows exactly one shelf item ("Download").
- [x] 7.4 Run `pnpm --filter web lint` and `pnpm --filter web exec tsc --noEmit`; confirm both pass clean. Both clean.

## 8. Documentation

- [x] 8.1 Update `SPEC.md` §6 (HTTP API) to add `GET /api/downloads` under "Public reads", and note the filename behavior on the existing `GET /api/series/:slug/download` line. Done.
- [x] 8.2 Update `SPEC.md` §8 (Frontend > Routes) to add `/downloads`, and update the existing hamburger-menu-related notes if any describe it as authenticated-only. Added `/downloads` and a note on the homepage's `/` line about the new link. Grepped SPEC.md for hamburger-menu wording — none exists there (that behavior was only ever documented in the `navigation/app-menu` capability spec, already updated during propose), so no correction needed.

## 9. End-to-end verification

- [x] 9.1 Run `pnpm --filter api test`, `pnpm --filter shared test`, `pnpm --recursive typecheck`, `pnpm --filter api lint`, `pnpm --filter web lint`, and `pnpm --filter web build`; confirm all succeed. api 230/230, shared 51/51, typecheck clean across all 4 packages, both lints clean, web build succeeded (pre-existing chunk-size warning only, unrelated to this change).
- [x] 9.2 Manually verify against the real running dev stack: as an anonymous visitor, download a dictionary's EPUB from the homepage link, from the menu's Download item, and from the all-dictionaries page, confirming all three land on the same page and produce a file named `<Title>_<ddMMMyyyyhhmm>.epub` matching the dictionary's latest build; confirm the existing per-series `/series/:slug` page's EPUB download now uses the same filename pattern; confirm a dictionary with no successful build does not appear on the all-dictionaries page; confirm the anonymous hamburger menu shows only Dictionaries > Download, and that logging in as admin restores the full menu with Download alongside Create/Update/Delete. All verified live: homepage link and menu Download item both navigate to `/downloads` (anonymous); a real anonymous browser click on `/downloads`' "Download .epub" link produced a genuinely saved file named `ASOIAF_26Aug20261733.epub`, exactly matching `buildDictionaryFilename` and the latest build's `createdAt`; `curl` against `/api/series/asoiaf/download` confirmed the identical filename via the same shared route (per-series and all-dictionaries page necessarily match since both hit `GET /api/series/:slug/download`); created a real throwaway series with no build via the admin API, confirmed via `GET /api/downloads` it was omitted, then deleted it, leaving dev data unchanged; anonymous vs. admin menu states re-confirmed from task 7.2/7.3's live checks.
