## 1. Shared: reusable password requirement checks

- [x] 1.1 In `packages/shared/src/auth.ts`, extract the four password rules into an exported `passwordRequirements: PasswordRequirement[]` array (`{ id, label, test }`, per design.md decision 1), and rewrite `passwordSchema`'s `.superRefine` to iterate `passwordRequirements` and emit each unsatisfied rule's `label` as the issue message (identical text to today's hardcoded messages). Verify `pnpm --filter shared test` passes unmodified (no assertion changes) — this is the acceptance check that the refactor didn't change `passwordSchema`'s behavior. 57/57 passing, zero assertion changes. Used the original hardcoded message text verbatim as each requirement's `label`, per the task's "identical text" requirement.

## 2. Frontend: live password requirement checklist

- [x] 2.1 In `apps/web/src/routes/login.tsx`'s `RegisterForm`, add a `useWatch` on the `password` field and render a bulleted checklist beneath the Password field using `passwordRequirements` from `@planetos/shared`: each item shows the requirement's `label`, with an unsatisfied (gray, plain bullet or empty circle) vs. satisfied (green checkmark, e.g. `CheckIcon` from `lucide-react`) visual state computed live via `.test(password)`. Remove the `<FormMessage />` for the `password` field (per design.md decision 4 — the checklist replaces it). Verify by typing a password into the Register form and watching each requirement toggle live: starts all-unsatisfied with an empty field, individual items turn green as their rule is met, and an item reverts if a later edit un-satisfies it. Implemented via `useWatch({ control: form.control, name: "password" })` driving `passwordRequirements.map(...)` directly (decoupled from RHF's submit-time validation, per design.md decision 2); `CheckIcon`/`CircleIcon` from `lucide-react`, green text/icon when satisfied. Verified live via Playwright screenshots: empty field shows all 4 unsatisfied; "abc" shows only the lowercase item green; a full valid password shows all 4 green; shortening to "Abc123" (6 chars) correctly reverted only the min-length item back to unsatisfied while the other 3 stayed green.

## 3. Frontend: live confirm-password mismatch

- [x] 3.1 In the same `RegisterForm`, add a `useWatch` on `confirmPassword` and show a mismatch indicator adjacent to the Confirm Password field whenever `confirmPassword` is non-empty and differs from the current `password` value, clearing the moment they match (per design.md decision 3). Remove the `<FormMessage />` for `confirmPassword` (superseded by this live indicator, same reasoning as the password field). Verify by typing a password, then typing a non-matching confirm value (indicator appears immediately, no submit needed), then editing it to match (indicator disappears immediately). Verified live via Playwright: typing a non-matching confirm value showed "Passwords do not match" immediately with no submit click; editing it to match made the message disappear immediately.
- [x] 3.2 Run `pnpm --filter web exec tsc --noEmit` and `pnpm --filter web lint`; confirm both pass clean. Both clean.

## 4. End-to-end verification

- [x] 4.1 Run `pnpm --filter shared test`, `pnpm --recursive typecheck`, `pnpm --filter web lint`, and `pnpm --filter web build`; confirm all succeed. shared 57/57, typecheck clean across all 4 packages, lint clean, web build succeeded (pre-existing chunk-size warning only, unrelated to this change).
- [x] 4.2 Manually verify against the real running dev stack: open `/login?mode=register`, confirm the checklist is visible with all items unsatisfied before typing anything; type a password meeting one rule at a time and confirm each checklist item turns green individually as its own rule becomes satisfied (not all-or-nothing); delete characters to break a previously-met rule and confirm that item reverts; type a confirm-password value that doesn't match and confirm the mismatch indicator appears without clicking submit; edit it to match and confirm the indicator disappears; submit a fully valid registration and confirm it still succeeds exactly as before (this change doesn't touch submit-time behavior, only pre-submit feedback). All verified live via Playwright (screenshots + accessibility snapshots): checklist all-unsatisfied on an empty field; "abc" showed only the lowercase item green; a full valid password showed all 4 green; shortening back to "Abc123" reverted only the min-length item; mismatch text appeared/disappeared live with no submit; a full registration with a throwaway account (`e2e-register-feedback-test@example.com`) succeeded and redirected home exactly as before, then the throwaway account was deleted.
