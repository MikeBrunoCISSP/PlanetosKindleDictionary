## 1. Env loading + config module

- [x] 1.1 Create `apps/api/src/load-env.ts` — a side-effect-only module that runs `dotenv.config({ path: <repo-root>/.env })` using the same path logic the entrypoints use today. Verify importing it from a scratch script populates `process.env` from the repo `.env`. (`tsx -e "import('./src/load-env.ts')"` → 13 vars injected, `DATABASE_URL`/`REDIS_URL` present.)
- [x] 1.2 Create `apps/api/src/config.ts`: `import "./load-env.js"`, then a `zod` schema over `process.env` parsed **once**. Compute `strict = NODE_ENV !== "development" && NODE_ENV !== "test"`. Apply local defaults only when `!strict`; apply `min(32)` + placeholder rejection (old committed fallback + the two `.env.example` `change-me-…` strings) for `SESSION_SECRET` / `SETTINGS_ENCRYPTION_KEY`, `.url()` + scheme + non-localhost for `PUBLIC_BASE_URL`, `.url()` + scheme for `SMTP_URL` / `REDIS_URL`, non-empty for `S3_BUCKET` / `S3_ACCESS_KEY_ID` / `S3_SECRET_ACCESS_KEY`, `coerce.number().int().positive()` for `PORT`, only when `strict`. Export a frozen typed `config` (throws an `Error` with an aggregated `VAR — reason` message if parse failed) plus `assertConfigValid()`. Verify `pnpm --filter @planetos/api typecheck` passes. (`config.ts` exports `config` (deep-frozen), `assertConfigValid()`, and pure `validateEnv()` / `parseEnv()` for testing; `zod` used for `PORT` coercion. typecheck clean.)
- [x] 1.3 Add a focused test file `apps/api/tests/config.test.ts` that imports the schema/parser in isolation and asserts: strict + missing `REDIS_URL` → aggregated error naming it; strict + `SESSION_SECRET` = old fallback → rejected; strict + non-URL `PUBLIC_BASE_URL` → rejected with that var named; strict + `localhost` `PUBLIC_BASE_URL` → rejected; `development` mode + empty env → parses with documented defaults. Verify the file passes. (14 tests, all green.)

## 2. Entrypoint wiring

- [x] 2.1 In `apps/api/src/index.ts` make `./load-env.js` the first import and `./config.js` the second; immediately after the import block, guard first config access (`try { assertConfigValid() } catch (e) { console.error(e.message); process.exit(1) }`) so a bad config aborts before `new PrismaClient()` / `Fastify()`. Replace `Number(process.env["PORT"] ?? 3000)` with `config.port`. Verify `pnpm --filter @planetos/api typecheck` passes and `NODE_ENV=production node dist/index.js` with a required var unset exits non-zero without listening. (index.ts: first import `./load-env.js`, then `./config.js`; `try { assertConfigValid() } catch { console.error; process.exit(1) }` before `new PrismaClient()`; `app.listen({ port: config.port })`. Removed the inline dotenv. typecheck clean. Runtime asserted in 6.2.)
- [x] 2.2 Same wiring in `apps/api/src/worker.ts`; replace `process.env["BUILD_CRON"] ?? "0 * * * *"` with `config.buildCron`. Verify `NODE_ENV=production node dist/worker.js` with missing config exits non-zero before any `new Worker(...)`. (worker.ts: same wiring, guard before `new PrismaClient()`; `pattern: config.buildCron`. typecheck clean. Runtime asserted in 6.4.)
- [x] 2.3 In `apps/api/tests/setup.ts` add `process.env.NODE_ENV ??= "test"` before the `dotenv` call. Verify `pnpm --filter @planetos/api test` still collects and runs (no strict-mode failure). (`tests/setup.ts` sets `process.env["NODE_ENV"] ??= "test"` before dotenv. Full suite still collects/runs: 21 files / 271 tests pass.)

## 3. Consumer refactor (no direct `process.env`)

- [x] 3.1 `apps/api/src/plugins/session.ts` — `config.redisUrl`, `config.sessionSecret`, `secure: config.isProduction`. `apps/api/src/plugins/cors.ts` — `config.publicBaseUrl`. `apps/api/src/plugins/rateLimit.ts` — `config.redisUrl`. Verify typecheck + `pnpm --filter @planetos/api test` green. (`session.ts` → `config.redisUrl` / `config.sessionSecret` / `secure: config.isProduction`; `cors.ts` → `config.publicBaseUrl`; `rateLimit.ts` → `config.redisUrl`. typecheck + suite green.)
- [x] 3.2 `apps/api/src/lib/queues.ts`, `lib/mailer.ts`, `lib/storage.ts` — keep the lazy connection/client caching, but read `config.*` instead of `process.env`; delete the "runs before dotenv" comments. Verify the sweep/build/storage tests still pass. (`queues.ts` / `mailer.ts` / `storage.ts` keep lazy caching, read `config.*`; stale dotenv-ordering comments replaced. sweep/build/storage tests pass.)
- [x] 3.3 `apps/api/src/lib/crypto.ts` — drop the hand-rolled `SETTINGS_ENCRYPTION_KEY` check, use `config.settingsEncryptionKey`; keep the SHA-256 derivation. Verify `apps/api/tests/lib/crypto.test.ts` passes. (`crypto.ts` drops the hand-rolled guard, uses `config.settingsEncryptionKey` + SHA-256. `tests/lib/crypto.test.ts` passes.)
- [x] 3.4 `apps/api/src/routes/auth.ts` — replace the three `PUBLIC_BASE_URL` reads for email links with `config.publicBaseUrl`. Verify `apps/api/tests/{auth,emailVerification,passwordReset}.test.ts` pass. (`routes/auth.ts` ×3 email-link `baseUrl` → `config.publicBaseUrl`. auth/emailVerification/passwordReset tests pass.)
- [x] 3.5 Grep `apps/api/src` for `process.env` — the only matches remaining are in `config.ts` and `load-env.ts`. Verify. (`grep -rn process.env apps/api/src` → only `config.ts` (the sanctioned reads + doc comments) and a `load-env.ts` comment.)

## 4. Seed script

- [x] 4.1 `apps/api/prisma/seed.ts` — import `../src/load-env.js` instead of its inline `dotenv.config`; reject `ADMIN_PASSWORD` that matches a known placeholder, and keep the existing `passwordSchema` complexity check + the "both must be set" guard. Verify `pnpm --filter @planetos/api seed` still upserts the admin locally. (seed.ts uses `../src/load-env.js`; rejects the `.env.example` `ChangeMe1!` sample; `pnpm --filter @planetos/api seed` → "Admin account upserted".)

## 5. Documentation

- [x] 5.1 `.env.example` — add a top note that the API/worker refuse to start unless `NODE_ENV` is `development`/`test` or every `[operator-set]` variable is present and valid; confirm `NODE_ENV=development` is already the sample value. (Added a "STARTUP VALIDATION" block at the top + reworded the `NODE_ENV` comment. Also added `NODE_ENV=development` to the local `.env` so `pnpm dev` keeps working.)
- [x] 5.2 `SPEC.md` §10 — note the strict-config behavior and that `NODE_ENV` must be `development` locally; cross-reference `infra/railway/README.md` for the production required set. Verify the §10 var list is unchanged otherwise. (Added a "Configuration validation" paragraph before "Production deployment"; the annotated var block is unchanged.)

## 6. Verification

- [x] 6.1 `pnpm --filter @planetos/api test` and `pnpm typecheck` pass with all changes. (`pnpm --filter @planetos/api test` → 21 files / 273 tests; `pnpm typecheck` → 0 TS errors across 4 workspaces + `.railway`.)
- [x] 6.2 Build (`pnpm run build:railway` or at least `pnpm --filter @planetos/api build`), then: `NODE_ENV=production PORT=8080 node apps/api/dist/index.js` with `SESSION_SECRET` unset → exits non-zero, prints an error naming `SESSION_SECRET`, never logs "Server listening". Repeat with `SESSION_SECRET` = the old fallback string → same. Repeat with everything valid (real Redis/DB/S3 from docker-compose + a strong secret + a non-localhost `PUBLIC_BASE_URL`) → boots and serves `/health`. (prod + placeholder `SESSION_SECRET` → exit 1, "must not be a placeholder", 0 listen lines. prod + localhost `PUBLIC_BASE_URL` + missing SMTP/S3 → exit 1, **all 5 problems listed together**. prod + all valid (local docker infra + strong secret + `https://dict.example.test`) → boots, `/health` → 200.)
- [x] 6.3 `NODE_ENV=development node apps/api/dist/index.js` with only the local `.env` → boots normally on defaults. (`/health` → `{"status":"ok"}`.)
- [x] 6.4 `node apps/api/dist/worker.js` with `NODE_ENV=production` and missing config → exits non-zero before "workers started". (worker scope validates only queues+storage+Prisma vars: missing `REDIS_URL` → exit 1, "Invalid worker configuration", 0 "workers started"; the 5 worker-required vars present → "workers started". Refined from the original whole-set check so the private Railway worker service — which has no `SESSION_SECRET`/`PUBLIC_BASE_URL` — can start; design Decision 2 updated.)
- [x] 6.5 `openspec validate add-startup-config-validation --strict` passes.
