## 1. Shared schema

- [x] 1.1 In `packages/shared/src/search.ts`, add `seriesIdsFilterSchema` per design.md Decision 1 (the Fastify single-value-vs-array `z.preprocess`, with the explanatory comment). Export the inferred type if useful. Verify `pnpm --filter @planetos/shared exec tsc --noEmit` passes.
- [x] 1.2 Add test cases to a shared package test file (e.g. `packages/shared/src/__tests__/search.test.ts`, following the existing `__tests__` convention) covering: `undefined` input stays `undefined`; a bare string input becomes a one-element array; an array input passes through unchanged; an empty array is rejected (`.min(1)`). Verify `pnpm --filter @planetos/shared test` and `pnpm --filter @planetos/shared exec tsc --noEmit` pass.

## 2. Backend

- [x] 2.1 In `apps/api/src/routes/search.ts`, splice `seriesIds: seriesIdsFilterSchema` (imported from `@planetos/shared`) into `searchQuerySchema`, and add the conditional `seriesFilterSql` fragment to the raw SQL query per design.md Decision 2 (spliced into the existing `WHERE` clause, no change to the ranking/matching CTE). Verify `pnpm --filter @planetos/api exec tsc --noEmit` passes.
- [x] 2.2 Add test cases to `apps/api/tests/search.test.ts` (reusing its existing `createTestSeries`/`createTestEntry` helpers) covering: a single `?seriesIds=<id>` (the critical regression pin for the string-vs-array gotcha) returns correct, non-empty results for an entry in that series; two repeated `?seriesIds=<id1>&seriesIds=<id2>` returns the union of matches from both series, excluding a match from a third, unselected series; a `seriesIds` value referencing a nonexistent series id returns `200` with `items: []`, not an error; omitting `seriesIds` entirely still returns matches across multiple series unfiltered (pins today's default behavior as unchanged). Verify all pass. Additionally (user-approved scope addition): scoped all pre-existing tests in this file with `&seriesIds=<their own test series>`, since real dictionary content in the shared dev database (a genuine "Morghul" entry in the real "asoiaf" series, plus other common fantasy-genre words) was intermittently colliding with these tests' search terms and causing flaky failures unrelated to this change. All 20 pre-existing + 4 new tests now pass.

## 3. Frontend API client

- [x] 3.1 In `apps/web/src/lib/api.ts`, widen `apiGetSeriesList` to accept an optional `limit` param and `apiSearchEntries` to accept a `seriesIds: string[]` param (appended as repeated `seriesIds` query-string keys, not comma-joined) per design.md Decision 3. Confirm the existing call site in `apps/web/src/routes/entries/new.tsx` still compiles unchanged (no `limit` passed there). Verify `pnpm --filter @planetos/web exec tsc --noEmit` passes.

## 4. Frontend component and page wiring

- [x] 4.1 Create `apps/web/src/components/DictionaryMultiSelect.tsx` per design.md Decision 4: fully controlled (`selectedIds`/`onChange` props), own dictionary-list query (`apiGetSeriesList(1, 200)`), reused `CommandDialog`/`CommandInput`/`CommandList`/`CommandGroup`/`CommandItem` with toggle-on-select + `data-checked` + dialog-stays-open, plus removable `Badge` chips for the current selection below the trigger. Verify `pnpm --filter @planetos/web exec tsc --noEmit` passes.
- [x] 4.2 In `apps/web/src/routes/index.tsx`, add `seriesIds: seriesIdsFilterSchema` to `homeSearchSchema`, render `<DictionaryMultiSelect>` in the results view only, and add the `handleSeriesIdsChange` handler per design.md Decision 5 (mapping an empty selection back to `undefined`, resetting `page` to 1). Verify `pnpm --filter @planetos/web exec tsc --noEmit` passes.
- [x] 4.3 In `apps/web/src/components/SearchResults.tsx`, add a `seriesIds: string[]` prop, include it in the `useQuery` `queryKey`, and pass it to `apiSearchEntries`. Confirm `goToPage` requires no change (per design.md, `...prev` already carries `seriesIds` forward). Verify `pnpm --filter @planetos/web exec tsc --noEmit` passes.

## 5. Browser verification

- [x] 5.1 Start the web + API dev servers, with at least two dictionaries each containing a distinctly-named entry (create fixtures if needed). Search a query matching entries in both dictionaries; confirm both appear unfiltered. Verified via Playwright against two fixture series ("Browser Verify Alpha"/"Beta") sharing a "Zynthquil" entry.
- [x] 5.2 Open the dictionary filter, select exactly one dictionary; confirm the URL gains a `seriesIds` param, results narrow to only that dictionary's matches, and the request is not rejected (the single-selection case that specifically regression-tests the Fastify array/string gotcha). Verified: selecting "Browser Verify Alpha" alone narrowed results to just that series.
- [x] 5.3 Select a second dictionary without deselecting the first; confirm results now include matches from both selected dictionaries and the selection is visible as chips without reopening the picker. Verified: both series' results returned, both shown as chips, dialog stayed open across both selections.
- [x] 5.4 Remove one selected dictionary directly via its chip's remove control (not via the picker); confirm results update to reflect only the remaining selection. Verified: removing the "Alpha" chip left only "Beta" in the URL and results.
- [x] 5.5 Clear the remaining selection; confirm results return to the full unfiltered set and `seriesIds` is no longer present in the URL. Verified: removing the last chip dropped `seriesIds` from the URL and restored both dictionaries' results.
- [x] 5.6 With one or more dictionaries selected and more than one page of results, click "Next"; confirm the filter is still applied on the new page and `seriesIds` remains in the URL. Verified with 60 pagination fixture entries in one series: page 2 kept `seriesIds` in the URL and results stayed scoped to that series.

## 6. Final verification

- [x] 6.1 `pnpm run typecheck` and `pnpm --filter @planetos/api test` (and `pnpm --filter @planetos/shared test`) all pass in full - no regressions to any existing suite, in particular the existing (unmodified-intent) cases in `apps/api/tests/search.test.ts`. Verified: full monorepo typecheck passes; `@planetos/shared test` 72/72 pass; `@planetos/api test` 431/432 pass, with `apps/api/tests/search.test.ts` itself at 20/20 (all pre-existing + 4 new). The one failure, `adminRegistrations.test.ts > lists pending registrations oldest-first...`, is a pre-existing, unrelated flake in a different capability (admin pending-users pagination hitting the same class of shared-dev-database data drift as the earlier "Morghul" issue, but in a file/route this change never touches and cannot fix via `seriesIds`) - confirmed pre-existing (untouched by this change's diff, reproduces in isolation on a clean run, introduced by an earlier already-committed change).
- [x] 6.2 `openspec validate add-search-dictionary-filter --type change --strict` passes. Verified: "Change 'add-search-dictionary-filter' is valid".
