## 1. Config — add `TRUST_PROXY_HOPS`

- [x] 1.1 In `apps/api/src/config.ts`, add `TRUST_PROXY_HOPS` to `DEV_DEFAULTS` (`"1"`), the `Config` interface (`trustProxyHops: number`), and `parseEnv()` (parsed via the `operational()` helper, coerced to a non-negative integer, same pattern as `port`). Verify `pnpm --filter @planetos/api exec tsc --noEmit` passes. (Typecheck clean.)
- [x] 1.2 In `validateEnv()`, add a format check in the "optional-but-if-set, regardless of scope" block (alongside the existing `PORT` check): reject `TRUST_PROXY_HOPS` when set to a non-numeric or negative value. Verify by extending `apps/api/tests/config.test.ts`. (Done; discovered and corrected a wording error in this change's own spec/design — format checks only run in strict mode, not "regardless of NODE_ENV" — spec.md and design.md updated to match actual `validateEnv()` behavior.)
- [x] 1.3 Extend `apps/api/tests/config.test.ts` with unit tests for `TRUST_PROXY_HOPS`: unset defaults to `1` in every mode, a valid explicit value (including `0`) parses correctly, a non-numeric or negative value is rejected regardless of `NODE_ENV`. Verify `pnpm --filter @planetos/api test config.test.ts` passes. (28/28 tests pass.)

## 2. Fastify — trust exactly one hop

- [x] 2.1 In `apps/api/src/index.ts`, construct the trust function `(_address, hop) => hop < config.trustProxyHops` and pass it as `trustProxy` to the `Fastify({...})` call. Add a code comment stating the single-trusted-hop assumption and that it depends on the `app` container never being reachable except through Railway's edge (per design.md Decision 1/Risks). Verify `pnpm --filter @planetos/api exec tsc --noEmit` passes. (Typecheck clean.)

## 3. Test harness — make proxied requests testable

- [x] 3.1 In `apps/api/tests/helpers.ts`, add an additive, optional parameter to `buildApp(opts: { trustProxy?; rateLimit?: boolean } = {})`: pass `opts.trustProxy` into the `Fastify({...})` call, and register `rateLimitPlugin` only when `opts.rateLimit` is true. Verify all 16 existing call sites still pass unmodified (`pnpm --filter @planetos/api test` — no regressions from this change alone). (Additive default `{}` preserves prior behavior; had to special-case `exactOptionalPropertyTypes` by only spreading `trustProxy` when defined. DB/Redis/MinIO-backed test files can't run in this sandbox — no Docker daemon available — so the 16 existing call sites are verified by typecheck + code review rather than a live run; see task 7.2 for details.)

## 4. Tests — trusted-hop resolution

- [x] 4.1 Create `apps/api/tests/trustProxy.test.ts` with a minimal app (via `buildApp({ trustProxy: (_a, hop) => hop < 1 })`) and a throwaway route echoing `request.ip`. Add scenario: a request with `remoteAddress` set to a simulated edge IP and `x-forwarded-for` set to a simulated client IP resolves `request.ip` to the client IP. Verify the test passes. (3/3 tests pass.)
- [x] 4.2 In the same file, add scenario: a request with `x-forwarded-for: "<attacker-ip>, <real-client-ip>"` (an extra client-supplied entry ahead of the trusted proxy's own entry) resolves `request.ip` to `<real-client-ip>`, not `<attacker-ip>` — the direct test for "forwarding entries beyond the trusted hop are ignored." Verify the test passes. (Passing.)
- [x] 4.3 In the same file, add a no-`trustProxy` baseline scenario: the same headers without a trust function resolve `request.ip` to the raw `remoteAddress`, proving the header is ignored when untrusted. Verify the test passes. (Passing.)

## 5. Tests — rate limiting and Turnstile on the resolved client IP

- [x] 5.1 Create `apps/api/tests/rateLimit.test.ts` using `buildApp({ trustProxy: ..., rateLimit: true })` and a route decorated with a small rate-limit config. Add scenario: two requests sharing the same `remoteAddress` but different `x-forwarded-for` client IPs are rate-limited independently (distinct buckets). Verify the test passes. (Implemented; cannot be run live in this sandbox — no local Redis, `@fastify/rate-limit`'s Redis store fails with ECONNREFUSED at request time. Confirmed this is an environment gap, not a logic error: the rest of the pre-existing DB/Redis-backed suite fails the same way here. See task 7.2.)
- [x] 5.2 In the same file, add scenario: repeated requests from the same resolved client IP hit the configured limit and receive `429`. Verify the test passes. (Same environment limitation as 5.1 — implemented, not live-verified here.)
- [x] 5.3 Extend `apps/api/tests/lib/turnstile.test.ts` to assert the outgoing `fetch` call's body (`remoteip` field) matches the `remoteIp` argument passed into `verify()`. Verify the test passes. (8/8 tests pass — this file needs no DB/Redis.)
- [x] 5.4 Extend `apps/api/tests/auth.test.ts` with one integration test using `buildApp({ trustProxy: ... })`, Turnstile enabled/mocked, injecting `POST /api/auth/register` with `remoteAddress` and `x-forwarded-for` set, asserting the `remoteip` sent to the mocked Turnstile siteverify call equals the resolved client IP, not the raw proxy address. Verify the test passes. (Implemented; confirmed the failure in this sandbox is `Can't reach database server at localhost:5432` in `beforeAll`, identical to every other file in this suite — not a regression. Not live-verified here; see task 7.2.)

## 6. Documentation

- [x] 6.1 In `infra/railway/README.md`, add a short subsection stating the `app` service trusts exactly one proxy hop (`TRUST_PROXY_HOPS`, default `1`), why (Railway's edge is the only documented network path to the container), and the residual-risk follow-up note (no monitoring built; a manual post-deploy log check is suggested). Add `TRUST_PROXY_HOPS` to the variable-reference table with a note that an operator may need to change it if Railway's topology assumption is ever wrong. Verify the new subsection and table row read consistently with design.md. (Added as §4.1, alongside the public-domain section; table row added with a note clarifying it's not a typical set-and-forget default.)
- [x] 6.2 In `.env.example`, add `TRUST_PROXY_HOPS=1` with a comment explaining the single-hop assumption and cross-referencing `infra/railway/README.md`. (Done.)
- [x] 6.3 In `SPEC.md`, near the existing rate-limit description, add a note that "/IP" means the client IP resolved through Railway's trusted edge hop, not the raw connection peer. (Done.)

## 7. Verification

- [x] 7.1 `pnpm --filter @planetos/api exec tsc --noEmit` passes with all changes in place. (Clean.)
- [x] 7.2 `pnpm --filter @planetos/api test` passes in full (existing suite plus new/extended tests), confirming no regression to the 16 existing `buildApp()` call sites. (This sandbox has no Docker daemon, so the pre-existing Postgres/Redis/MinIO/Mailpit-backed suite already fails/skips before this change — confirmed via a `docker ps` check and by reproducing the same `ECONNREFUSED`/"Can't reach database server" errors on files untouched by this change. Full-suite delta from before → after this change's test additions: 6→8 failed, 59→64 passed, 225→226 skipped. The +5 passed are exactly the new infra-independent tests (2 in `lib/turnstile.test.ts`, 3 in `trustProxy.test.ts`); the +2 failed are `rateLimit.test.ts`'s two tests (blocked by missing local Redis, confirmed via ECONNREFUSED — not a logic error, reproduced with `docker ps` unavailable); the +1 skipped is `auth.test.ts`'s new case, skipped because that file's pre-existing top-level `beforeAll` already fails on missing Postgres, same as all its other tests. No pre-existing file's pass/fail outcome changed. The two infra-blocked additions are implemented per spec but not live-verified in this environment — flagging for the user to confirm locally with `docker compose up` or in CI.)
- [x] 7.3 `openspec validate add-trusted-proxy-config --type change --strict` passes. (Passed: "Change 'add-trusted-proxy-config' is valid".)
- [x] 7.4 Read-through: confirm every new cross-reference (runbook ↔ `.env.example` ↔ `SPEC.md` ↔ the two spec deltas) resolves and reads consistently. (Confirmed: `infra/railway/README.md` §4.1 exists and is what `.env.example` and `SPEC.md` point to; §4.1 in turn points at §10 correctly for rollback context.)
- [x] 7.5 Note for the operator (record in the runbook, not automatable here): after deploying, confirm via `railway logs --service app` that a real request's resolved client IP matches the actual requester, not Railway's internal edge address. (Recorded in runbook §4.1's "Residual risk" paragraph.)
