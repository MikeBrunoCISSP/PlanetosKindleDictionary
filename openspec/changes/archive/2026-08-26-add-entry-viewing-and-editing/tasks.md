## 1. Data model and migration

- [x] 1.1 In `apps/api/prisma/schema.prisma`, add `enum EntryEditProposalStatus { PENDING APPROVED REJECTED }`, the `EntryEditProposal` model (fields: `id, entryId, entry, proposedDefinitionHtml, status, submittedById, submittedBy, reviewedById, reviewedBy, reviewedAt, rejectionNote, baseEntryUpdatedAt, createdAt, inflections`, `@@index([status, createdAt])`, `@@index([entryId])`), and the `EntryEditProposalInflection` model (`id, proposalId, proposal, value`, `@@index([proposalId])`). Add `submittedEntryEditProposals`/`reviewedEntryEditProposals` relation fields to `User` (relation names `"EntryEditProposalSubmittedBy"`/`"EntryEditProposalReviewedBy"`, distinct from `Entry`'s existing `"EntrySubmittedBy"`/`"EntryReviewedBy"`). Add a comment on `Entry.updatedAt` noting it doubles as the edit-proposal concurrency token and must keep advancing on every entry-content mutation. Verify with `pnpm --filter api prisma validate`. `prisma format` auto-added the required reverse relation `Entry.entryEditProposals EntryEditProposal[]`. Validates clean.
- [x] 1.2 Create the migration (`pnpm --filter api prisma migrate dev --name add_entry_edit_proposals`) for the schema changes in 1.1, then hand-append the partial unique index Prisma can't express declaratively: `CREATE UNIQUE INDEX "EntryEditProposal_entryId_pending_unique" ON "EntryEditProposal" ("entryId") WHERE "status" = 'PENDING';`. Verify via `prisma migrate status` ("Database schema is up to date!") and a direct query confirming the partial index exists and is scoped to `status = 'PENDING'` (`pg_indexes`/`\d EntryEditProposal`). Run `prisma generate` afterward. `migrate dev` refused to run non-interactively (the earlier hand-edited pg_trgm migration's checksum no longer matched what's on disk, and it wanted a destructive `migrate reset`); avoided that entirely via `prisma migrate diff --from-migrations ... --to-schema-datamodel ... --shadow-database-url ...` to generate correct SQL without touching migration history or dev data, hand-wrote the migration folder, appended the partial index, and applied via `prisma migrate deploy`. Verified via `pg_indexes`: `EntryEditProposal_entryId_pending_unique` exists with `WHERE (status = 'PENDING'::"EntryEditProposalStatus")`.

## 2. Shared package

- [x] 2.1 In `packages/shared/src/entries.ts`, factor `definitionHtmlSchema = z.string().trim().min(1, "Definition is required").max(5000, "Definition must be at most 5,000 characters")` out and use it in `createEntrySchema`. Promote the module-local `DUPLICATE_WORD_MESSAGE` to `export const`. Update `entries/new.tsx` to import it instead of its own local copy. Add/adjust unit tests: whitespace-only `definitionHtml` is now rejected by `createEntrySchema` (closing the trim gap); existing create-entry tests that may have assumed the old permissive behavior are reviewed and updated if needed. New `packages/shared/src/__tests__/entries.test.ts` added; no existing test relied on the old permissive whitespace-only behavior (grep confirmed), so nothing needed updating.
- [x] 2.2 Add `submitEntryEditProposalSchema` (`{ definitionHtml: definitionHtmlSchema, inflections: z.array(plainText({max:200,...})).default([]) }`, `.refine()` for no internal duplicates) to `packages/shared/src/entries.ts`. No headword field — it isn't submitted.
- [x] 2.3 Add DTO schemas: `publicEntryDtoSchema` (`id, seriesId, seriesSlug, headword, definitionHtml, approvalStatus: "PENDING"|"APPROVED", inflections: {id,value}[]`), `entryEditProposalDtoSchema` (`id, entryId, status, submittedById, submittedAt, reviewedById, reviewedAt, rejectionNote, current: {headword, definitionHtml, inflections}, proposed: {definitionHtml, inflections}`), `pendingQueueItemDtoSchema` (discriminated union: `{type:"NEW_ENTRY", id, headword, createdAt}` | `{type:"EDIT", id, entryId, headword, createdAt}`). Export inferred types (`PublicEntryDto`, `EntryEditProposalDto`, `PendingQueueItemDto`).
- [x] 2.4 Run `pnpm --filter shared build && pnpm --filter shared test`. 51/51 passing.

## 3. Backend: public entry detail

- [x] 3.1 In `apps/api/src/routes/entries.ts`, add `GET /api/entries/:id` (public, no `preHandler`): 404 unless `status === "PUBLISHED"` and `approvalStatus` is `"APPROVED"` or `"PENDING"` (404 for `REJECTED` or `DELETED`); return `PublicEntryDto` including `seriesSlug` (join `Series`) and `approvalStatus`, never `submittedById`/`reviewedById`/`reviewedAt`/`rejectionNote`.
- [x] 3.2 Add tests covering: Approved entry is visible to an anonymous request; Pending entry is visible (with `approvalStatus: "PENDING"` in the response) to an anonymous request; Rejected entry 404s; Deleted (soft-deleted) entry 404s; response never includes `submittedById`/`reviewedById`/`reviewedAt`/`rejectionNote`. New `apps/api/tests/entryDetail.test.ts`, 6 tests, all passing.

## 4. Backend: submitting an edit

- [x] 4.1 Add `apps/api/src/routes/entryEditProposals.ts` exporting a `FastifyPluginAsync<{ prisma: PrismaClient }>`. Add `POST /api/entries/:id/edit-proposals` with `preHandler: requireAuth` (not `requireApproved` — any authenticated user, per the authorization matrix). Body validated by `submitEntryEditProposalSchema`.
- [x] 4.2 Implement the handler: 404 if the entry doesn't exist or isn't `status: PUBLISHED, approvalStatus: APPROVED`; sanitize `definitionHtml` via `sanitizeDefinitionHtml`; within a `Serializable` transaction, check for an existing `PENDING` proposal for this entry (app-level check → `Errors.EDIT_ALREADY_PENDING()` for the friendly-message case), check proposed inflections against other entries' `SeriesWord` rows in the same dictionary excluding this entry's own words (→ `Errors.DUPLICATE_WORD()`), check no proposed inflection equals the entry's own headword or duplicates another proposed inflection (mirrors `createEntrySchema`'s refine, done here since headword isn't in the schema), capture `entry.updatedAt` as `baseEntryUpdatedAt`, and create the `EntryEditProposal` + its `EntryEditProposalInflection` rows. Catch a P2002 on the partial unique index as a race-safety backstop for `EDIT_ALREADY_PENDING`.
- [x] 4.3 Register `entryEditProposalRoutes` in `apps/api/src/index.ts` and `apps/api/tests/helpers.ts`.
- [x] 4.4 Add tests covering: valid submission creates a Pending proposal attributed to the submitter with a recorded timestamp and unchanged live entry; unauthenticated request is rejected (401) and creates nothing; submission against a non-existent entry 404s; submission against a Pending (not yet ever approved) entry 404s; blank/whitespace-only `definitionHtml` is rejected server-side; over-length `definitionHtml` is rejected; a proposed inflection matching another entry's headword/inflection is rejected with `DUPLICATE_WORD`; a proposed inflection matching the entry's own headword is rejected; a proposed inflection duplicating another proposed inflection in the same submission is rejected; the entry's own existing (retained) inflections do not conflict with themselves; a second submission while one is already Pending for the same entry is rejected with `EDIT_ALREADY_PENDING` and creates no second row; two concurrent submissions for the same previously-clean entry result in exactly one success. New `apps/api/tests/entryEditProposals.test.ts`. Found and fixed a real bug via the concurrency test: a losing concurrent transaction under `Serializable` isolation surfaces as Postgres error P2034 (serialization failure), not P2002 - the original code only caught P2002 and let P2034 fall through as an unhandled 500. Now caught at the outer `$transaction` call and mapped to `EDIT_ALREADY_PENDING`/`ALREADY_REVIEWED` as appropriate (applied to both the submission and approval transactions).

## 5. Backend: merged review queue and edit-proposal review

- [x] 5.1 Add `GET /api/admin/review-queue` (`requireAdmin`) to `entryEditProposals.ts` (or a shared admin-queue module): merge `Entry.findMany({approvalStatus: PENDING})` (id, headword, createdAt) and `EntryEditProposal.findMany({status: PENDING}, include entry.headword)` (id, entryId, headword, createdAt) into one `PendingQueueItemDto[]`, sorted oldest-first by their respective timestamp.
- [x] 5.2 Add `GET /api/admin/entry-edit-proposals/:id` (`requireAdmin`): 404 if missing; return `EntryEditProposalDto` with the entry's *live* current headword/definition/inflections plus the proposal's proposed definition/inflections.
- [x] 5.3 Add `POST /api/admin/entry-edit-proposals/:id/approve` (`requireAdmin`, `Serializable` transaction) implementing the 9-step transaction from design.md: 404 missing proposal/entry; `ALREADY_REVIEWED` if not Pending; `STALE_ENTRY_REVISION` if `entry.updatedAt !== proposal.baseEntryUpdatedAt` (leaves proposal Pending); re-validate inflection uniqueness against current dictionary state (→ `DUPLICATE_WORD`, leaves proposal Pending); diff current vs. proposed inflections; `tx.entry.update()` with the proposed definition (always executed, to reliably bump `updatedAt`); delete removed / create added inflections + their `SeriesWord` rows (P2002 → `DUPLICATE_WORD`, rolls back); mark proposal `APPROVED` with reviewer/timestamp; write one audit `Revision` row (`action: "UPDATE"`).
- [x] 5.4 Add `POST /api/admin/entry-edit-proposals/:id/reject` (`requireAdmin`): body `{ note? }` via `rejectEntrySchema` (reused); 404 missing; `ALREADY_REVIEWED` if not Pending; set `REJECTED` + reviewer/timestamp/note; no `Entry` mutation, no `Revision` row.
- [x] 5.5 Add `EDIT_ALREADY_PENDING` and `STALE_ENTRY_REVISION` to `apps/api/src/lib/errors.ts`. Done early (needed by task 4.2's submission handler too).
- [x] 5.6 Add tests covering: merged queue returns both a Pending new entry and a Pending edit proposal, oldest-first, each correctly typed; non-pending items of either kind are excluded; non-admin/unauthenticated requests to all four endpoints are rejected; edit-proposal detail view returns live current values alongside the stored proposed values; approving updates the entry's definition/inflections atomically, marks the proposal Approved, and removes it from the queue; approving with a stale `baseEntryUpdatedAt` (simulate by updating the entry after proposal submission) is refused with `STALE_ENTRY_REVISION`, entry unchanged, proposal still Pending; approving when another entry has since claimed one of the proposed inflections is refused with `DUPLICATE_WORD`, entry unchanged, proposal still Pending; approving an already-reviewed proposal returns `ALREADY_REVIEWED`; rejecting persists an optional note and leaves the entry completely unchanged; rejecting an already-reviewed proposal returns `ALREADY_REVIEWED`; two concurrent approval attempts on the same proposal result in exactly one success. 25 tests total in `entryEditProposals.test.ts`, all passing.
- [x] 5.7 Run `pnpm --filter api typecheck` and `pnpm --filter api lint`; confirm both pass. Both clean.

## 6. Frontend: entry detail and edit page

- [x] 6.1 Add `apps/web/src/lib/api.ts` functions: `apiGetEntryPublic(id)`, `apiSubmitEntryEditProposal(id, data)`, `apiGetReviewQueue()`, `apiGetEntryEditProposal(id)`, `apiApproveEntryEditProposal(id)`, `apiRejectEntryEditProposal(id, data)`.
- [x] 6.2 Add `apps/web/src/routes/entries/$id.tsx` (flat, no `beforeLoad` redirect): fetches the entry via `useQuery`; loading/error states following house copy conventions; renders Headword (primary), read-only Definition, Inflections list (or a clean "No inflections." empty state); shows a "This entry is awaiting administrator approval." banner when `approvalStatus === "PENDING"`.
- [x] 6.3 Add the Edit button (upper-left), shown only when `useMe()` returns a user AND `approvalStatus === "APPROVED"`. Clicking it switches the page into `mode: "edit"` locally (one component, not a separate route/page).
- [x] 6.4 Build edit mode: Headword shown read-only; Definition as a `Textarea` (reusing the Add-Entry-screen conventions) seeded from the fetched entry; Inflection add/remove reusing the `Badge`+X-icon chip pattern from `entries/new.tsx`, with the duplicate-check word set built from `apiGetSeriesWords(seriesSlug)` minus this entry's own known headword/inflections (structural self-exclusion, no backend change) — duplicate hit shows `toast.error(DUPLICATE_WORD_MESSAGE)` (imported from shared) same as Add Entry.
- [x] 6.5 Implement normalized-value dirty-tracking: capture a normalized baseline ref on entering edit mode; compute `isReallyDirty` via `watch()` against that baseline (not RHF's `formState.isDirty`); Submit (bottom-right) disabled unless `isReallyDirty && formState.isValid && definitionHtml is non-blank`.
- [x] 6.6 Wire Submit to `apiSubmitEntryEditProposal`, guarded by mutation-pending state against double-submission; on success show `toast.success("Your edit has been submitted for approval.")` only after server confirmation, and return to read-only mode showing the still-approved (unchanged) content — never showing the just-submitted proposed values as if live.
- [x] 6.7 Add a Cancel control in edit mode: discards the draft, returns to read-only mode showing the fetched approved content, creates no proposal.
- [x] 6.8 Add Playwright/manual-test notes aside; add any frontend unit tests the project's existing conventions call for (this project's test suite is API-integration-focused — confirm during implementation whether any frontend unit tests exist to extend, and note the finding in this task's completion notes rather than inventing a new test layer if none does). Confirmed: no frontend unit-test tooling exists anywhere in `apps/web` (no vitest config, no testing-library, no `.test.tsx` files) — the project's test suite is entirely API-integration-focused. No new test layer invented; covered instead by the manual Playwright walkthrough in task 10.3.

## 7. Frontend: search result hyperlinks

- [x] 7.1 Update `apps/web/src/components/SearchResults.tsx`: wrap each result's headword in a `Link` (TanStack Router) to `/entries/$id` using `item.entryId`, preserving the existing bold-when-matched styling. No other change to search behavior, request shape, or ranking.

## 8. Frontend: Approval Queue extension

- [x] 8.1 Update `apps/web/src/routes/admin_.approval-queue.tsx`: replace its `apiGetPendingEntries()` query with `apiGetReviewQueue()`; add a Type column (`Badge`: "New Entry" / "Edit") to the table.
- [x] 8.2 Branch each row's Headword-click and Approve/Reject actions on `item.type`: `NEW_ENTRY` rows keep using the existing `EntryDetailsDialog`/`apiApproveEntry`/`apiRejectEntry`; `EDIT` rows use a new `EntryEditProposalDetailsDialog` (fetching via `apiGetEntryEditProposal`) and `apiApproveEntryEditProposal`/`apiRejectEntryEditProposal`. Reuse the existing per-row busy-state/row-error/no-optimistic-removal pattern for both.
- [x] 8.3 Build `EntryEditProposalDetailsDialog`: shows Headword, current Definition, proposed Definition (with a "(changed)" indicator when they differ after normalization), current Inflections and proposed Inflections with added/removed entries visually distinguished (simple normalized-set diff, no diff library) — reuse the existing reject-dialog note pattern for the Reject action.
- [x] 8.4 Add tests covering: queue lists both a pending new entry and a pending edit proposal together, oldest-first, with correct Type badges; clicking an Edit row's headword opens the edit-proposal dialog showing current and proposed values; Approve/Reject on an Edit row call the correct edit-proposal endpoints and leave New-Entry rows/endpoints untouched, and vice versa. No frontend unit-test tooling exists in this project (confirmed via grep, same finding as task 6.8: no vitest config, no testing-library, no `.test.tsx` files anywhere — the test suite is entirely API-integration-focused). Deferring this coverage to the manual Playwright walkthrough in task 10.3 rather than introducing a new test layer for one component.

## 9. Documentation

- [x] 9.1 Update `SPEC.md` §6 "Authenticated writes"/"Public reads" (as appropriate) to document `GET /api/entries/:id`, `POST /api/entries/:id/edit-proposals`, and the admin review-queue/edit-proposal endpoints.
- [x] 9.2 Update `SPEC.md` §4 schema excerpt to add `EntryEditProposal`, `EntryEditProposalInflection`, `EntryEditProposalStatus`.
- [x] 9.3 Update `SPEC.md` §8 "Frontend > Routes" to add `/entries/$id`.

## 10. End-to-end verification

- [x] 10.1 Run `pnpm --recursive typecheck`, `pnpm --filter api lint`, `pnpm --filter web lint`, and `pnpm --filter web build`; confirm all succeed. All four passed clean (web build has a pre-existing >500kB chunk-size warning, not an error, unrelated to this change).
- [x] 10.2 Run `pnpm --filter api test` and `pnpm --filter shared test`; confirm all new and existing tests pass. 178/178 API tests passed (11 files, including the 25-test `entryEditProposals.test.ts` and 6-test `entryDetail.test.ts` added this change), 51/51 shared tests passed.
- [x] 10.3 Manually run the full app (real Postgres, both dev servers, real browser): as an anonymous visitor, click a search result's headword and confirm the detail page renders read-only with no Edit button; view a Pending entry's detail page directly and confirm the "awaiting approval" banner (and no Edit button); log in as a regular (non-approved-account) member and confirm the Edit button now appears on an Approved entry and edit mode works end-to-end (headword read-only, definition/inflection edits, dirty-gated Submit, duplicate-word toast, self-exclusion of the entry's own inflections, Cancel discards); submit an edit and confirm search/detail still show the old approved content until reviewed; as admin, confirm the Approval Queue shows both a pending new entry and the pending edit with correct Type badges, open the edit review dialog and confirm current-vs-proposed display with added/removed inflections visually distinguished, approve it and confirm the entry's live content updates everywhere (detail page, search) and the item leaves the queue; submit a second edit, have another admin session approve a *different* edit to the same entry first, then attempt to approve the first (now-stale) proposal and confirm it's refused with a clear message and stays Pending; submit an edit whose new inflection collides with a word introduced by an unrelated entry approved in the meantime, confirm approval is refused; confirm rejecting an edit proposal leaves the entry untouched and honors an optional note.

  All scenarios verified via Playwright against the real dev stack (Postgres, API on :3000, web on :5173), using freshly-registered/approved test accounts and a temporary entry, all cleaned up from the DB afterward:
  - Anonymous: search → headword link → detail page renders read-only, no Edit button.
  - Anonymous viewing a Pending entry directly: "This entry is awaiting administrator approval." banner shown, no Edit button.
  - Anonymous viewing a Rejected entry directly: "Entry not found." (404).
  - Logged-in approved member on an Approved entry: Edit button appears; edit mode shows Headword disabled/read-only, Submit disabled until a real change is made (dirty-gating), duplicate-word toast on both an existing dictionary word and the entry's own headword, Cancel discards the draft back to the read-only view unchanged.
  - Submitted edit left the live detail page and search results showing the old content unchanged (verified via a direct DB read of the entry's inflections mid-flight).
  - Admin Approval Queue showed a pending new entry ("New Entry" badge) and a pending edit ("Edit" badge) together, oldest first; the edit review dialog showed Current vs. Proposed Definition (with a "(changed)" indicator when they differ) and a `+ value` inflection diff.
  - Approving the edit updated the entry live everywhere (detail page inflection list, and the new inflection became searchable/bolded on the homepage) and removed it from the queue.
  - Stale-proposal refusal: submitted an edit, then updated the entry's `updatedAt` directly (simulating an out-of-band change) before approving — the UI showed an inline "This entry has changed since this edit was submitted. Please review the current entry before approving." error, and the proposal stayed Pending (confirmed via DB). Reject still worked on the stale proposal.
  - Word-collision refusal: submitted an edit proposing a new inflection, then approved an unrelated new entry using that same word as its headword, then attempted to approve the pending edit — refused inline with "The word already exists in the dictionary.", proposal stayed Pending.
  - Rejecting an edit proposal with a note left the entry's inflections untouched and persisted the note (verified via DB).
