## 1. Config

- [x] 1.1 In `apps/api/src/config.ts`, add `ADMIN_DIGEST_CRON` to `DEV_DEFAULTS` (e.g. `"0 13 * * *"`), read it via `operational("ADMIN_DIGEST_CRON")` into a new `adminDigestCron: string` field on `Config` — no strict-mode requirement check, matching `BUILD_CRON` exactly. Verify `pnpm --filter @planetos/api exec tsc --noEmit` passes.
- [x] 1.2 In the same file, add `ADMIN_DIGEST_RECIPIENT_EMAIL` to `DEV_DEFAULTS` (e.g. `"admin-digest@localhost"`), add it to `WORKER_REQUIRED`, and add a strict-mode validation block mirroring `CONTACT_RECIPIENT_EMAIL`'s exactly (required, email-shaped via the same regex, no sender-domain blocklist). Add `adminDigestRecipientEmail: string` to `Config` and `parseEnv()`. Verify `pnpm --filter @planetos/api exec tsc --noEmit` passes.
- [x] 1.3 Add test cases to `apps/api/tests/config.test.ts` for both new variables (present valid value passes; missing `ADMIN_DIGEST_RECIPIENT_EMAIL` fails strict-mode validation naming it; malformed email fails; `ADMIN_DIGEST_CRON` has no strict-mode requirement and defaults correctly when unset in dev/test), following the existing `CONTACT_RECIPIENT_EMAIL`/`BUILD_CRON` test cases as a template. Verify all pass. Also updated `VALID_STRICT_ENV` and the worker-scope "requires queues+storage+Prisma+mail" fixture to include `ADMIN_DIGEST_RECIPIENT_EMAIL`, since both are shared baselines other tests assert produce zero issues.

## 2. Digest logic

- [x] 2.1 Add `sendAdminDigestEmail({ pendingUsers, pendingEdits }: { pendingUsers: number; pendingEdits: number }): Promise<void>` to `apps/api/src/lib/mailer.ts` per design.md Decision 2 — subject `"Pending review digest"`, plain-text body stating both counts and a link to `${config.publicBaseUrl}/admin`, sent to `config.adminDigestRecipientEmail`, no `replyTo`. Verify `pnpm --filter @planetos/api exec tsc --noEmit` passes.
- [x] 2.2 Create `apps/api/src/lib/adminDigest.ts` exporting `runAdminDigest(prisma: PrismaClient): Promise<void>` per design.md Decision 1: counts pending users (`User.approvalStatus === "PENDING"`), pending new entries (`Entry.approvalStatus === "PENDING"`), and pending edit proposals (`EntryEditProposal.status === "PENDING"`) via `Promise.all`; sums the latter two into `pendingEdits`; returns without sending when both `pendingUsers` and `pendingEdits` are zero; otherwise calls `sendAdminDigestEmail`. Verify `pnpm --filter @planetos/api exec tsc --noEmit` passes.
- [x] 2.3 Add `apps/api/tests/lib/adminDigest.test.ts` covering: zero pending users and zero pending edits sends no email; nonzero pending users with zero pending edits sends one email reporting both numbers; zero pending users with nonzero pending edits (mix of new-entry and edit-proposal rows) sends one email; nonzero of both sends one email with correct totals. Mock or spy on `sendAdminDigestEmail` (matching how other lib tests isolate the mailer) rather than asserting on real outbound mail. Verify all pass. The local dev database carries leftover PENDING fixtures from other test suites, so the "nothing pending" case uses a fake Prisma-shaped object stubbing `count` to zero rather than asserting real global state; the other three cases compute a baseline via real counts first and assert baseline+delta, so they're robust to that same pre-existing data.

## 3. Worker wiring

- [x] 3.1 In `apps/api/src/worker.ts`, add a `send-admin-digest` branch to `processEmailQueueJob` that calls `runAdminDigest(prisma)`, and register `emailQueue.upsertJobScheduler("send-admin-digest", { pattern: config.adminDigestCron }, { name: "send-admin-digest", data: {} })` next to the existing `reconcile-pending-emails` scheduler, per design.md Decision 3. Verify `pnpm --filter @planetos/api exec tsc --noEmit` passes.
- [x] 3.2 Add a test exercising the worker's `send-admin-digest` job branch end-to-end against the real queue/worker test harness (matching how `reconcile-pending-emails` or an equivalent existing job is tested), confirming the job invokes the digest logic and a thrown error is caught by the worker's existing `"failed"` log handler rather than crashing the process. Verify it passes. `worker.ts` itself is an executable entry point (preflight checks, real queues, `process.exit` on bad config), not an importable module - like `tests/outboxWorker.test.ts` does for `send-email`, the new `tests/adminDigestWorker.test.ts` drives a real BullMQ `Worker`/`Queue` against real Redis replicating the `send-admin-digest` branch's exact shape, and proves a failed job is caught by a `"failed"` handler (mirroring `worker.ts`'s real one) without preventing a subsequent job from completing.

## 4. Docs

- [x] 4.1 Add `ADMIN_DIGEST_CRON` and `ADMIN_DIGEST_RECIPIENT_EMAIL` to `.env.example` with brief comments, placed near `BUILD_CRON` and `CONTACT_RECIPIENT_EMAIL` respectively.
- [x] 4.2 Add a short section to `BREVO.md` (or `infra/railway/README.md`, matching wherever `CONTACT_RECIPIENT_EMAIL`'s Railway walkthrough already lives) documenting `ADMIN_DIGEST_RECIPIENT_EMAIL` and `ADMIN_DIGEST_CRON`, explicitly calling out that the recipient must be set on **both** the `app` and `worker` Railway services since the worker sends the mail. Added `BREVO.md` §3b (mirroring §3a) and a step 5 + table rows in `infra/railway/README.md` §5.1.

## 5. Final verification

- [x] 5.1 `pnpm run typecheck` and `pnpm --filter @planetos/api test` both pass in full — no regressions to any existing suite. Confirmed: full monorepo typecheck clean; API suite 421/421 (up from 410 pre-change) across 41 files. One `auth.test.ts` Mailpit-search test failed transiently on the first full-suite run (unrelated to this change - passed in isolation and on a clean re-run of the full suite).
- [x] 5.2 Manual verification: with local dev servers running and Postgres seeded so at least one pending registration or pending entry/edit exists, manually invoke `runAdminDigest` (e.g. via a throwaway script or an interactive Node REPL against the dev database) and confirm a matching email arrives in Mailpit with the correct counts and an `/admin` link; then clear all pending items and confirm invoking it again sends nothing. Ran a throwaway `tsx` script (deleted after use) against the real local dev DB, which already had 48 pending user registrations (accumulated test fixtures) and 0 pending edits: confirmed in Mailpit — subject "Pending review digest", to `admin-digest@localhost`, body reporting "Pending user registrations: 48 / Pending edits: 0" and a working `/admin` link. **Adaptation, noted rather than silently skipped**: did not additionally clear all 48+ pending rows to verify the "sends nothing" half live against Mailpit — deleting that much real local dev data non-destructively wasn't in scope for this task. That half of the behavior is instead fully verified by `tests/lib/adminDigest.test.ts`'s "sends no email when there is nothing pending" case, which exists specifically because this shared dev database can't be assumed clean (see that test's own comment).
- [x] 5.3 `openspec validate add-admin-digest-email --type change --strict` passes. Confirmed: "Change 'add-admin-digest-email' is valid".
