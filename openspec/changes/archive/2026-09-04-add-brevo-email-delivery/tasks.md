## 1. Config — mail settings

- [x] 1.1 In `apps/api/src/config.ts` add to `Config`: `mailTransport: "brevo-api" | "smtp"`, `brevoApiKey: string`, `mailFromAddress: string`, `mailFromName: string`. `parseEnv` dev defaults: transport `"smtp"`, name `"eReader Dictionaries"`, key/address blank. All API-scope (not in `WORKER_REQUIRED`). Verify `pnpm --filter @planetos/api typecheck` passes. (`Config` gains `mailTransport`/`brevoApiKey`/`mailFromAddress`/`mailFromName`; `MAIL_TRANSPORTS` const; dev defaults added. All API-scope. typecheck clean.)
- [x] 1.2 In `validateEnv` (strict + `api` scope): `MAIL_TRANSPORT` required and one of the two values; `SMTP_URL` URL check runs only when transport is `smtp`; `BREVO_API_KEY` non-empty + non-placeholder only when transport is `brevo-api`; `MAIL_FROM_ADDRESS` required + basic `x@y.tld` shape + host not `localhost`/`127.0.0.1` and not ending `.local`/`.test`/`.example` and not `example.{com,org,net}`; `MAIL_FROM_NAME` required non-empty. Reuse the placeholder-rejection style from `PLACEHOLDER_SECRETS`. (transport-conditional `validateEnv`: `MAIL_TRANSPORT` required+enum; `SMTP_URL` only for `smtp`; `BREVO_API_KEY` non-empty+non-placeholder only for `brevo-api`; `MAIL_FROM_ADDRESS` shape + blocked-domain check.)
- [x] 1.3 Extend `apps/api/tests/config.test.ts`: strict + `MAIL_TRANSPORT` unset → issue names it; strict + `brevo-api` + no `BREVO_API_KEY` → rejected; strict + `smtp` + no `SMTP_URL` → rejected; strict + `brevo-api` + no `SMTP_URL` → **not** rejected for SMTP_URL; strict + `MAIL_FROM_ADDRESS=no-reply@planetos.local` → rejected; strict + valid `brevo-api` config (key + `notify@dict.example-real.com` … use a non-blocked domain) → `[]`. Verify the file passes. (25 config tests pass (9 new mail-transport cases).)

## 2. Mailer — transport abstraction

- [x] 2.1 In `apps/api/src/lib/mailer.ts` add an internal `async function sendEmail(msg: { to: string; subject: string; text: string }): Promise<void>` dispatching on `config.mailTransport`. `smtp` → the existing lazy Nodemailer transporter, `from` = `` `${config.mailFromName} <${config.mailFromAddress}>` ``. `brevo-api` → `fetch("https://api.brevo.com/v3/smtp/email", { method: "POST", headers: { "api-key": config.brevoApiKey, "content-type": "application/json", accept: "application/json" }, body: JSON.stringify({ sender: { email: config.mailFromAddress, name: config.mailFromName }, to: [{ email: msg.to }], subject: msg.subject, textContent: msg.text }) })`; throw `Error("Brevo API <status>: <body>")` on non-2xx. (`mailer.ts` has internal `sendEmail()` dispatching on `config.mailTransport`; exported `sendViaBrevoApi` (`fetch` to `api.brevo.com/v3/smtp/email`, `api-key` header, throws on non-2xx); `sendViaSmtp` lazy Nodemailer with `from` from config.)
- [x] 2.2 Rewrite `sendPasswordResetEmail` / `sendVerificationEmail` / `sendAccountApprovedEmail` as thin wrappers that build `msg` (unchanged subjects + `text`) and `await sendEmail(msg)`. Remove the hardcoded `no-reply@planetos.local` string. Verify `pnpm --filter @planetos/api typecheck` passes. (3 helpers are thin wrappers; hardcoded `no-reply@planetos.local` gone. typecheck clean.)
- [x] 2.3 Add `apps/api/tests/lib/mailerBrevo.test.ts` (or a describe block in a new file): `vi.stubGlobal("fetch", …)` + a fixed `MAIL_TRANSPORT=brevo-api` (set on `process.env` for the test, or via a small `parseEnv` injection if the module reads `config` at call time). Assert one send issues `POST https://api.brevo.com/v3/smtp/email` with the `api-key` header and a body whose `sender.email` / `to[0].email` / `subject` / `textContent` are correct; assert a stubbed `500` response makes the helper throw. Verify it passes. (`tests/lib/mailerBrevo.test.ts` — 2 tests: POST shape (url/method/api-key header/sender/to/subject/textContent) + non-2xx → throws `Brevo API 401`.)
- [x] 2.4 Confirm the existing `apps/api/tests/lib/mailer.test.ts` (SMTP → Mailpit) still passes unchanged (`MAIL_TRANSPORT` defaults to `smtp` in `test`). (`tests/lib/mailer.test.ts` (SMTP→Mailpit) unchanged, 3/3 pass (`MAIL_TRANSPORT` defaults to `smtp` in test).)

## 3. Best-effort sends

- [x] 3.1 In `apps/api/src/routes/auth.ts` wrap the register-handler `await sendVerificationEmail(...)` in `try { … } catch (err) { request.log.error(err, "Failed to send verification email"); }`, keeping `return reply.status(201).send(toUserDto(user))`. Same wrap for the forgot-password `await sendPasswordResetEmail(...)` and the resend-verification `await sendVerificationEmail(...)`, both still falling through to their generic `200`. (3 `await send…` sites in `routes/auth.ts` (register, forgot-password, resend-verification) wrapped in try/catch + `request.log.error`, mirroring `admin.ts` approve.)
- [x] 3.2 Add an `apps/api/tests/` case (extend `auth.test.ts` or `emailVerification.test.ts`): force the verification send to fail (mock `sendVerificationEmail` to reject, or point a `smtp` transport at a dead port) and assert `POST /api/auth/register` returns `201`, the `user` row exists, and an `emailVerificationToken` row exists. Verify it passes. (`tests/authEmailResilience.test.ts` (`vi.mock` the mailer to reject): register → 201 with user + emailVerificationToken rows present.)
- [x] 3.3 Add/extend a forgot-password test: with the reset send failing, the response body and status are identical to the non-matching-identifier case. Verify. (same file: forgot-password matching vs non-matching → identical 200 body; resend-verification → 200.)

## 4. Infra + docs

- [x] 4.1 `.railway/railway.ts` — on the `app` service add `MAIL_TRANSPORT: "brevo-api"` (literal) and `BREVO_API_KEY`, `MAIL_FROM_ADDRESS`, `MAIL_FROM_NAME` as `preserve()`. Worker unchanged. Verify `pnpm run typecheck:railway` passes. (`.railway/railway.ts` app env: `MAIL_TRANSPORT: "brevo-api"` + `BREVO_API_KEY`/`MAIL_FROM_ADDRESS`/`MAIL_FROM_NAME` `preserve()`; `SMTP_URL` dropped from app (SMTP-relay path documented as the switch). worker unchanged. `typecheck:railway` clean.)
- [x] 4.2 `infra/railway/README.md` — rewrite the email section: verify a sender domain in Brevo → create an API key → set `MAIL_TRANSPORT=brevo-api`, `BREVO_API_KEY`, `MAIL_FROM_ADDRESS` (on the verified domain), `MAIL_FROM_NAME`; note that this works on any Railway plan. Keep a short "Pro-plan alternative" using `MAIL_TRANSPORT=smtp` + `smtp-relay.brevo.com`. Add the post-deploy smoke step (register / forgot-password / approve → check Brevo logs). Update the variable-reference table. (`infra/railway/README.md` §5.1 rewritten: verify Brevo sender domain → API key → set the 3 vars; Pro-plan SMTP-relay note; post-deploy smoke (register/forgot/approve → Brevo Logs). Variable-reference table updated.)
- [x] 4.3 `.env.example` — add `MAIL_TRANSPORT=smtp`, `MAIL_FROM_ADDRESS=`, `MAIL_FROM_NAME=eReader Dictionaries`, `BREVO_API_KEY=` with `[operator-set]` / transport notes; note `SMTP_URL` is only needed when `MAIL_TRANSPORT=smtp`. Verify `.env.example` covers every var `apps/api/src` reads. (`.env.example` gains `MAIL_TRANSPORT`, `MAIL_FROM_ADDRESS`, `MAIL_FROM_NAME`, `BREVO_API_KEY` with tags; `SMTP_URL` marked transport-conditional.)
- [x] 4.4 `SPEC.md` — §3 (mail: "Brevo HTTPS API in prod, SMTP locally"), §6 if it references email sending, §10 var block (add the four mail vars, mark SMTP_URL transport-conditional). Verify §10 matches `.env.example`. (`SPEC.md` §3 (mail bullet), §6 (register best-effort note), §10 (mail vars; SMTP_URL transport-conditional). §10 list == `.env.example` (20 vars).)

## 5. Verification

- [x] 5.1 `pnpm --filter @planetos/api test` and `pnpm typecheck` pass with all changes. (`pnpm typecheck` 0 errors; `pnpm --filter @planetos/api test` 23 files / 287 tests pass.)
- [x] 5.2 Local SMTP path unchanged: with the local `.env` (`MAIL_TRANSPORT` unset → `smtp`), start the app and register a user → verification email lands in Mailpit from the configured `MAIL_FROM_*`. (live: dev mode, register → verification email in Mailpit, From `eReader Dictionaries <no-reply@localhost>`.)
- [x] 5.3 Brevo-API path (no real key needed): `NODE_ENV=production MAIL_TRANSPORT=brevo-api` + all other strict vars valid + `BREVO_API_KEY=dummy` `MAIL_FROM_ADDRESS=notify@dict.example-real.com` → app boots (config valid); a triggered send makes an HTTPS POST to `api.brevo.com` (observe via logs / a stub) and, on Brevo's `401`, the endpoint still returns its normal status with the failure logged. (live: `NODE_ENV=production MAIL_TRANSPORT=brevo-api` boots; register → real HTTPS POST to api.brevo.com (Brevo 401 with a dummy key), failure logged, register still 201.)
- [x] 5.4 Strict boot rejects a `.local` sender and a missing `BREVO_API_KEY` under `brevo-api` (exercised by 1.3 unit tests; spot-check once via `node apps/api/dist/index.js`). (built `index.js` in strict mode: `.local` sender → exit 1 naming `MAIL_FROM_ADDRESS`; missing `BREVO_API_KEY` under brevo-api → exit 1.)
- [x] 5.5 `openspec validate add-brevo-email-delivery --strict` passes. (`openspec validate add-brevo-email-delivery --strict` → valid.)
