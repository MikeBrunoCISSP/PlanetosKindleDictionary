## 1. API: exclude empty dictionaries from the downloads list

- [x] 1.1 In `apps/api/src/routes/downloads.ts`'s `GET /api/downloads` handler, change the `prisma.series.findMany` query to also select each matching series' latest `SUCCESS` build's `entryCount` (e.g. a nested `builds` selection filtered to `status: "SUCCESS"`, ordered by `createdAt` desc, `take: 1`), then filter the results in code to drop any series whose latest successful build has `entryCount === 0` before mapping to the existing `{ slug, title }` DTO. Verify by running `pnpm --filter @planetos/api exec tsc --noEmit`.
- [x] 1.2 Extend `apps/api/tests/downloads.test.ts`'s `GET /api/downloads` describe block: add a series with a `SUCCESS` build whose `entryCount` is `0` and assert its slug is excluded from the response; add a series whose earlier `SUCCESS` build had a non-zero `entryCount` but whose latest `SUCCESS` build has `entryCount` `0`, and assert it is also excluded. Verify by running `pnpm --filter @planetos/api test downloads`.
- [x] 1.3 Run the full API test suite (`pnpm --filter @planetos/api test`) and confirm no other test (e.g. anything relying on `createSuccessBuild`'s default `entryCount: 1`) regresses.
