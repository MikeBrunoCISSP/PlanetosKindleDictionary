## 1. Backend: verification-token model, migration, and backfill

- [x] 1.1 Add an `EmailVerificationToken` model to `apps/api/prisma/schema.prisma` (per design.md decision 1: `id`, `userId` + `user` relation with `onDelete: Cascade` from the start, `tokenHash` unique, `expiresAt`, `usedAt` nullable, `createdAt`, `@@index([userId])`). Generate the migration and hand-add the backfill statement `UPDATE "User" SET "emailVerified" = true WHERE "emailVerified" = false;` to the same migration file (per design.md decision 2; fall back to the established shadow-DB-diff workaround if blocked by the known checksum-mismatch issue). Verify `pnpm --filter api prisma migrate status` shows up to date, verify via `psql` that a pre-migration seeded account now has `emailVerified = true`, and confirm `pnpm --filter api test` (existing suite) still passes unaffected. Included `onDelete: Cascade` from the start (per design.md decision 1), avoiding the follow-up-migration issue hit last time. Applied via the established shadow-DB-diff workaround (same known checksum-mismatch issue as prior changes). Verified via `psql`: `admin@example.com`'s `emailVerified` was `false` before the migration and `true` immediately after, confirming the backfill statement works. `migrate status` shows up to date; full suite 241/241 unaffected (register/login tests not yet updated for the new behavior — that's tasks 5.1/6.1). Also hit and resolved the recurring EPERM Prisma-DLL-lock issue (killed leftover `tsx watch` processes, regenerated the client, restarted one clean `pnpm dev` stack).
- [x] 1.2 In `apps/api/prisma/seed.ts`, add `emailVerified: true` to both the `create` and `update` blocks of the admin upsert. Verify by re-running `pnpm --filter api seed` against the dev database and confirming the admin account's `emailVerified` is `true`. Confirmed via `psql`.

## 2. Backend: mailer additions

- [x] 2.1 In `apps/api/src/lib/mailer.ts`, add `sendVerificationEmail(to: string, verifyUrl: string): Promise<void>` (plain-text email with the verification link) and `sendAccountApprovedEmail(to: string): Promise<void>` (plain-text email informing the recipient their account was approved). Add tests in `apps/api/tests/lib/mailer.test.ts` for both, following the existing real-Mailpit pattern (send, then fetch and assert on the real received message). Verify with `pnpm --filter api test`. 3/3 mailer tests passing.

## 3. Shared: resend-verification schema

- [x] 3.1 Add `resendVerificationSchema` (`{ identifier: string, min 1 }`, matching `forgotPasswordSchema`'s shape) to `packages/shared/src/auth.ts`, exporting a `ResendVerificationDto` type. Add unit tests in `packages/shared/src/__tests__/auth.test.ts`. Verify with `pnpm --filter shared test`. 3 new tests; full shared suite 60/60.

## 4. Backend: errors and rate limits

- [x] 4.1 Add `EMAIL_NOT_VERIFIED: () => new DomainError("EMAIL_NOT_VERIFIED", "Please verify your email address before signing in.", 403)` and `INVALID_VERIFICATION_TOKEN: () => new DomainError("INVALID_VERIFICATION_TOKEN", "This verification link is invalid or has expired.", 400)` to `apps/api/src/lib/errors.ts`. Add `VERIFY_EMAIL_RATE_LIMIT = { max: 10, timeWindow: "15 minutes" }` and `RESEND_VERIFICATION_RATE_LIMIT = { max: 5, timeWindow: "1 hour" }` to `apps/api/src/plugins/rateLimit.ts`. Done.

## 5. Backend: registration no longer opens a session

- [x] 5.1 In `apps/api/src/routes/auth.ts`'s `POST /api/auth/register` handler: remove the `request.session.userId = user.id` / `request.session.save()` calls; after creating the user, generate a random token, hash it, create the `EmailVerificationToken` row (`expiresAt` = now + 24h), and send the verification email via `sendVerificationEmail` with a URL built from `PUBLIC_BASE_URL` + `/verify-email?token=...`. Response stays `201` with the same `UserDto` body. Update the existing registration tests in `apps/api/tests/auth.test.ts` that currently assert `res.headers["set-cookie"]` is defined on success — assert it is now `undefined` instead. Add a test confirming a real verification email is received via Mailpit after registration. Verify with `pnpm --filter api test`. Also had to fix downstream test setup that relied on registration returning a session cookie: `POST /api/auth/login`'s `beforeEach`, and the `GET /api/auth/me`/`POST /api/auth/logout` tests, now explicitly mark the test account verified via Prisma and log in for a real cookie, rather than relying on register's (now removed) cookie. 22/22 auth tests passing.

## 6. Backend: login requires a verified email

- [x] 6.1 In the `POST /api/auth/login` handler, add `if (!user.emailVerified) throw Errors.EMAIL_NOT_VERIFIED();` immediately after the existing `isActive` check (per design.md decision 3 — same placement, before password verification). Add tests: an unverified account with correct credentials → `403` `EMAIL_NOT_VERIFIED`, no session cookie; a pre-existing (pre-migration-backfilled, i.e. seeded before this feature) account still logs in successfully without ever verifying. Verify with `pnpm --filter api test`. The "pre-existing account" test simulates a backfilled row directly via Prisma (`emailVerified: true`, no `EmailVerificationToken` ever created), since the real migration already ran once against real data. **Found a much larger ripple than anticipated**: nearly every other test file (`admin.test.ts`, `adminRegistrations.test.ts`, `downloads.test.ts`, `entries.test.ts`, `entryEditProposals.test.ts`, `turnstile.test.ts`, `series.test.ts`) has its own local `registerAndGetCookie` helper that relied on registration's own `set-cookie` header to authenticate test setup - 113 tests across 7 files broke. Fixed each helper identically: register, mark the account verified directly via Prisma, then perform a real `/api/auth/login` for the cookie. Also fixed `passwordReset.test.ts`'s `findResetEmail` helper, which picked the newest Mailpit message for a recipient - now that registration also sends a verification email to the same address, it needed to filter by subject to still find the actual reset email. Full API suite 246/246 passing.

## 7. Backend: verify-email endpoint

- [x] 7.1 Implement `POST /api/auth/verify-email` in `auth.ts`, rate-limited with `VERIFY_EMAIL_RATE_LIMIT`: parse `{ token: string }`, hash it, look up an unused `EmailVerificationToken` by `tokenHash` with `expiresAt > now`; if none found throw `Errors.INVALID_VERIFICATION_TOKEN()`; otherwise set `user.emailVerified = true` and mark the token `usedAt = now` in one transaction, return `200` (no session established). Add tests: valid token verifies the account and a subsequent login succeeds; reused token → `400` `INVALID_VERIFICATION_TOKEN`; expired token (seed one with a past `expiresAt`) → same error; unknown/garbage token → same error. Verify with `pnpm --filter api test`. Implemented in new `apps/api/tests/emailVerification.test.ts`, same unique-per-test-email pattern as `passwordReset.test.ts`. Used a local `verifyEmailSchema` (zod) for the request body rather than a manual type-cast, matching the codebase's consistent `Schema.parse()` convention. 4/4 verify-email tests passing.

## 8. Backend: resend-verification endpoint

- [x] 8.1 Implement `POST /api/auth/resend-verification` in `auth.ts`, rate-limited with `RESEND_VERIFICATION_RATE_LIMIT`: parse `resendVerificationSchema`, look up the user by normalized username or email; only if found, `isActive`, and `!emailVerified`, invalidate that account's prior unused `EmailVerificationToken` rows, issue a new one, and send the verification email. Always respond `200` with the same generic message regardless of match. Add tests: matching unverified active account → real email received via Mailpit, generic response; already-verified account → generic response, no email sent; unknown identifier → generic response, no email sent; disabled account → generic response, no email sent; a token issued before a resend is no longer redeemable after the resend issues a new one. Verify with `pnpm --filter api test`. 5/5 resend-verification tests passing; full API suite 255/255 (246 + 9 new).

## 9. Backend: approval notification email

- [x] 9.1 In `apps/api/src/routes/admin.ts`'s `POST /api/admin/users/:id/approve` handler, after the `approvalStatus` update succeeds, call `sendAccountApprovedEmail(user's email)` wrapped in try/catch that logs on failure without rethrowing (per design.md decision 6). Add tests: approving a pending user sends a real email (verified via Mailpit) and the approval still returns its existing success response; simulate a send failure (e.g. inject a storage/mailer double that throws, matching the existing `BuildStorage`-style injected-failure test pattern from the Kindle automation change) and confirm the approval still succeeds with `approvalStatus: "APPROVED"` in the response despite the failure. Verify with `pnpm --filter api test`. Used `vi.spyOn(mailer, "sendAccountApprovedEmail").mockRejectedValueOnce(...)` for the failure-simulation test only (restored immediately after) rather than a file-wide `vi.mock`, so every other test in the file still exercises the real mailer/Mailpit path — matches this project's established precedent of mocking only to exercise an otherwise-untriggerable failure branch (the Kindle automation change's injected `BuildStorage` failure test), not as a general substitute for real services. 16/16 tests passing (14 original + 2 new).

## 10. Frontend: registration success screen

- [x] 10.1 In `apps/web/src/routes/login.tsx`'s `RegisterForm`, change the submit handler to stop calling `queryClient.setQueryData(ME_QUERY_KEY, user)` and stop navigating to `/` on success; instead render an inline "Check your email to verify your account" success card (mirroring `ForgotPasswordForm`'s existing success-card pattern) with a "Resend verification email" action that calls `apiResendVerification({ identifier: <the email just submitted> })`. Add `apiResendVerification` to `apps/web/src/lib/api.ts`. Verify by registering a new account and confirming: no redirect to `/`, the header still shows "Log In" (no session), the success card appears, and clicking resend triggers a second real email (visible in Mailpit). Verified live via Playwright: registering `e2e-verify-test@example.com` showed the "Check your email" card in place of the form, header stayed on "Log In" (no session), and a real verification email arrived in Mailpit with the expected subject/sender.

## 11. Frontend: login surfaces the resend action on an unverified-email error

- [x] 11.1 In `SignInForm`, detect the `EMAIL_NOT_VERIFIED` error type on a failed login and show a distinct inline message with a "Resend verification email" action using the identifier already typed into the form (reusing `apiResendVerification`). Verify by attempting to log into an unverified account and confirming the distinct message and working resend action appear, while a plain wrong-password attempt still shows the existing generic message unchanged.
  - Implemented (`onSubmit`'s catch branches on `err.type === "urn:planetos:error:email-not-verified"`) and verified live via Playwright during the task 14.2 walkthrough: logging into `walkthrough-verify@example.com` before verifying showed "Please verify your email address before signing in." with a working "Resend verification email" button that swapped to a confirmation message and triggered a real second email. Separately confirmed via curl that a wrong-password attempt against a verified account (`admin@example.com`) still returns the plain unchanged `invalid-credentials` error.

## 12. Frontend: verify-email page

- [x] 12.1 Create `apps/web/src/routes/verify-email.tsx`: reads `token` from the URL search params, calls `apiVerifyEmail({ token })` on mount, and shows a success state (with a link to `/login`) or a clear "invalid or expired" state (with a link to resend, reusing the same resend action) depending on the result. Add `apiVerifyEmail` to `api.ts`. Verify `pnpm --filter web exec vite build` regenerates `routeTree.gen.ts` with the new `/verify-email` route present. Confirmed via grep. The invalid/expired state links to Sign In rather than a standalone resend page (no identifier is recoverable from a dead token) — signing in with an unverified account is exactly what surfaces the resend action from task 11.1, per design.md decision 5's deliberate choice not to add a separate resend mode/page.
- [x] 12.2 Run `pnpm --filter web lint` and `pnpm --filter web exec tsc --noEmit`; confirm both pass clean. Both clean.

## 13. Documentation

- [x] 13.1 Update `SPEC.md` §6 (HTTP API) — confirm `POST /api/auth/verify-email` now matches what was actually built, and add `POST /api/auth/resend-verification` alongside it.
  - Reworded the register/login entries to state the new behavior (register no longer opens a session and sends a verification email; login now rejects unverified accounts with 403 EMAIL_NOT_VERIFIED, with a note on the migration-backfill grandfathering), expanded the verify-email entry, added the resend-verification entry, and noted the approval-email side effect on `POST /api/admin/users/:id/approve`.
- [x] 13.2 Update `SPEC.md` §8 (Frontend > Routes) to add `/verify-email`.
  - Added the `/verify-email` route entry and updated the `/login` entry to describe the new EMAIL_NOT_VERIFIED error state + resend action and the Register success card replacing auto-sign-in/navigation.

## 14. End-to-end verification

- [x] 14.1 Run `pnpm --filter api test`, `pnpm --filter shared test`, `pnpm --recursive typecheck`, `pnpm --filter api lint`, `pnpm --filter web lint`, and `pnpm --filter web build`; confirm all succeed.
  - All green: shared 60/60, api 257/257, typecheck clean across all 4 workspace packages, both lints clean, web build succeeds (pre-existing >500kB main-chunk warning is unrelated to this change).
- [x] 14.2 Manually verify against the real running dev stack: register a new throwaway account and confirm no session is opened and a real verification email arrives via Mailpit; attempt to log in before verifying and confirm the distinct "please verify" error with a working resend action; click the (possibly resent) verification link and confirm the account becomes verified and can then log in normally; confirm the pre-existing seeded admin account logs in without ever needing to verify; as admin, approve a different pending throwaway registration and confirm that user receives a real approval-notification email via Mailpit. Clean up all throwaway accounts created during this walkthrough afterward.
  - Full walkthrough done live against the real dev stack (Postgres/Redis/Mailpit) via Playwright + curl. Registering `walkthrough-verify@example.com` showed the "Check your email" card with no session opened (header still showed "Log In") and a real verification email arrived in Mailpit. Logging in before verifying showed the distinct "Please verify your email address before signing in." error with a working "Resend verification email" button; clicking it swapped in the confirmation text and a second real email arrived.
  - **Bug found and fixed during this pass**: clicking the resent verification link left the page stuck on "Verifying your email…" forever, even though the server-side call succeeded (confirmed via `psql`: `emailVerified` was already `true`). Root cause: the StrictMode `requestedTokenRef` guard (added earlier this change) correctly prevents the second dev-mode effect invocation from re-fetching, but the surviving fetch was the *first* invocation's, and StrictMode's simulated unmount still ran that invocation's own cleanup, setting its local `cancelled` flag to `true` - so when that fetch resolved, `if (!cancelled) setStatus(...)` silently swallowed the update. Fixed in `apps/web/src/routes/verify-email.tsx` by removing the `cancelled`/cleanup pattern entirely (the ref guard alone is sufficient to prevent a double-fetch; React 18 safely no-ops a `setState` after real unmount). Re-verified `tsc --noEmit` and `eslint` clean, then re-ran the full flow end-to-end with a fresh account (`walkthrough-verify2@example.com`): "Email Verified" now renders correctly and login succeeds afterward.
  - Confirmed the pre-existing seeded `admin@example.com` account logs in successfully without ever verifying (backfilled `emailVerified=true` by the migration).
  - As admin, approved `walkthrough-verify2@example.com`'s pending registration; confirmed a real "Your eReader Dictionaries account has been approved" email arrived in Mailpit.
  - Cleaned up both throwaway accounts (`walkthrough-verify@example.com`, `walkthrough-verify2@example.com`) via `psql` afterward.
