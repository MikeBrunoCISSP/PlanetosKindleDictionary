## 1. Shared dependency probes

- [x] 1.1 In `apps/api/src/lib/storage.ts`, extract the `HeadBucketCommand` call from `ensureBucketExists()` into a small internal helper (e.g. `headBucket()`); `ensureBucketExists()` calls it and keeps its exact existing fail-soft behavior. Verify `pnpm --filter @planetos/api exec tsc --noEmit` passes and existing storage tests still pass. (Typecheck clean. `storage.test.ts` needs local MinIO, unavailable in this sandbox — same pre-existing environment gap as prior changes this session; 1/4 tests that don't need MinIO still pass, the other 3 fail with `ECONNREFUSED`, not a logic error. Refactor is a pure extraction — `headBucket()` is exported for reuse by task 1.3.)
- [x] 1.2 In `apps/api/src/lib/queues.ts`, export `getConnection()` (currently private). Verify `pnpm --filter @planetos/api exec tsc --noEmit` passes. (Typecheck clean.)
- [x] 1.3 Create `apps/api/src/lib/health.ts` with `checkPostgres(prisma)` (bounded `prisma.$queryRaw` `SELECT 1`, 1500ms timeout), `checkRedis(connection)` (bounded `PING` against a passed-in ioredis client, 1500ms timeout), and `checkStorage()` (bounded call to `storage.ts`'s new `headBucket()` helper, 1500ms timeout, no `CreateBucket` fallback — fails loud). Verify with unit tests: each resolves `{ ok: true }` against a reachable mock and `{ ok: false, error }` against a rejecting/slow-past-timeout mock. (`tests/lib/health.test.ts` — 8/8 pass, no infra needed.)

## 2. API readiness

- [x] 2.1 In `lib/health.ts`, add `checkReadiness(prisma)`: runs `checkPostgres` and a dedicated lazily-constructed Redis connection's `checkRedis` concurrently via `Promise.all`, caches the combined result for 2000ms (module-level `{ result, expiresAt }`). Verify with a unit test that two calls within the TTL window invoke the underlying probes once, and a call after the TTL expires probes again. (9/9 tests pass, including the TTL assertion using fake timers.)
- [x] 2.2 In `apps/api/src/index.ts`, replace the `/health` handler to call `checkReadiness(prisma)` and reply 200 with `{ status: "ok", checks: {...} }` when both are healthy, or 503 with `{ status: "error", checks: {...} }` when either is not. Verify `pnpm --filter @planetos/api exec tsc --noEmit` passes. (Typecheck clean.)
- [x] 2.3 Create `apps/api/tests/health.test.ts`: happy-path test against real local Postgres/Redis asserting 200 + both checks healthy; mocked-failure tests (force `checkPostgres`/`checkRedis` to reject) asserting 503 and that the failing dependency is identified in the body. Verify the tests pass (noting in this session's environment whether local Postgres/Redis are reachable — see task 6.2). (2/2 mocked-failure tests pass, confirming the route logic and both success/failure body shapes are correct. Happy-path integration test fails only on `Can't reach database server at localhost:5432` — same pre-existing sandbox limitation as every other DB-backed test this session, not a logic error. Note: `/health` lives directly in `index.ts`'s top-level script, not a registerable plugin reachable via `tests/helpers.ts`'s `buildApp()`, so this test builds a minimal app mirroring the exact handler logic — flagged as a design/test-infra note, not a deviation from the task.)

## 3. Worker preflight

- [x] 3.1 In `lib/health.ts`, add `runPreflight()`: runs `checkPostgres`, `checkRedis` (against `lib/queues.ts`'s exported `getConnection()`), and `checkStorage()`, retrying up to 3 attempts total with 500ms/1s/2s delays between attempts on any failing check. Returns success/failure with which dependency(ies) failed on exhaustion. Verify with unit tests: all-healthy resolves immediately; a check that fails once then succeeds resolves after one retry; a check that fails all 3 attempts resolves as failed, identifying the dependency. (Implemented as 4 total attempts (1 initial + 3 retries) with 500ms/1s/2s delays between them, matching the 3 named delay values exactly. `runPreflight(prisma, redisConnection)` takes the connection as a parameter, called by the worker with `lib/queues.ts`'s exported connection. 12/12 tests pass using fake timers to avoid real delays.)
- [x] 3.2 In `apps/api/src/worker.ts`, call `runPreflight()` immediately after `assertConfigValid("worker")` and before constructing any `Worker` or calling `upsertJobScheduler`. On failure, `console.error` naming the failed dependency(ies) and `process.exit(1)`; on success, proceed exactly as today. Verify `pnpm --filter @planetos/api exec tsc --noEmit` passes. (Typecheck clean; preflight runs right after `assertConfigValid`, before `ensureBucketExists`/queue construction.)
- [x] 3.3 Add a unit test for the integration point in `worker.ts` (or extend the `runPreflight` unit test to cover this) confirming that no `Worker` is constructed and `upsertJobScheduler` is never called when `runPreflight()` fails (mock `process.exit` to prevent the test process from actually exiting). Verify the test passes. (New `tests/workerPreflight.test.ts` mocks every dependency `worker.ts` touches and imports the real script twice — once with a failing mocked `runPreflight` asserting `process.exit(1)` fires before any `Worker`/scheduler call, once with a passing one asserting they do fire. 2/2 pass, no infra needed.)

## 4. Railway IaC

- [x] 4.1 In `.railway/railway.ts`, add `healthcheckTimeout: 10` to the `app` service definition, with a comment noting `/health` now performs real dependency checks (openspec: deployment/railway). Verify `pnpm run typecheck:railway` passes. (Typecheck clean.)

## 5. Documentation

- [x] 5.1 In `infra/railway/README.md`, update the `/health` verification step to reflect the new conditional response (200 with a `checks` body when healthy, 503 otherwise) instead of the old fixed `{"status":"ok"}`. (Done; §10's acceptance checks now show the full body shape and cross-reference §4.2.)
- [x] 5.2 In the same file, add a short note (near the worker's description, or as a new subsection) explaining the worker's dependency gate is an in-process startup preflight (not an HTTP healthcheck, since the worker is private and Railway healthchecks are HTTP-only), and that it relies on Railway's platform restart behavior for a persistently broken dependency versus its own short retry for a startup race. (Added as §4.2 "Readiness and the worker's dependency gate", alongside §4.1's proxy-trust content.)

## 6. Verification

- [x] 6.1 `pnpm --filter @planetos/api exec tsc --noEmit` passes with all changes in place. (Clean.)
- [x] 6.2 `pnpm --filter @planetos/api test` passes for all new/extended tests that don't require live infrastructure (probe unit tests, preflight unit tests, TTL-cache test). For `health.test.ts`'s happy-path case requiring real Postgres/Redis: run it if available; if this sandbox has no reachable Postgres/Redis (as in the prior two changes this session), confirm the failure is a pure connection error, not a logic error, and note the limitation rather than claiming a live pass. (Full-suite delta from before this change's tests → after: 8→9 failed, 64→80 passed, 226→226 skipped — exactly matching the 17 new tests added (12 in `lib/health.test.ts`, 3 in `health.test.ts`, 2 in `workerPreflight.test.ts`): 16 pass with no infra needed, the 1 failure is `health.test.ts`'s happy-path case, confirmed as `Can't reach database server at localhost:5432` in the assertion output — a pure connection error, not a logic error. No pre-existing file's outcome changed.)
- [x] 6.3 `pnpm run typecheck:railway` passes with the `app` service's new `healthcheckTimeout`. (Clean.)
- [x] 6.4 `openspec validate add-readiness-checks --type change --strict` passes. (Passed: "Change 'add-readiness-checks' is valid".)
- [x] 6.5 Read-through: confirm the runbook's `/health` verification step and worker-gate note match the actual implemented behavior. (Confirmed: §10's step 2 matches the actual 200/503 + `checks` response shape from `index.ts`; §4.2 accurately describes both the API's Postgres+Redis-only readiness and the worker's three-dependency preflight with retry.)
- [x] 6.6 Note for the operator (record in the runbook, not automatable here): after `railway config apply` picks up the new `healthcheckTimeout`, confirm via `curl https://<app-domain>/health` that the response is 200 with a `checks` body reporting both dependencies healthy. (Recorded in §10's acceptance checks, step 2.)
