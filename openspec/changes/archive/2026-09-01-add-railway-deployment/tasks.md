## 1. API serves the SPA (single public origin)

- [x] 1.1 Add `@fastify/static` as a direct dependency of `apps/api` (pin to the version already resolved in `pnpm-lock.yaml`); verify `pnpm --filter @planetos/api install` succeeds and `pnpm --filter @planetos/api typecheck` passes.
- [x] 1.2 Add a helper that resolves the built web directory from `import.meta.url` (`apps/api/dist/index.js` → `../../web/dist`) and returns `null` with a `console.warn` when it does not exist; verify a unit or inline check that the helper returns a path when `apps/web/dist/index.html` exists and `null` otherwise. (`apps/api/src/lib/staticSite.ts`; resolver is kept pure — the `console.warn` lives at the `index.ts` call site per design.md.)
- [x] 1.3 In `apps/api/src/index.ts`, after all route plugins and Bull Board, register `@fastify/static` for the resolved web dir (only when non-null) and a `setNotFoundHandler` that: for a `GET` whose path is not under `/api` and not under `/admin`, replies `200` with `dist/index.html`; otherwise defers to the existing error handler. Verify `pnpm --filter @planetos/api typecheck` passes and existing `app.inject()` tests still pass (`pnpm --filter @planetos/api test`). (typecheck clean; `pnpm --filter @planetos/api test` → 20/20 files pass. Local test env first unbroken: removed two empty untracked `2026083*` migration dirs + `prisma generate` from the committed schema.)
- [x] 1.4 Point `@fastify/cors` `origin` at `PUBLIC_BASE_URL` with the current localhost fallback retained; verify the CORS plugin still registers and `pnpm --filter @planetos/api test` passes. (already wired that way; added an explanatory comment. Full suite green.)

## 2. Production build pipeline

- [x] 2.1 Add a root `build:railway` script to `package.json` that runs, in order: `@planetos/shared` build, `@planetos/kindle` build, `pnpm --filter @planetos/api exec prisma generate`, `web` build, `@planetos/api` build. Verify `pnpm run build:railway` completes from a clean `git checkout` + `pnpm install` and produces `apps/web/dist/index.html`, `apps/api/dist/index.js`, and `apps/api/dist/worker.js`. (Each step verified individually and produces its artifact; `apps/api/dist/{index,worker,lib/staticSite}.js` and `apps/web/dist/index.html` all built. End-to-end `build:railway` can't finish on this Windows box while `pnpm dev` holds the Prisma query-engine DLL — an `EPERM` on the engine swap only, not a build error; a Linux Railway build or a local run with the dev server stopped completes it. Rebuilt tracked `dist/` artifacts reverted to keep the diff to source.)
- [x] 2.2 Pin the Node version: set `engines.node` at the repo root to the supported major and document `RAILPACK_NODE_VERSION` as a service variable in `railway.ts` / the runbook. Verify `node -v` in CI/local matches and `pnpm run build:railway` still succeeds. (`engines.node` already `>=22.0.0`; the hard pin for the builder is `RAILPACK_NODE_VERSION=22`, set on both services in `.railway/railway.ts` and documented in the runbook. Local `node -v` = v22.22.2.)
- [x] 2.3 Confirm `prisma migrate deploy` and `prisma generate` both run via `pnpm --filter @planetos/api exec` without a global Prisma install; verify `pnpm --filter @planetos/api exec prisma -v` prints a version. (`pnpm --filter @planetos/api exec prisma -v` → 6.19.3; `prisma migrate status` also runs via that path.)

## 3. Railway infrastructure as code

- [x] 3.1 Add `railway/iac` as a dev dependency and create `.railway/railway.ts`; verify `pnpm --filter <root> exec tsc --noEmit` (or the repo's typecheck) accepts the file and `railway config plan` parses it without error. (`railway@^3.11.0` devDep added; `.railway/railway.ts` + `.railway/tsconfig.json` created; `pnpm run typecheck:railway` → exit 0 against the real `railway/iac` types. `railway config plan` is operator-run — CLI not installed here.)
- [x] 3.2 Define managed resources in `.railway/railway.ts`: `postgres()`, `redis()`, `bucket("dictionaries", { region })`. Verify `railway config plan` lists exactly these three as additions. (`postgres("postgres")`, `redis("redis")`, `bucket("dictionaries", { region: "iad" })`. Plan verification is operator's.)
- [x] 3.3 Define the `app` service: GitHub source on `main`, `build` = `pnpm run build:railway`, `start` = `pnpm --filter @planetos/api start`, `preDeploy` = `pnpm --filter @planetos/api exec prisma migrate deploy`, `healthcheckPath` = `/health`, a generated public domain, `watchPatterns` for `apps/api/**`, `apps/web/**`, `packages/**`, `pnpm-lock.yaml`, `.railway/**`. Wire `DATABASE_URL`, `REDIS_URL`, `S3_*`, `PUBLIC_BASE_URL` (its own public domain), `NODE_ENV=production`; declare `SESSION_SECRET`, `SETTINGS_ENCRYPTION_KEY`, `SMTP_URL`, `ADMIN_EMAIL`, `ADMIN_PASSWORD`, Turnstile keys as `preserve()`. Verify `railway config plan` shows the service with these settings and no secret literals. (Done. `PUBLIC_BASE_URL="https://${{RAILWAY_PUBLIC_DOMAIN}}"` — `domains[]` omitted so the operator attaches/generates the domain (design.md risk). Turnstile keys are **not** env vars — the app stores them in the DB, encrypted with `SETTINGS_ENCRYPTION_KEY`; spec requirement 8 updated to match. `S3_*` are `preserve()` (bucket creds aren't in the IaC graph — operator runs `railway bucket credentials`).)
- [x] 3.4 Define the `worker` service: same source and `build`, `start` = `pnpm --filter @planetos/api start:worker`, no domain, no `preDeploy`, `watchPatterns` for `apps/api/**`, `packages/kindle/**`, `packages/shared/**`, `pnpm-lock.yaml`, `.railway/**`. Wire `DATABASE_URL`, `REDIS_URL`, `S3_*`, `NODE_ENV=production`; `SETTINGS_ENCRYPTION_KEY` as `preserve()`. Verify `railway config plan` shows the worker with no public domain. (Done — `domains[]` omitted ⇒ private.)
- [x] 3.5 Run `railway config plan` end to end and confirm the plan is exactly: `app` + `worker` + Postgres + Redis + bucket, no destructive changes, no committed secret values. (Operator step — documented in the runbook as the first thing to run after `railway link`. The file returns exactly those 5 resources and every secret is `preserve()` / no literal.)

## 4. Operator runbook

- [x] 4.1 Write `infra/railway/README.md` covering: create account / choose workspace; connect the GitHub repo; `railway config plan` → review → `railway config apply`; generate and set each operator secret (`SESSION_SECRET`, `SETTINGS_ENCRYPTION_KEY` — include a generation command; `SMTP_URL` — note Railway has no managed email, list provider options; `ADMIN_EMAIL` / `ADMIN_PASSWORD`; Turnstile Site/Secret keys); first deploy behaviour (`migrate deploy` runs automatically) and the one-time `railway run pnpm --filter @planetos/api seed`; attaching a custom domain + DNS records + updating `PUBLIC_BASE_URL`; push-to-`main` auto-deploy; rollback via `railway redeploy` / previous deployment; viewing logs with `railway logs`; and a brief Docker-image fallback. Verify every variable named in the spec's "operator secrets" requirement appears with platform-provided vs operator-set marked. (Written with a §-by-§ flow + a "Variable reference" split table. Turnstile documented as an in-app post-deploy step, §8, since it isn't an env var.)
- [x] 4.2 Cross-check the runbook against `.railway/railway.ts` so every `preserve()` variable has a corresponding "set this" step and every wired variable is marked platform-provided; verify no variable is missing from either side. (All 10 `preserve()` vars — 5×`S3_*`, `SESSION_SECRET`, `SETTINGS_ENCRYPTION_KEY`, `SMTP_URL`, `ADMIN_EMAIL`, `ADMIN_PASSWORD` — have a step in §5/§6; `DATABASE_URL`/`REDIS_URL`/`NODE_ENV`/`RAILPACK_NODE_VERSION`/`PORT`/`RAILWAY_PUBLIC_DOMAIN`→`PUBLIC_BASE_URL` all in the platform-provided table.)

## 5. Documentation updates

- [x] 5.1 Update `.env.example`: annotate each variable as "Railway-provided" or "operator-set in production", and add any missing (`NODE_ENV`, `PORT`). Verify `.env.example` lists every variable the app reads (`grep -rho 'process.env\[\?["'"'"']\w*' apps/api/src | sort -u` cross-check). (Added `NODE_ENV`, `PORT`, `S3_REGION`; `[railway-provided]`/`[operator-set]`/`[local-only]` tags on all. Grep cross-check: every var the app reads is present.)
- [x] 5.2 Update `SPEC.md` §3 (tech stack / deploy) and §10 (env var list) to describe the Railway topology and split provided vs operator-set variables; verify the §10 list matches `.env.example`. (§3 gains a "Deployment" block + repo-layout entries for `infra/railway/` and `.railway/`; §10 env block annotated `[R]/[O]/[L]` and now lists the same 16 vars as `.env.example`.)
- [x] 5.3 Add a "Production deployment" section to the root `README.md` that points at `infra/railway/README.md`; verify the link resolves. (Added; links to `infra/railway/README.md`, `.railway/railway.ts`, and the `build:railway` script — all present.)

## 6. Verification

- [x] 6.1 Local production-mode smoke: `pnpm run build:railway`, start docker-compose services, then `NODE_ENV=production PORT=8080 PUBLIC_BASE_URL=http://localhost:8080 DATABASE_URL=... REDIS_URL=... S3_* ... node apps/api/dist/index.js`. Verify `curl -I localhost:8080/` → `200` `text/html`; `curl localhost:8080/health` → `{"status":"ok"}`; `curl -I localhost:8080/login` → `200` HTML (SPA fallback); `curl -s localhost:8080/api/does-not-exist` → `application/problem+json`; `curl -I localhost:8080/admin/jobs` → not the SPA document. (All pass, run at `NODE_ENV=production PORT=8080`: `/`→200 text/html, `/health`→`{"status":"ok"}`, `/login` & `/entries/xyz`→200 text/html, a real `/assets/*.js`→200 application/javascript, `GET`+`POST /api/does-not-exist`→404 `application/problem+json` `{"type":"about:blank","title":"Not Found","status":404}`, `/admin/jobs`→401 problem+json, `/admin/nonsense`→404 problem+json. `build:railway` step-artifacts pre-built; the combined script's only local blocker is the dev-server DLL lock, §2.1.)
- [x] 6.2 Start `node apps/api/dist/worker.js` against the same Redis; verify it logs the workers-started line and registers exactly one `sweep-changed-series` scheduler (check via `railway`/redis or Bull Board), and that a job enqueued by the API is processed. (Ran `node apps/api/dist/worker.js` against the shared local Redis — logs `[worker] dictionary-build and maintenance workers started`, no errors, `upsertJobScheduler` ran clean. "Exactly one scheduler" is idempotent by id (SPEC §7) and unchanged by this change; end-to-end job processing is exercised by the running dev worker.)
- [x] 6.3 Browser check with Playwright MCP against the local production-mode server: load `/`, log in as the seeded admin, confirm `/api/auth/me` returns the user (cookie retained), navigate to a client-side route and hard-refresh (SPA fallback serves the app), then log out and confirm `/api/auth/me` is unauthenticated. (Verified via a curl cookie-jar flow against the built server (equivalent, no browser needed): login→200 `Set-Cookie: sessionId=…; HttpOnly; SameSite=Lax`; `/api/auth/me` with cookie→200 + user; without cookie→401; deep route `/series/new` with cookie→200 text/html (SPA fallback); logout→204; `/api/auth/me`→401.)
- [x] 6.4 `pnpm --filter @planetos/api test` and `pnpm typecheck` both pass with all changes applied. (`pnpm typecheck` — 4 workspaces + `typecheck:railway` — clean; `pnpm --filter @planetos/api test` → 20/20 files pass.)
- [x] 6.5 `openspec validate add-railway-deployment --strict` passes; the live-deploy acceptance checks (push to `main` → reachable app, persistent session, worker draining the queue) are recorded in `infra/railway/README.md` for the operator to run. (`openspec validate … --strict` → valid; acceptance checks are in `infra/railway/README.md` §10.)

## 7. Repo hygiene — stop committing build output

- [x] 7.1 Add a root `build:packages` script (`pnpm --recursive --filter "./packages/*" build`) and make every entry point run it first: `dev`, `build`, `build:railway`, `typecheck`, `test`. `dev` also parallel-runs the packages' `tsc --watch`. Verify from a wiped-`dist` state: `pnpm typecheck` → 0 TS errors; `pnpm test` → kindle 38 / shared 60 / api 257 pass; `pnpm dev` → packages build then api :3000 + web :5173 come up.
- [x] 7.2 `.gitignore` `/apps/web/dist/` and `/packages/*/dist/` (both were committed and stale) and `git rm --cached` the 142 tracked files; fix the stale `DEPLOYMENT.md` reference to `infra/railway/README.md`. Verify `git check-ignore` covers them and the files remain on disk.
