## 1. Schema

- [x] 1.1 Add `EmailOutboxType`, `EmailOutboxStatus` enums and the `EmailOutbox` model to `apps/api/prisma/schema.prisma` per design.md Decision 1. Generate and apply the migration (`prisma migrate dev` locally); verify `prisma generate` succeeds and `pnpm --filter @planetos/api exec tsc --noEmit` passes. (Blocked initially by pre-existing local-dev DB drift, unrelated to this change: an orphaned `OutboxEmail` table/enum with no corresponding migration file anywhere in git history and no code references — an abandoned, differently-designed attempt at this same feature from outside this session — plus a stale never-finished duplicate tracking row for an older migration. Confirmed 0 rows in the orphaned table and no code dependency before touching anything; surgically dropped just that table/enum and both phantom `_prisma_migrations` rows, per the user's explicit choice over a full `prisma migrate reset`, preserving all other local dev data. Migration then applied cleanly: `20260907011959_add_email_outbox`, purely additive (2 new enums, 1 new table, no existing table touched). Typecheck clean.)

## 2. Mailer timeout

- [x] 2.1 In `apps/api/src/lib/mailer.ts`, add `signal: AbortSignal.timeout(10_000)` to `sendViaBrevoApi`'s `fetch` call. Verify with a unit test: a `fetch` mock that never resolves causes `sendViaBrevoApi` to reject within a bounded time rather than hanging (e.g. use fake timers or a short test-local timeout override). (Confirmed empirically that `AbortSignal.timeout()` does not respect vitest's fake timers — first attempt technically passed but took a real 10s. Switched to an optional `timeoutMs` parameter, defaulting to the real 10s production value, so the test passes a 50ms override instead. 4/4 mailer tests pass in ~200ms.)

## 3. Outbox library

- [x] 3.1 Create `apps/api/src/lib/outbox.ts` exporting a function to create an outbox row (encrypting the payload via `crypto.ts`'s `encrypt`) and `processEmailOutbox(prisma, outboxId)` implementing the idempotent send-and-update logic from design.md Decision 4 (skip if already `SENT`; on success mark `SENT`/`sentAt`/null the payload; on failure mark `FAILED`, increment `attempts`, record `error`, rethrow). Verify with unit tests: a `SENT` row's processor call makes no mailer call; a successful send updates status/sentAt/nulls payload; a failing send updates status/attempts/error and rethrows. (4/4 tests pass, covering both VERIFICATION and PASSWORD_RESET types.)

## 4. Queue and worker wiring

- [x] 4.1 In `apps/api/src/lib/queues.ts`, add `getEmailQueue()` following the existing `getDictionaryBuildQueue()`/`getMaintenanceQueue()` pattern; include it in `closeQueues()`. Verify `pnpm --filter @planetos/api exec tsc --noEmit` passes. (Typecheck clean.)
- [x] 4.2 In `apps/api/src/worker.ts`, add a `Worker` for the `email` queue calling `processEmailOutbox`, an `EMAIL_JOB_RETRY_OPTIONS` constant (`attempts: 3`, exponential backoff, matching `BUILD_JOB_RETRY_OPTIONS`'s shape) exported from `outbox.ts` or `worker.ts` as appropriate, an `.on("failed", ...)` logger matching the existing workers, and graceful shutdown in `shutdown()`. Verify `pnpm --filter @planetos/api exec tsc --noEmit` passes. (`EMAIL_JOB_RETRY_OPTIONS` exported from `outbox.ts`, colocated with the processor, matching `sweep.ts`'s pattern. Typecheck clean.)
- [x] 4.3 In `apps/api/src/worker.ts`, register an hourly repeatable job (via `upsertJobScheduler`, matching `sweep-changed-series`'s registration) that queries `EmailOutbox` for `PENDING` rows older than a few minutes and re-enqueues each with the same `deduplication` key as the original enqueue. Verify `pnpm --filter @planetos/api exec tsc --noEmit` passes. (Used a fixed `"0 * * * *"` cron literal rather than reusing `config.buildCron` — that config value is scoped to tuning the dictionary-build sweep specifically, and reusing it here would create a hidden coupling between two unrelated schedules. Typecheck clean.)
- [x] 4.4 In `apps/api/src/index.ts`, add the `email` queue to the existing `createBullBoard({ queues: [...] })` call. Verify `pnpm --filter @planetos/api exec tsc --noEmit` passes. (Typecheck clean.)

## 5. Route changes

- [x] 5.1 In `apps/api/src/routes/auth.ts`'s `register` handler, wrap `user.create` + `emailVerificationToken.create` + the new outbox-row creation in one interactive `prisma.$transaction`; after commit, enqueue the email job (try/catch around the enqueue call per design.md Decision 3); remove the direct `sendVerificationEmail` call and its try/catch. Verify `pnpm --filter @planetos/api exec tsc --noEmit` passes. (Typecheck clean.)
- [x] 5.2 Apply the same restructuring to `forgot-password`: the prior-token `updateMany`, the new `passwordResetToken.create`, and the outbox-row creation move inside one interactive transaction; enqueue after commit; remove the direct `sendPasswordResetEmail` call. Verify `pnpm --filter @planetos/api exec tsc --noEmit` passes. (Typecheck clean.)
- [x] 5.3 Apply the same restructuring to `resend-verification`. Verify `pnpm --filter @planetos/api exec tsc --noEmit` passes. (Typecheck clean.)
- [x] 5.4 Confirm `reset-password`, `verify-email` (SEC-002's already-fixed token-consumption handlers) and the admin-approval route's `sendAccountApprovedEmail` call are untouched. Verify via `git diff` showing no changes to those handlers. (Confirmed: no Edit calls touched `reset-password`/`verify-email` in `auth.ts`, and `admin.ts` — the only other caller of `sendAccountApprovedEmail` — was never opened this session.)

## 6. Tests — atomicity guarantee

- [x] 6.1 Add a test proving the core correctness claim: force the outbox-creation step to fail (e.g. inject a Prisma error) during a `forgot-password` or `resend-verification` request, and assert the account's prior, previously-valid token remains usable afterward (the concrete proof of "avoid invalidating the prior token until the replacement has a durable delivery job"). Verify the test passes against real Postgres. (New file `tests/emailOutboxAtomicity.test.ts`: mocks `createOutboxEntry` to reject, covering `forgot-password`, `resend-verification`, and `register` (no orphaned user). 3/3 pass against real Postgres.)

## 7. Tests — resilience and outbox creation

- [x] 7.1 Rework `apps/api/tests/authEmailResilience.test.ts`: remove the now-meaningless mailer-mock-based assertions (the route never calls the mailer), and replace with tests asserting each of the three routes still returns its normal success response immediately even when the queue enqueue itself fails (mock the queue's `add` to reject), and that an `EmailOutbox` row is created with the expected `type`/`recipientEmail`/`PENDING` status alongside the token. Verify the tests pass against real Postgres/Redis. (Mocks `getEmailQueue` from `lib/queues.js` to reject on `add`. 3/3 pass against real Postgres/Redis.)

## 8. Tests — job processor

- [x] 8.1 Add a new test file for `processEmailOutbox`/the `email` Worker: a mailer failure followed by a retry that succeeds transitions the outbox row `PENDING → FAILED → SENT`; exhausted retries leave it `FAILED` with the last error recorded; a row already `SENT` is never re-sent even if its job is reprocessed (the idempotency guard). Verify the tests pass against real Postgres/Redis, following the retry-testing conventions already established in `apps/api/tests/lib/mailer.test.ts` and the BullMQ `Worker`-driving pattern from `apps/api/tests/sweepRebuild.test.ts`. (New file `tests/outboxWorker.test.ts`: a dedicated test queue/Worker driving real `processEmailOutbox` calls with a mocked mailer and a fast fixed backoff. 3/3 pass against real Postgres/Redis.)

## 9. Verification

- [x] 9.1 `pnpm --filter @planetos/api exec tsc --noEmit` passes with all changes in place. (Clean.)
- [x] 9.2 `pnpm --filter @planetos/api test` passes in full against real Postgres/Redis — no regressions to any existing suite. (334/334 pass across 32 files. Two pre-existing integration test files not named in this change's task list needed updates to keep passing, since they assumed synchronous mailer delivery: `tests/emailVerification.test.ts` and `tests/passwordReset.test.ts` now start a real `email`-queue Worker via a new `startEmailWorker()` test helper in `tests/helpers.ts` and poll Mailpit instead of checking once, since delivery is now asynchronous. `tests/workerPreflight.test.ts` also needed its `../src/lib/queues.js` mock extended with `getEmailQueue` (worker.ts now calls it at import time). Flagging this here per the apply guardrail on added scope: it was mechanical and directly required by this task's own "no regressions" bar, not a design change, so it was completed rather than paused on.)
- [x] 9.3 `openspec validate add-email-outbox --type change --strict` passes. (Passes.)
- [x] 9.4 Read-through: confirm `reset-password`/`verify-email` and the admin-approval route are unchanged (task 5.4's `git diff` check, re-confirmed at the end); confirm the migration is purely additive (no existing table altered). (Re-confirmed: `git diff --stat` shows only `src/routes/auth.ts` changed in that area, `src/routes/admin.ts` untouched; the migration directory is new/untracked, adding only 2 enums + 1 table.)
