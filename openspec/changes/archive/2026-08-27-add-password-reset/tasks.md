## 1. Housekeeping: untrack .env

- [x] 1.1 Add `.env` to `.gitignore`, then run `git rm --cached .env` (keeps the file on disk, stops git from tracking it). Verify with `git status` showing `.env` as untracked and `git ls-files .env` returning nothing. Do NOT touch `.env.example` (stays tracked, never contains real values). Done ahead of the normal apply cycle, at the user's explicit request right after this change was proposed (urgency: a real Brevo credential was about to be added to local config). `.gitignore` now lists `.env`; `git ls-files .env` returns nothing; the file is still present on disk (confirmed via `test -f`). The deletion from git's index is staged but **not committed** — left for the user to commit, per this project's "never commit unless explicitly asked" convention.

## 2. Infra: local SMTP test-catcher

- [x] 2.1 Add a `mailpit` service to `infra/docker-compose.yml` (image `axllent/mailpit`, SMTP port `1025:1025`, HTTP API/UI port `8025:8025`, a healthcheck). Verify `docker compose -f infra/docker-compose.yml up -d` starts it and `curl http://localhost:8025/api/v1/messages` returns a valid (empty) JSON response. Verified: container reports healthy, API returned `{"total":0,...,"messages":[]}`.

## 3. Backend: mailer

- [x] 3.1 Add `nodemailer` + `@types/nodemailer` to `apps/api/package.json`. Create `apps/api/src/lib/mailer.ts`: a lazily-constructed transporter (same lazy-getter pattern as `storage.ts`/`queues.ts` — avoids the established ESM-import-hoisting-vs-dotenv-config ordering bug) built from `SMTP_URL`, and an exported `sendPasswordResetEmail(to: string, resetUrl: string): Promise<void>` that sends a plain-text email with the reset link. Verify `pnpm --filter api exec tsc --noEmit` passes. Clean.
- [x] 3.2 Add a test (`apps/api/tests/lib/mailer.test.ts`) that calls `sendPasswordResetEmail` against the real local Mailpit (`SMTP_URL=smtp://localhost:1025`), then fetches Mailpit's `GET /api/v1/messages` HTTP API and asserts a real message arrived with the expected recipient and a body containing the reset URL. Verify with `pnpm --filter api test`. Used Mailpit's `/search?query=to:` endpoint to find the message by its unique test recipient, then `/message/:id` to fetch the full body and assert the exact reset URL is present. 1/1 passing.

## 4. Backend: reset-token model

- [x] 4.1 Add a `PasswordResetToken` model to `apps/api/prisma/schema.prisma` (per design.md decision 3: `id`, `userId` + `user` relation, `tokenHash` unique, `expiresAt`, `usedAt` nullable, `createdAt`, `@@index([userId])`). Generate and apply the migration (`prisma migrate dev`; fall back to the established shadow-DB-diff workaround only if blocked by the known checksum-mismatch issue). Verify `pnpm --filter api prisma migrate status` shows up to date and `pnpm --filter api test` (existing suite) still passes unaffected. Hit the same known checksum-mismatch-on-an-earlier-hand-edited-migration issue as prior changes; applied via the established shadow-DB-diff-then-hand-author-then-`migrate deploy` workaround. Also hit the recurring EPERM Prisma-DLL-lock issue (two leftover duplicate `pnpm dev` stacks from earlier sessions holding the client DLL open) — killed the offending `tsx watch` processes, regenerated the client successfully, then restarted one clean `pnpm dev` stack so the environment wasn't left broken. `migrate status` shows "up to date"; full suite 231/231 (230 prior + the new mailer test).

## 5. Shared: schemas

- [x] 5.1 Add `forgotPasswordSchema` (`{ identifier: string, min 1 }`) and `resetPasswordSchema` (`{ token: string, min 1, password: passwordSchema }`) to `packages/shared/src/auth.ts`, exporting `ForgotPasswordDto`/`ResetPasswordDto` types. Add unit tests in `packages/shared/src/__tests__/auth.test.ts` covering valid input, empty identifier, and password-complexity failures reusing the existing `passwordSchema` test patterns. Verify with `pnpm --filter shared test`. 6 new tests; full shared suite 57/57.

## 6. Backend: forgot-password route

- [x] 6.1 Add `FORGOT_PASSWORD_RATE_LIMIT = { max: 5, timeWindow: "1 hour" }` to `apps/api/src/plugins/rateLimit.ts` (per design.md decision 7). Add `INVALID_RESET_TOKEN: () => new DomainError("INVALID_RESET_TOKEN", "This password reset link is invalid or has expired.", 400)` to `apps/api/src/lib/errors.ts`. Also added `RESET_PASSWORD_RATE_LIMIT` (task 7.1's tier) at the same time, same file.
- [x] 6.2 Implement `POST /api/auth/forgot-password` in `apps/api/src/routes/auth.ts`: parse `forgotPasswordSchema`, look up the user by normalized username or email (same `OR` pattern as login), and — only if found and `isActive` — generate a random token, hash it, invalidate any of that user's prior unused `PasswordResetToken` rows (set `usedAt`), create the new row (`expiresAt` = now + 1h), and send the reset email via `sendPasswordResetEmail` with a URL built from `PUBLIC_BASE_URL` + `/reset-password?token=...`. Always respond `200` with the same generic message body regardless of match. Verify with tests: matching active user → email actually received via Mailpit with a working-looking link; unknown identifier → same response body, no email sent (assert via Mailpit's message list being empty for that run); disabled account → same response body, no email sent; two requests for the same account → the first token no longer redeemable (covered together with task 7's tests once redemption exists). Verify with `pnpm --filter api test`. Implemented in `apps/api/tests/passwordReset.test.ts` (new file, matching the project's one-file-per-feature test convention), using per-test unique email addresses to avoid any dependency on Mailpit's message ordering. **Found and fixed a real bug during testing**: deleting a User with outstanding `PasswordResetToken` rows failed with a foreign-key violation (the relation defaulted to `ON DELETE RESTRICT`) — added `onDelete: Cascade` to the relation and applied it as a second migration (`20260827202030_password_reset_token_cascade_delete`, same shadow-DB-diff workaround as 4.1). All 5 forgot-password scenarios passing; full API suite 241/241.

## 7. Backend: reset-password route

- [x] 7.1 Implement `POST /api/auth/reset-password` in `auth.ts`: parse `resetPasswordSchema`, hash the incoming token, look up an unused `PasswordResetToken` by `tokenHash` with `expiresAt > now`; if none found throw `Errors.INVALID_RESET_TOKEN()`; otherwise hash the new password, update the user's `passwordHash`, mark the token `usedAt = now`, and return `200` (no session established). Add `RESET_PASSWORD_RATE_LIMIT = { max: 10, timeWindow: "15 minutes" }` to `rateLimit.ts` and apply it to this route. Verify with tests: valid token → password actually changed (subsequent login with the new password succeeds, old password fails); reused token → `400` `INVALID_RESET_TOKEN`, password unchanged; expired token (seed one with a past `expiresAt`) → same error; unknown/garbage token → same error; requesting a second forgot-password for an account invalidates the first token (redeeming the first after the second was issued fails). Verify with `pnpm --filter api test`. All 5 reset-password scenarios passing (valid token changes password and old password stops working; reused token rejected with the generic error and the first change stays in effect; expired token rejected via a directly-seeded expired row; unknown/garbage token rejected; complexity-rule violation rejected without touching the password). No session established on success (`set-cookie` header absent), per design.md decision. Full API suite 241/241.

## 8. Frontend: login page forgot-password mode

- [x] 8.1 In `apps/web/src/routes/login.tsx`: extend `loginSearchSchema`'s `mode` enum to include `"forgot-password"`, add a "Forgot Password?" link/button in `SignInForm` navigating to `/login?mode=forgot-password`, and render a `ForgotPasswordForm` (single "Username or Email" field, submit button, generic confirmation message shown in place of the form after submit) when `mode === "forgot-password"` instead of the Sign In/Register tabs — with a "Back to Sign In" link. Add `apiForgotPassword(identifier: string): Promise<void>` to `apps/web/src/lib/api.ts`. Verify by visiting `/login`, clicking "Forgot Password?", submitting a username, and confirming the generic message appears (regardless of whether that username exists) and no tabs are shown in that mode. Implemented `apiForgotPassword(data: ForgotPasswordDto)` (DTO-object shape, matching `apiLogin`/`apiRegister`'s established convention, rather than a bare string param) — same effective behavior. The form's catch block deliberately swallows any request error before showing the confirmation, so a network hiccup can't accidentally leak a different UI state than the no-match case. Verified live via Playwright: clicked "Forgot Password?" from `/login`, submitted `admin@example.com`, confirmed the generic "If an account...was found..." message rendered with no Sign In/Register tabs visible.
- [x] 8.2 Run `pnpm --filter web exec vite build` to confirm `routeTree.gen.ts` still resolves cleanly for `/login` (search schema change only, no new route file for this task). Verify `pnpm --filter web exec tsc --noEmit` passes. Both clean.

## 9. Frontend: reset-password page

- [x] 9.1 Create `apps/web/src/routes/reset-password.tsx`: reads `token` from the URL search params (validated via a route search schema), shows a "new password" + "confirm password" form (reusing the same confirm-password-match pattern as `RegisterForm`) when a token is present, calls a new `apiResetPassword({ token, password }): Promise<void>` in `api.ts`, and on success navigates to `/login` with a success toast/message. On a `400 INVALID_RESET_TOKEN` response (or no `token` param at all), shows a clear "this link is invalid or expired" message instead of the form. Verify `pnpm --filter web exec vite build` regenerates `routeTree.gen.ts` with the new `/reset-password` route present. Confirmed via grep: `ResetPasswordRoute`/`/reset-password` present in the generated route tree.
- [x] 9.2 Run `pnpm --filter web lint` and `pnpm --filter web exec tsc --noEmit`; confirm both pass clean. Both clean.

## 10. Documentation

- [x] 10.1 Update `SPEC.md` §6 (HTTP API) — correct the `forgot-password` line from `{ email }` to `{ identifier }` (username or email, matching `login`'s own convention), and confirm `reset-password`'s `{ token, password }` line is already accurate. Done; also noted the generic-response/no-session-on-success behavior inline.
- [x] 10.2 Update `SPEC.md` §8 (Frontend > Routes) to note `/login`'s new `forgot-password` mode and add `/reset-password` to the routes list (it's currently only mentioned inline as `/login /register /reset-password` with no dedicated line). Split the old combined `/login /register /reset-password` line into a proper `/login` entry (noting the new mode) and a dedicated `/reset-password` entry.
- [x] 10.3 Update `.env.example`'s `SMTP_URL` comment to note the `@`-in-username percent-encoding gotcha (per design.md decision 1), and update `SPEC.md` §10 (Local development) to mention the new `mailpit` service if that section lists the other docker-compose services by name. Done — `.env.example` now calls out percent-encoding and the local Mailpit default; `SPEC.md`'s docker-compose comment now lists `mailpit` alongside `postgres, redis, minio`.

## 11. End-to-end verification

- [x] 11.1 Run `pnpm --filter api test`, `pnpm --filter shared test`, `pnpm --recursive typecheck`, `pnpm --filter api lint`, `pnpm --filter web lint`, and `pnpm --filter web build`; confirm all succeed. api 241/241, shared 57/57, typecheck clean across all 4 packages, both lints clean, web build succeeded (pre-existing chunk-size warning only, unrelated to this change).
- [x] 11.2 Manually verify against the real running dev stack (including the new Mailpit service): submit the forgot-password form for a real seeded user, confirm a real email arrives in Mailpit's web UI (`localhost:8025`) with a working-looking reset link; open that link, set a new password, confirm redirect to `/login`; log in with the new password successfully and confirm the old password no longer works; submit forgot-password for a nonexistent username and confirm the identical generic message appears; attempt to reuse the already-redeemed reset link and confirm a clear "invalid or expired" message; confirm `.env` is genuinely untracked (`git ls-files .env` empty) and the real Brevo credential exists nowhere in the repo (`git grep` for a distinctive fragment of it returns nothing). Used a throwaway registered user (not the real seeded admin, to avoid altering persistent dev credentials) for the parts that change a real password; deleted it afterward (its `PasswordResetToken` row cascade-deleted along with it, exercising that fix in the real app path too, not just tests). All verified live via Playwright + Mailpit's real UI/API: real email received with a working link; reset succeeded and redirected to `/login`; login with the new password succeeded; the old password was rejected (`401 invalid-credentials`); a nonexistent identifier produced the byte-identical generic message with zero messages in Mailpit for that address; reusing the redeemed link showed "Invalid or Expired Link" with a "Request a new reset link" action. `git ls-files .env` empty, `.env` still present on disk; `git grep` for a distinctive fragment of the real Brevo key and for the Brevo login address both returned no matches anywhere in the repo.
