## 1. Schema

- [x] 1.1 Add `Series.dirtySince DateTime?` and `@@index([dirtySince])` to `apps/api/prisma/schema.prisma` per design.md Decision 1. Generate and apply the migration (`prisma migrate dev`); verify it is purely additive (no existing column/table altered) and `pnpm --filter @planetos/api exec tsc --noEmit` passes. (Migration `20260907020911_add_series_dirty_since`: adds one nullable column + one index only. Typecheck clean.)

## 2. Dirty-marking helper

- [x] 2.1 Create `apps/api/src/lib/dirtySeries.ts` exporting `markSeriesDirty(tx, seriesId)` per design.md Decision 2, with the doc comment warning future write paths (including test fixtures) to call it. Verify `pnpm --filter @planetos/api exec tsc --noEmit` passes. (Typecheck clean.)

## 3. Wire dirty-marking into write paths

- [x] 3.1 In `apps/api/src/routes/entries.ts`, call `markSeriesDirty` inside the existing transaction in the admin-auto-approved branch of `POST /api/series/:slug/entries` (entry created with `approvalStatus: "APPROVED"`), and inside the existing transaction in `POST /api/admin/entries/:id/approve`. Add a comment at `POST /api/admin/entries/:id/reject` explaining why it is deliberately not marked dirty (tied to the existing PENDING-only guard, per design.md Decision 2). Verify `pnpm --filter @planetos/api exec tsc --noEmit` passes. (Typecheck clean.)
- [x] 3.2 In `apps/api/src/routes/entryEditProposals.ts`, call `markSeriesDirty(tx, entry.seriesId)` once inside `applyEditProposalToEntry`, covering both its call sites. Verify `pnpm --filter @planetos/api exec tsc --noEmit` passes. (Typecheck clean.)
- [x] 3.3 In `apps/api/src/routes/series.ts`'s `PATCH /api/series/:slug` handler, fold `dirtySince: new Date()` into the existing `series.update` call's `data`. Verify `pnpm --filter @planetos/api exec tsc --noEmit` passes. (Typecheck clean.)

## 4. Sweep and reconciliation

- [x] 4.1 In `apps/api/src/jobs/sweep.ts`, extract the existing hash-check-and-enqueue logic into a shared `checkAndMaybeEnqueue(prisma, queue, series)` helper with the race-safe compare-and-swap clear on a hash match, per design.md Decision 4. Add the `withConcurrency` helper and `SWEEP_CONCURRENCY = 5` constant per Decision 5. Verify `pnpm --filter @planetos/api exec tsc --noEmit` passes. (Typecheck clean.)
- [x] 4.2 Update `runSweep` to select only `WHERE dirtySince IS NOT NULL OR contentHash IS NULL`, process candidates via `withConcurrency`, and log `[sweep] checked N ..., enqueued M ...` per design.md Decision 6. Verify `pnpm --filter @planetos/api exec tsc --noEmit` passes. (Typecheck clean.)
- [x] 4.3 Add a new exported `runReconciliation(prisma, queue)` to `apps/api/src/jobs/sweep.ts` that selects every `Series` row, runs the same `checkAndMaybeEnqueue` via `withConcurrency`, and logs candidates checked / builds enqueued / a "missed by dirty-tracking" count (a mismatch found where `dirtySince` was `null`) per design.md Decision 6. Verify `pnpm --filter @planetos/api exec tsc --noEmit` passes. (Typecheck clean.)
- [x] 4.4 In `apps/api/src/jobs/build.ts`, read `dirtySince` at the start of `processDictionaryBuild` (before `loadSeriesInputs`) and add a race-safe `dirtySince` clear (a separate `series.updateMany` CAS'd on that value) to the success transaction, alongside the unconditional `contentHash` update, per design.md Decision 8's two-statement structure. Verify `pnpm --filter @planetos/api exec tsc --noEmit` passes. (Typecheck clean.)
- [x] 4.5 In `apps/api/src/worker.ts`, register the `reconcile-all-series` repeatable job via `upsertJobScheduler` on the literal `"0 3 * * *"` cron, and add the `job.name === "reconcile-all-series"` dispatch branch calling `runReconciliation`, per design.md Decision 7. Verify `pnpm --filter @planetos/api exec tsc --noEmit` passes. (Typecheck clean.)

## 5. Test rework — keep existing coverage real

- [x] 5.1 Update `apps/api/tests/sweep.test.ts`'s content-mutation helpers (the raw `prisma.entry.update`/`prisma.entry.create` calls that simulate an edit) to also call `markSeriesDirty` directly, so the existing 5 test cases keep exercising real candidate-selection and hash-comparison behavior under the new query rather than passing vacuously. Verify all 5 existing cases still pass, and specifically confirm (e.g. via a temporary manual check during implementation) that the "an edit reverted to its exact prior state..." case genuinely re-enters `checkAndMaybeEnqueue` rather than being skipped as a non-candidate. (5/5 pass; confirmed via the `[sweep] checked 1 ...` log line that the reverted-edit case is genuinely re-checked, not skipped.)
- [x] 5.2 Apply the same fix to `apps/api/tests/sweepRebuild.test.ts`'s raw `prisma.entry.update` calls between the A→B→A build phases. Verify both existing test cases still pass against real Postgres/Redis with a live `Worker`. (2/2 pass; the A->B->A test's B-build and revert-to-A-build steps now genuinely proceed instead of timing out, confirming the earlier baseline failure is fixed.)

## 6. New test coverage

- [x] 6.1 Add a test to `apps/api/tests/sweep.test.ts` proving proportionality: create a series with a `contentHash` already matching its current content and `dirtySince: null` (clean), and a separate dirty series; run `runSweep`; assert only the dirty series' build is enqueued (the clean series is never a sweep candidate). Verify against real Postgres/Redis. (Log line `checked 1 dirty/new series` with 2 series present confirms the clean one was never inspected. 6/6 pass.)
- [x] 6.2 Add a new test file `apps/api/tests/sweepReconciliation.test.ts` proving the "missed marker" case: create a series with a stale `contentHash` (no longer matching current content) and `dirtySince: null` (simulating a write path that failed to mark dirty); run `runSweep` and assert it enqueues nothing (proving the gap is real); then run `runReconciliation` and assert it enqueues a build for that series. Verify against real Postgres/Redis. (Exported `checkAndMaybeEnqueue`/`SeriesCandidate` from sweep.ts for direct testability. Log line `1 missed by dirty-tracking` confirms detection. Also added a "leaves an already-consistent series alone" case.)
- [x] 6.3 In the same new test file, add a test proving the CAS-based clear survives a race: set up a series whose hash will match (no rebuild needed), but mutate `dirtySince` to a newer value (simulating a concurrent edit landing) after the check's `loadSeriesInputs` read would occur but before its clear would apply — e.g. by driving `checkAndMaybeEnqueue` directly with a stale `series.dirtySince` snapshot argument while the row's actual `dirtySince` has already moved on — and assert the clear does not erase the newer mark (the row's `dirtySince` remains non-null afterward). Verify against real Postgres. (Also added the mirror case: a clean CAS clear with no concurrent edit does clear the mark. 4/4 tests in the new file pass.)
- [x] 6.4 Add or extend tests in `apps/api/tests/entries.test.ts` and/or `apps/api/tests/entryEditProposals.test.ts` and `apps/api/tests/series.test.ts` (whichever exist) asserting that a real route call that changes hash-relevant content (admin entry creation, entry approval, edit-proposal approval, series `PATCH`) sets `Series.dirtySince` to a non-null value, and that the reject handler and non-admin pending entry creation do not. Verify against real Postgres. (4 new tests in entries.test.ts (44/44 pass), 4 new in entryEditProposals.test.ts (34/34 pass), 1 new in series.test.ts (22/22 pass).)

## 7. Verification

- [x] 7.1 `pnpm --filter @planetos/api exec tsc --noEmit` passes with all changes in place. (Clean.)
- [x] 7.2 `pnpm --filter @planetos/api test` passes in full against real Postgres/Redis — no regressions to any existing suite. (348/348 pass across 33 files.)
- [x] 7.3 `openspec validate add-sweep-dirty-tracking --type change --strict` passes. (Passes.)
- [x] 7.4 Read-through: confirm every write site enumerated in design.md's Context either calls `markSeriesDirty`/folds the field, or carries a comment explaining why not; confirm both clear sites (sweep's `checkAndMaybeEnqueue`, `build.ts`'s success path) use the CAS `updateMany` pattern, not a blind update; confirm the migration is purely additive. (Grep-confirmed all 7 write/clear sites match design.md exactly: entries.ts create (line 135) and approve (254) call `markSeriesDirty`, reject (290) has the explanatory comment; entryEditProposals.ts's shared `applyEditProposalToEntry` (67) calls it once for both call sites; series.ts PATCH (160) folds the field; sweep.ts's `checkAndMaybeEnqueue` (61-62) and build.ts's success path (78-79) both use CAS `updateMany`, not a blind update. Migration re-confirmed as 1 column + 1 index only.)
