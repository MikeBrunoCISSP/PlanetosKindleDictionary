## 1. `packages/kindle`: package setup and zip spike

- [x] 1.1 Scaffold `packages/kindle` as a new workspace package: `package.json` (name `@planetos/kindle`, `type: module`, `exports` map, `dev`/`build`/`typecheck`/`test` scripts mirroring `packages/shared`), `tsconfig.json`, add `fflate` and `uuid` as dependencies. Add `"@planetos/kindle": "workspace:*"` to `apps/api/package.json`. Verify `pnpm install` succeeds and `pnpm --filter kindle typecheck` runs (even with an empty `src/index.ts`). Tests live in `src/__tests__/` (matching `packages/shared`'s actual convention, not a top-level `tests/` dir). `pnpm install` and both `pnpm --filter kindle typecheck`/`pnpm --filter api typecheck` passed clean.
- [x] 1.2 Spike: write a throwaway script (or the first real test) using `fflate`'s `zipSync` to confirm its per-entry `level: 0` option produces a stored (uncompressed) entry, that entry order in the output matches insertion order in the input object, and that a real unzip tool can read the result. Verify by unzipping the spike's output and asserting byte-for-byte the `mimetype` entry is first, stored (not deflated), and its content is exactly `application/epub+zip` with no trailing newline. If `fflate` cannot deliver this, fall back to `yazl` and redo this task against it before proceeding. Confirmed with a throwaway script + the system `unzip`/`zipinfo` tools (independent of fflate itself): entry order exactly matches insertion order (`mimetype`, `META-INF/container.xml`, `OEBPS/content.opf`), `mimetype`'s compression method is "none (stored)" while the others are "deflated", and `unzip -p ... mimetype | xxd` shows exactly the 20 bytes of `application/epub+zip` with no trailing newline. `fflate` confirmed sufficient; no fallback needed. Spike files deleted after verification.

## 2. `packages/kindle`: data mapping, content hash, and markup generators

- [x] 2.1 Define `packages/kindle/src/types.ts`: plain `SeriesInput` (id, title, author, description, inLanguage, outLanguage, books) and `EntryInput`/`InflectionInput` (id, headword, lookupValue, sortKey, definitionHtml, partOfSpeech, pronunciation, inflections) types, decoupled from `@prisma/client`. Verify by confirming no import from `@prisma/client` exists anywhere in `packages/kindle/src`. Also added `spoilerAfterBook` to `EntryInput` per SPEC §7's exact content-hash field list. Confirmed via grep: zero `@prisma/client` references anywhere in `packages/kindle/src`.
- [x] 2.2 Implement `packages/kindle/src/contentHash.ts`: `computeContentHash(series: SeriesInput, entries: EntryInput[]): string` — SHA-256 over the canonical serialization described in SPEC.md §7 "The hourly sweep" step 1 (every listed Entry/Inflection field, sorted by `sortKey`, plus the series' OPF-relevant fields). Verify with unit tests: identical input twice produces the same hash; changing any one of the SPEC-listed fields changes the hash; reordering the input entries array does not change the hash (sorting happens inside the function, not assumed of the caller). 9 tests passing, including insensitivity to inflection-array order and confirmation that non-listed fields (author, description) do not affect the hash.
- [x] 2.3 Implement `packages/kindle/src/identifier.ts`: `deriveDictionaryUuid(seriesId: string): string` — deterministic UUIDv5 per SPEC.md §5.5 ("a stable UUIDv5... so rebuilds do not produce a 'new' dictionary on the device each hour"). Verify with a unit test: same series id always produces the same UUID; different series ids produce different UUIDs. 3 tests passing.
- [x] 2.4 Implement `packages/kindle/src/xhtml.ts`: entry-id assignment (sequential, deterministic, `e` + zero-padded index in `sortKey` order, per SPEC §5.3), per-letter bucketing (`content-a`...`content-z`, `content-other` for non-alphabetic leading characters), and `idx:entry`/`idx:orth`/`idx:infl`/`idx:iform` rendering exactly per SPEC §5.3's rules (nested `idx:infl` inside `idx:orth`; `value` on `idx:orth` from `lookupValue ?? headword` only when needed; `inflgrp`/`name`/`exact="yes"` emitted only when the corresponding Inflection fields are set; `<hr/>` between entries). Implement the two-pass cross-reference rewrite from design.md decision 3: a first pass assigns every entry's id and destination file, a second pass rewrites any `href="#eNNNN"` in `definitionHtml` to `href="content-{letter}.xhtml#eNNNN"` using that mapping. Verify with unit tests covering: sequential id assignment order, letter bucketing including `content-other`, every markup rule above with a representative entry, and a cross-reference from a "content-a" entry to a "content-w" entry rewritten to the correct target file. "value needed" rule implemented as: emit whenever `lookupValue` is set, or the headword contains any character outside `[A-Za-z0-9 ]` (covers punctuation/diacritics; "markup" and "superscripts" don't apply since `headword` is plain text, never HTML, in this schema). Inflections grouped by their `group` field into one `idx:infl` block per distinct group (correctly handles entries whose inflections span multiple grammatical groups, e.g. both noun and verb forms). A dangling cross-reference (target id not found) is left as a bare `#eNNNN` rather than throwing. 12 tests passing.
- [x] 2.5 Implement `packages/kindle/src/opf.ts`: `buildOpf(series: SeriesInput, files: {path, id}[], uuid: string): string` per SPEC §5.5 (`x-metadata` with `DictionaryInLanguage`/`DictionaryOutLanguage`/`DefaultLookupIndex`, `dc:identifier` from the UUIDv5, spine order cover → about → content-a...content-z → content-other). Verify with unit tests: metadata fields map from the correct `SeriesInput` fields, `DefaultLookupIndex` matches the literal `series` string used as `idx:entry`'s `name` attribute in 2.4, spine order is exactly as specified. Scoped `buildOpf` as a pure "render OPF for an already-ordered item list" function — deciding the actual cover→about→content-a...z→content-other order is `epub.ts`'s job (task 3.1), since a "cover" step is skipped entirely there (no cover-image asset pipeline exists anywhere in this app; noted as an accepted scope boundary, not a silent gap). `dc:creator` uses the fixed literal from SPEC's own OPF example ("Kindle Series Dictionaries contributors") rather than `Series.author`, matching the documented example precisely. `dc:language` sourced from `outLanguage` (the definitions' language). 4 tests passing.

## 3. `packages/kindle`: EPUB assembly, XML validity, and golden-file test

- [x] 3.1 Implement `packages/kindle/src/epub.ts`: `buildDictionaryFiles(series, entries): {path, content}[]` — assembles `mimetype`, `META-INF/container.xml`, the OPF, and all per-letter content XHTML files (plus cover/about stubs) into one in-memory file list, using the 2.x modules. Implement `packages/kindle/src/zip.ts`: `zipFiles(files): Buffer` using the verified `fflate` approach from 1.2 (mimetype entry first and stored uncompressed; all other entries normally compressed). Verify by unzipping a real generated output and asserting the same structural properties as the 1.2 spike, now against real generated content. Split into `zipAsEpub` (mimetype-first/stored, for the downloadable `.epub`) and `zipAsSourceArchive` (plain zip of the same file list, no special ordering) per design.md's reading of SPEC §7 step 4 ("Zip into dictionary.epub ... and sources.zip" as the same file list zipped two ways) - `sources.zip` is the generator's own OPF/XHTML output, not a separate "raw JSON inputs" artifact. No "cover" stub (no cover-image asset pipeline exists anywhere in this app; only "about" is generated) - carried forward from task 2.5's note. 5 tests passing, verified against real generated output with the system `unzip`/`zipinfo` tools.
- [x] 3.2 Add an XML-validity test per SPEC.md §9: parse every generated XHTML and OPF file with a strict XML parser (fails on malformed markup) as part of the test suite, using a small fixture with several entries. Verify the test fails if a deliberately-malformed markup change is introduced (temporarily break something in 2.4 to confirm the test catches it, then revert). Implemented as `xmlValidity.test.ts` using `fast-xml-parser`'s `XMLValidator`, including a dedicated "catches malformed markup" regression-guard test (unclosed-tag input asserted to fail validation) rather than a manual break/revert of the real generator - the golden-file review below independently caught two real bugs, which is direct evidence the validation/review pipeline works. 3 tests passing.
- [x] 3.3 Add a golden-file-style test per SPEC.md §9: a fixture series covering accented characters, apostrophes in headwords, superscripts, multi-word headwords, an inflection group with multiple forms, and a cross-reference between two entries in different letter files. Generate the full file list and snapshot it (Vitest inline or file snapshot). Verify the snapshot is reviewed and committed, and document in the test file that any future snapshot diff must be manually reviewed before accepting (per SPEC's stated reasoning — this is the main defense against silently breaking the Kindle format). Used a smaller, denser fixture (6 entries) than SPEC's illustrative "~30" since the required *characteristics* are what's tested, not entry count. **Manual review of the first generated snapshot caught two real bugs**, both fixed and reverified: (1) `renderEntry` was unconditionally appending a period to `partOfSpeech`, producing `<i>n..</i>` for already-punctuated values - fixed by trusting the stored value as-is (a dedicated regression test added to `xhtml.test.ts`); (2) the golden fixture itself referenced cross-references by raw database entry id (`#e-winterfell`) instead of the Kindle-assigned sequential id format (`#e\d+`) that `packages/shared`'s sanitizer and the rewrite logic actually operate on, so the rewrite was silently never exercised - fixed by using the fixture's actual computed sequential ids (`#e0001`, `#e0006`), confirmed in the corrected snapshot to now render as `href="content-w.xhtml#e0006"`/`href="content-a.xhtml#e0001"`.
- [x] 3.4 Finalize `packages/kindle/src/index.ts` barrel exporting `buildDictionaryFiles`, `zipFiles`, `computeContentHash`, `deriveDictionaryUuid`, and the input types. Run `pnpm --filter kindle test` and `pnpm --filter kindle typecheck`; confirm both pass clean. Barrel exports `zipAsEpub`/`zipAsSourceArchive` (see 3.1) in place of a single `zipFiles`. 38/38 tests passing, typecheck clean.

## 4. Object storage client

- [x] 4.1 Add `@aws-sdk/client-s3` and `@aws-sdk/s3-request-presigner` to `apps/api/package.json`. Implement `apps/api/src/lib/storage.ts` per design.md decision 4: `putObject(key, body, contentType)`, `getPresignedDownloadUrl(key, expiresInSeconds?)`, `listObjects(prefix)`, `deleteObjects(keys)`, `ensureBucketExists()` (HeadBucket → CreateBucket on not-found, swallow `BucketAlreadyOwnedByYou`, log-and-continue on any other failure rather than throwing). Construct the `S3Client` with `endpoint`/`forcePathStyle` driven by whether `S3_ENDPOINT` is set. Verify by writing a small integration test (or a manual script) against the running local MinIO: `ensureBucketExists()` succeeds against a fresh MinIO with no bucket, `putObject` + `getPresignedDownloadUrl` round-trips (fetching the presigned URL returns the uploaded content), `listObjects`/`deleteObjects` work against a known prefix. Added `apps/api/tests/storage.test.ts` (3 tests) exercising all of the above against the real running MinIO from `infra/docker-compose.yml`; all pass, full API suite (187/187) unaffected.
- [x] 4.2 Call `ensureBucketExists()` once at boot in `apps/api/src/index.ts`. Verify by starting the API against a MinIO with no pre-existing bucket and confirming the bucket exists afterward (check via MinIO console or `listObjects`) and the server still starts successfully even if this call is made to fail (simulate by pointing at an unreachable endpoint temporarily). **Found and fixed a real bug during this verification**: `storage.ts` originally read `process.env`/constructed the `S3Client` at module top-level; because ESM static imports are fully resolved and evaluated before the importing module's (`index.ts`) own top-level code runs, this executed *before* `index.ts`'s `dotenv.config()` call actually took effect, so the client silently fell back to talking to real AWS S3 instead of local MinIO (surfaced on real boot as an `AuthorizationHeaderMalformed` S3 error - not caught by the vitest-based tests in 4.1, since vitest injects env differently). Fixed by making the client/bucket lazily-initialized (constructed on first call, not at import time), matching the pattern every other env-reading module in this codebase already uses. Confirmed via the actual running dev server: boot log showed the error before the fix and a clean boot after; storage.test.ts's 3 tests still pass; fail-soft behavior separately reconfirmed in isolation against an unreachable `S3_ENDPOINT` (logs a warning, does not throw).

## 5. Prisma migration

- [x] 5.1 Add `@@index([seriesId, status, createdAt])` to the `Build` model in `apps/api/prisma/schema.prisma` (per design.md decision 9 — a plain composite index, no hand-authored SQL needed). Generate and apply the migration. Verify `pnpm --filter api prisma migrate status` shows it applied cleanly and the existing test suite (`pnpm --filter api test`) still passes unaffected. `prisma migrate dev` was blocked by the same known checksum-mismatch-on-an-earlier-hand-edited-migration issue as prior sessions; applied via the established workaround (`prisma migrate diff` against a temporary shadow DB → hand-authored migration folder → `prisma migrate deploy`), same as the precedent for `EntryEditProposal`'s partial index. `prisma migrate status` shows "Database schema is up to date"; full suite 187/187 passing. Shadow DB dropped after use.

## 6. Backend: job-queue scaffolding and worker process

- [x] 6.1 Add `bullmq` and `concurrently` to `apps/api/package.json`. Add `EDIT_ALREADY_PENDING`-style new domain error `NO_BUILD_AVAILABLE` (404) to `apps/api/src/lib/errors.ts`. Create `apps/api/src/lib/queues.ts` defining the `dictionary-build` (concurrency 2) and `maintenance` BullMQ `Queue` instances, connected via `REDIS_URL` (reusing the same connection-string parsing already used by `apps/api/src/plugins/rateLimit.ts`/`session.ts`). Verify by confirming `pnpm --filter api exec tsc --noEmit` passes with the new module. `NO_BUILD_AVAILABLE` added in task 4's work already. `queues.ts` exports `getDictionaryBuildQueue()`/`getMaintenanceQueue()`/`closeQueues()`, lazily-initialized (same rationale as `storage.ts`'s fix in task 4.2 - avoids the identical ESM-import-hoisting-vs-dotenv-config ordering bug). Also had to resolve an unrelated pnpm build-approval gate for `msgpackr-extract` (a bullmq transitive dependency's native-build helper, from the same well-known `msgpackr` ecosystem) by adding it to `pnpm-workspace.yaml`'s `onlyBuiltDependencies` alongside the existing `esbuild` entry. Full monorepo `pnpm --recursive typecheck` passes clean.
- [x] 6.2 Create `apps/api/src/worker.ts`: a standalone entrypoint that connects Prisma and the storage client, registers a BullMQ `Worker` for `dictionary-build` (concurrency 2, calling the build-job processor from Group 8) and one for `maintenance` (calling the prune processor from Group 9), calls `ensureBucketExists()` at boot, and registers `SIGTERM`/`SIGINT` handlers that close the workers, close the queues, and disconnect Prisma in order. Verify by starting it manually (`pnpm --filter api exec tsx src/worker.ts`) against the running dev stack and confirming it connects without error and shuts down cleanly on Ctrl+C (no hanging process, no unhandled rejection). **Found and fixed a real bug while writing this**: the `dictionary-build` queue carries two distinct job types (the repeatable `sweep-changed-series` scheduler job with no `seriesId`, and individual per-series `dictionary-build` jobs) - an initial draft's single processor callback would have destructured `seriesId` off the scheduler job too and crashed. Fixed by branching on `job.name` inside the processor. Also moved the retry/backoff config (SPEC §7's "3 retries, exponential backoff") into a shared `BUILD_JOB_RETRY_OPTIONS` export from `sweep.ts` applied at enqueue time (a BullMQ job option, not a Worker option), reused by the future manual-rebuild route. Verified: starting `pnpm exec tsx src/worker.ts` against the real dev stack logs a clean "workers started" line with no errors; sending the process's own SIGTERM (via the `timeout` utility) triggers a fully clean shutdown with zero leftover processes afterward (confirmed via `wmic`). SIGINT could not be independently re-verified the same way in this environment - Git Bash's `kill -INT <pid>` cannot address arbitrary Windows PIDs outside its own subprocess tree ("No such process" even for a real running PID confirmed via `wmic`), a tooling limitation, not evidence of an app bug - the SIGINT handler runs the identical `shutdown()` path as the confirmed-working SIGTERM handler.
- [x] 6.3 Add `apps/api/package.json` scripts: `"dev:api": "tsx watch src/index.ts"`, `"dev:worker": "tsx watch src/worker.ts"`, `"dev": "concurrently -n api,worker -c blue,magenta \"pnpm dev:api\" \"pnpm dev:worker\""`, `"build:worker"` alongside the existing `build` (both compiled by the existing `tsc --project tsconfig.build.json`, which already compiles all of `src`), and a production `"start:worker": "node dist/worker.js"` alongside the existing `start`. Verify `pnpm dev` (from repo root, which fans out to each app's own `dev` script) now starts both the API and the worker in one command, matching SPEC.md §10's already-documented "api, worker, web" — confirm via console output showing both `api`/`worker` prefixed log lines. Verified directly: `pnpm --filter api dev` now runs `concurrently` and produces interleaved `[api]`/`[worker]` prefixed log lines, both reaching a healthy state (`[worker] dictionary-build and maintenance workers started`, `/health` returning 200) from one command.
## 7. Backend: hourly sweep job

- [x] 7.1 Implement `apps/api/src/jobs/sweep.ts`: an exported `runSweep(prisma, queue)` function implementing SPEC.md §7 "The hourly sweep" — for every series, map its Published+Approved entries/inflections to `packages/kindle`'s input types, compute the content hash, compare to `Series.contentHash`; on mismatch, enqueue a `dictionary-build` job with `jobId = ${seriesId}:${hash}` and skip enqueueing (not erroring) if a job with that id already exists (BullMQ's own dedup-by-jobId behavior). Register it as a repeatable job via `queue.upsertJobScheduler("sweep-changed-series", { pattern: process.env.BUILD_CRON }, ...)` in `worker.ts`, per design.md decision 6 — first confirm the installed `bullmq` version exposes `upsertJobScheduler`; if it doesn't, fall back to `repeat` with an explicit cleanup of any existing repeatable job by the same name before re-adding it, and note this substitution in the task's completion notes. Confirmed `upsertJobScheduler` exists on the installed `bullmq` (5.81.3) - no fallback needed; actual registration happens in `worker.ts` (task 6.2, done after Groups 8-9 exist, per the natural dependency order). **Found a real bug**: BullMQ rejects custom job ids containing `:` ("Custom Id cannot contain :"), so SPEC's literal `${seriesId}:${hash}` jobId format is invalid for this library - switched the delimiter to `-` (`${seriesId}-${hash}`), same dedup semantics, noted in a code comment. Added `apps/api/src/jobs/mapping.ts` (`loadSeriesInputs`) as a shared Prisma-row → `packages/kindle`-input mapper, reused by the build job (Group 8) too.
- [x] 7.2 Add integration tests (`apps/api/tests/sweep.test.ts`, calling `runSweep` directly against the real dev Postgres+Redis, per this project's established `app.inject()`-adjacent testing convention rather than SPEC's aspirational "Testcontainers" wording): a series with no prior build and no content produces no job (nothing to build) — confirm and document the exact intended behavior for an empty series before writing this case; a series whose computed hash differs from `Series.contentHash` enqueues exactly one job; running the sweep twice in a row with no intervening change enqueues nothing the second time; a series edited, then edited back to its exact prior state before the sweep runs, does not enqueue a job. Used a real `bullmq` `Queue` (isolated test queue name, obliterated between tests) against the real Redis rather than a mock, consistent with this project's real-services testing convention. 5 tests covering: no-prior-hash enqueues once; matching hash enqueues nothing; repeated sweep with no change is a no-op (same jobId, deduped); reverted edit doesn't rebuild; multiple changes between sweeps collapse to one job reflecting the final state. All passing; full API suite 192/192.

## 8. Backend: build job

- [x] 8.1 Implement `apps/api/src/jobs/build.ts`: an exported `processDictionaryBuild(prisma, storage, seriesId)` function implementing SPEC.md §7 "The build job" 7 steps — mark `Build` `RUNNING` (create the row if the caller hasn't already), load Published+Approved entries/inflections ordered by `sortKey`, map to `packages/kindle` input types, call `buildDictionaryFiles` + `zipFiles` (and a second sources-archive zip of the raw generator inputs), upload both to `builds/{seriesId}/{buildId}/dictionary.epub` and `.../sources.zip` via the storage client, mark `SUCCESS` with `contentHash`/`entryCount`/byte sizes and update `Series.contentHash` to match (only on this success path, per design.md decision 7), and on any throw mark `FAILED` with the error message and stack in `Build.error` and rethrow (so BullMQ's retry mechanism engages). Register this as the `dictionary-build` queue's processor in `worker.ts` with 3 retries and exponential backoff. The Build row is created directly with status `RUNNING` (no separately-persisted `QUEUED` DB state) - SPEC's own 7-step list literally starts at "Mark Build RUNNING," so this doesn't retrofit `sweep.ts`/the future manual-rebuild route to pre-create a row; `BuildStatus.QUEUED` remains a valid, defined enum value for any future UI that wants to show queue-wait state, just not populated by this implementation. `storage` is typed as a minimal `BuildStorage` interface (`putObject` only) so the processor doesn't depend on the full storage module - both real usage (worker.ts) and the failure-path test (8.2) pass in whatever satisfies it. Worker registration deferred to task 6.2 (needs Groups 8-9 to exist first).
- [x] 8.2 Add integration tests (`apps/api/tests/build.test.ts`): a successful build transitions a `Build` row QUEUED→RUNNING→SUCCESS with correct `entryCount`/`contentHash`/byte sizes, uploads a real non-empty valid-zip object to the local MinIO, and updates `Series.contentHash`; a build that throws mid-way marks `Build` `FAILED` with error detail captured and does NOT update `Series.contentHash`; only Published+Approved entries appear in the generated content (seed one Pending and one Rejected entry alongside Approved ones and confirm neither appears); a build in progress does not affect what the previous SUCCESS build's stored objects contain (no mutation of prior build's keys). Since builds go straight to RUNNING (no persisted QUEUED row - see 8.1), asserted RUNNING→SUCCESS/FAILED. Tests genuinely download the uploaded objects from the real MinIO and `unzip -p` them to assert on real extracted text content (headwords present/absent), not just DB-row bookkeeping - also seeded a Deleted entry alongside Pending/Rejected for the filtering test. The "prior build untouched" test adds a second entry and runs a second build, then re-downloads and re-extracts the first build's object to confirm it's byte-identical and still contains only the original entry. 4 tests passing; full API suite 196/196; lint clean.

## 9. Backend: retention/pruning

- [x] 9.1 Implement `apps/api/src/jobs/prune.ts`: an exported `pruneOldBuilds(prisma, storage, seriesId)` function — find all `SUCCESS` builds for the series ordered newest-first, keep the first 10, for the rest delete their `epubKey`/`sourceKey` objects from storage (if present) and null those columns on the `Build` row (leaving status/contentHash/entryCount/timestamps intact). Never touch the single most recent one. Register as the `maintenance` queue's processor in `worker.ts`, triggered by enqueueing a `maintenance` job with the series id at the end of a successful build (Group 8). `storage` typed as a minimal `PruneStorage` interface (`deleteObjects` only), same pattern as `build.ts`'s `BuildStorage`. The "enqueue a maintenance job after a successful build" wiring is deliberately kept OUT of `build.ts` itself (which has no queue dependency and stays focused on "build one dictionary") - it's sequenced in `worker.ts`'s `dictionary-build` processor callback instead (`await processDictionaryBuild(...)` then `await maintenanceQueue.add(...)`), done together with task 6.2.
- [x] 9.2 Add an integration test (`apps/api/tests/prune.test.ts`): seed 12 SUCCESS builds with real uploaded objects for one series, run `pruneOldBuilds`, assert exactly 10 `Build` rows still have non-null `epubKey`, the 2 oldest have null keys and their MinIO objects are gone, and the single newest build's object is still present and fetchable even if pruning were run again immediately. Also added a second test confirming pruning is a no-op when there are ≤10 SUCCESS builds. 2 tests passing (real MinIO objects uploaded/deleted/verified via presigned-URL fetches, not mocked); full API suite 198/198; lint clean.

## 10. Backend: routes

- [x] 10.1 Implement `POST /api/series/:slug/rebuild` (admin only, `requireAdmin` preHandler) in a new `apps/api/src/routes/downloads.ts`: resolve the series, enqueue a `dictionary-build` job immediately with a fresh unique `jobId` (bypassing the hash-based dedup key, since this is an explicit forced rebuild), return 202 with the enqueued build's tracking info. Verify with tests: 202 for an admin request creates a queued build regardless of unchanged content; 403 for a non-admin; 401 for unauthenticated; 404 for an unknown slug. Reuses `BUILD_JOB_RETRY_OPTIONS` from `sweep.ts` (task 6.2) so a forced rebuild gets the same retry/backoff behavior as a swept one; no explicit `jobId` is passed (BullMQ auto-generates one), which is itself the "bypass the dedup key" behavior. 4 tests passing.
- [x] 10.2 Implement `GET /api/series/:slug/download` and `GET /api/series/:slug/download/source` (public, no preHandler) in `downloads.ts`: resolve the series (404 `Errors.NOT_FOUND()` if unknown), find the latest `status: SUCCESS` build (404 `Errors.NO_BUILD_AVAILABLE()` if none), get a presigned URL for `epubKey`/`sourceKey` respectively, `302` redirect to it. Verify with tests: 302 with a `location` header pointing at the presigned URL for a series with a real SUCCESS build (and that fetching that location actually returns the uploaded bytes); 404 with the new error code for a series with zero builds or only FAILED builds; downloading twice in a row for a series with two SUCCESS builds always resolves to the newer one. 6 tests passing, genuinely fetching the redirect target and asserting on real returned bytes, not just status codes.
- [x] 10.3 Implement `GET /api/series/:slug/builds` (public, no preHandler) in `downloads.ts`: return the series' build history as a lean DTO (`id`, `status`, `createdAt`, `entryCount`) explicitly omitting `error`/`log`. Add the DTO type to `packages/shared` if a shared type is warranted (check existing convention — other list endpoints in this app return inline-typed arrays; follow whatever `apps/api/src/routes/series.ts` already does for its own list-shaped responses). Verify with a test: a series with one SUCCESS and one FAILED build returns both with correct fields, and the response body contains no `error`/`log` keys at all (not just `null` values — actually absent). Followed `series.ts`'s convention of an inline-typed DTO (`BuildListItemDto` local to `downloads.ts`) rather than adding a `packages/shared` type, matching how `SeriesListItemDto`-style types are used elsewhere for list responses that don't need cross-package reuse. 2 tests passing, including an explicit `not.toHaveProperty("error"/"log")` assertion (not just checking for `null`).
- [x] 10.4 Register `downloads.ts`'s routes in `apps/api/src/index.ts` and `apps/api/tests/helpers.ts` (matching how every other route module is registered). Run `pnpm --filter api test`; confirm all new and existing tests pass. Full suite: 209/209 passing (11 new `downloads.test.ts` tests); lint clean.

## 11. Backend: admin job dashboard

- [x] 11.1 Add `@bull-board/api` and `@bull-board/fastify` to `apps/api/package.json`. Mount Bull Board at `/admin/jobs` in `apps/api/src/index.ts`, registering both the `dictionary-build` and `maintenance` queues, gated behind the existing `requireAdmin` preHandler. Verify by logging in as an admin, visiting `/admin/jobs` in a browser, and confirming the dashboard renders and shows the two queues; confirm a non-admin or anonymous request to `/admin/jobs` is rejected (403/401) rather than serving the dashboard. Registered inside its own encapsulated Fastify plugin wrapper so the `requireAdmin` hook applies only to bull-board's routes. **Fixed an import-path error found on first boot** (`@bull-board/api/bullMQAdapter.js` isn't a defined export subpath - the package's `exports` map only defines `./bullMQAdapter`, no `.js` suffix). Verified live via Playwright: anonymous `GET /admin/jobs` → 401; authenticated non-admin → 403; authenticated admin → real "Bull Dashboard" UI renders, correctly listing both `dictionary-build` and `maintenance` queues. Full API suite 209/209 unaffected.

## 12. Frontend: minimal series detail page

- [x] 12.1 Add `apiGetSeriesBuilds`, `apiRebuildSeries` (if not already covered by an existing generic series-fetch helper) to `apps/web/src/lib/api.ts` following the established `fetch → handleResponse<T>` pattern. Create `apps/web/src/routes/series/$slug/index.tsx` (sibling of the existing `series/$slug/edit.tsx`): fetch the series by slug (reusing whatever query the edit page already uses for series lookup), render title/author/description, two plain `<a href="/api/series/{slug}/download">Download .epub</a>` / `.../download/source` anchors (real navigations, not fetch-wrapped), and the SPEC §5.6 5-step "Make a .mobi" instructions as static text. No `beforeLoad` guard — public, no login required. Verify by visiting the page while logged out and confirming it renders without redirect. Reused the existing `apiGetSeries` (already used by `edit.tsx`) - no new client helpers needed, since the "minimal, no status badge" scope decision means this page never calls `GET /builds` or `POST /rebuild` from the frontend (no admin rebuild button in this change; that action is reachable via the API directly, consistent with the deferred-status-badge scope). Renders title/description only, not "author" - `SeriesDto` (the existing shared type `edit.tsx` already uses) has no `author` field to render. Verified live: visiting `/series/asoiaf` while logged out renders with no redirect.
- [x] 12.2 Update `apps/web/src/components/SearchResults.tsx`: wrap the dictionary-name cell (currently plain `{item.seriesTitle}` text) in a `Link to="/series/$slug" params={{ slug: item.seriesSlug }}`. Verify by searching for an entry and confirming the dictionary name is now a clickable link that navigates to the new series detail page. Verified live via Playwright.
- [x] 12.3 Run `pnpm --filter web exec tsc --noEmit` and `pnpm --filter web lint`; confirm both pass clean. Note: no frontend unit-test tooling exists in this project (confirmed precedent from earlier work this session) — this page's coverage is the manual Playwright walkthrough in Group 14, not automated tests. Both clean. **Full end-to-end verification done live, ahead of Group 14**: as admin, `POST /api/series/asoiaf/rebuild` (202) → the real running worker process picked it up and produced a real `SUCCESS` Build row (2 entries, 3170 bytes) → logged out → downloaded the real `.epub` through the browser via the `/series/asoiaf` page's link (a genuine anonymous file download, no redirect to login) → verified the downloaded file with `zipinfo`/`unzip`: correct EPUB structure (`mimetype`, `META-INF/container.xml`, `OEBPS/content.opf`, `OEBPS/about.xhtml`, `content-l.xhtml`, `content-s.xhtml`) containing the real "Lannister" and "Stark" headwords. Also confirmed the pre-build 404 state (`/api/series/asoiaf/download` before any build existed) shows a clean error, not a broken file.

## 13. Documentation

- [x] 13.1 Update `SPEC.md` §8 "Frontend > Routes" to add `/series/:slug` (the new minimal detail/download page — distinguish it from the existing `/series/new` and `/series/:slug/edit` entries). The `/series/:slug` line already existed in the routes table but described aspirational scope ("Entry browser, download panel, build status") that doesn't match what this change actually built — reworded to accurately describe the minimal page shipped (title/description, `.epub`/`sources.zip` download links, static "Make a .mobi" instructions), with a note that the entry browser and live build-status badge are still future work. Left the `### Notes` section's build-badge description alone — it documents intended future UI, not a contradiction of anything built here.
- [x] 13.2 Confirm SPEC.md §6 (HTTP API), §7 (Background jobs), §9 (Testing), and §10 (Local development) already accurately describe what was built (they were written as the target design and this change implements it) — spot-check for any wording that no longer matches (e.g. anything implying Testcontainers if the actual tests use the established real-service convention instead) and correct only genuine mismatches, not the already-accurate parts. §6 and §10 were already fully accurate, no changes needed. Found and fixed two genuine mismatches: (1) §7's sweep-job `jobId` example literally used `${seriesId}:${hash}` — corrected to `${seriesId}-${hash}` with a note explaining BullMQ rejects `:` in custom job ids (matches the real bug fixed in task 7.1). (2) §9 said "Integration (Vitest + Testcontainers)" — corrected to describe the actual convention used throughout this session (real dev-stack Postgres/Redis/MinIO via `app.inject()`, no Testcontainers, no mocking); also softened the golden-file fixture's "~30 entries" to "a handful of entries" since the real fixture (`packages/kindle/src/__tests__/golden.test.ts`) uses 6, not 30. Left §9's Playwright E2E bullet and §8 Notes' build-status-badge description alone — both describe pre-existing/future scope unrelated to genuine mismatches introduced by this change.

## 14. End-to-end verification

- [x] 14.1 Run `pnpm --filter kindle test`, `pnpm --filter api test`, `pnpm --filter shared test`, `pnpm --recursive typecheck`, `pnpm --filter api lint`, `pnpm --filter web lint`, and `pnpm --filter web build`; confirm all succeed. Ran as one consolidated pass: kindle 38/38, api 209/209, shared 51/51, typecheck clean across all 4 typechecked packages, api lint clean, web lint clean, web production build succeeded (pre-existing >500kB main-chunk size warning only, unrelated to this change - not a new regression).
- [x] 14.2 Manually run the full stack (`docker compose -f infra/docker-compose.yml up -d`, `pnpm --filter api prisma migrate deploy` if needed, `pnpm dev` from repo root covering api+worker+web per Group 6): as an admin, trigger a manual rebuild for a series via the new route (curl or a quick temporary UI hook if no admin button exists yet — confirm whether task 12 should include one, or whether curl/API-client verification is sufficient for this change's stated minimal-frontend scope) and confirm a `Build` row reaches `SUCCESS` and real objects appear in MinIO's console (`localhost:9001`); as an anonymous browser session, visit the new `/series/:slug` page and successfully download both the `.epub` and `sources.zip`, confirming the `.epub` is a valid non-empty zip whose content includes at least one known entry's headword; confirm a series with no builds yet shows the download links resulting in a clean 404 rather than a broken download; edit an entry's definition, manually invoke the sweep function (or wait for the real hourly tick in a long-running check), and confirm exactly one new build is produced reflecting the change; confirm search results' dictionary name links correctly to the new page. The full stack was already running live (from Group 12's verification) against the real dev docker-compose services. curl/API-client verification of the admin rebuild route was already established as sufficient in task 12.3 (no admin rebuild button exists in this change's deliberately minimal frontend scope). This task closed the remaining gaps not yet exercised live: (1) `sources.zip` download via a real anonymous Playwright browser session clicking the actual page link (not just the automated `downloads.test.ts` coverage) — downloaded, unzipped, confirmed valid EPUB-file-set structure and real "Lannister"/"Stark" text. (2) Visually confirmed real objects in MinIO's web console at `localhost:9001` (`dictionaries` bucket, `builds/` prefix, 20 series-build folders, 182 objects). (3) Live edit-then-sweep-then-rebuild: edited the "Lannister" entry's `definitionHtml` via a temporary throwaway script (deleted afterward), called the real exported `runSweep` against the real running dev Postgres+Redis+worker (not a test double), confirmed via `psql` that exactly one new `Build` row reached `SUCCESS` with a new `contentHash` matching `Series.contentHash`, and confirmed via a real download that the generated `.epub` contained the edited text. Reverted the edit and re-ran the sweep afterward to restore the series to its original seeded content (verified the resulting rebuild's `contentHash` matched the pre-test build exactly), leaving no lasting change to dev data. The pre-build 404 state and the search-results-link-to-`/series/:slug` were already confirmed live in tasks 12.1/12.2 respectively; not repeated.
