## 1. API

- [x] 1.1 In `apps/api/src/routes/downloads.ts`, update the `GET /api/downloads` handler: remove the `where: { builds: { some: { status: "SUCCESS" } } }` clause so every `Series` is selected, remove the `entryCount > 0` filter, and map each series to `{ slug, title, entryCount, lastModifiedAt }` using `builds[0]?.entryCount ?? 0` and `builds[0]?.entryCount ? builds[0].createdAt.toISOString() : null`. Verify by calling the endpoint locally against seed data that includes an unbuilt series and confirming it's returned with `entryCount: 0, lastModifiedAt: null`.
- [x] 1.2 In the same handler, replace the Prisma `orderBy: { title: "asc" }` with an in-memory sort of the mapped DTO array: dictionaries with `entryCount > 0` before those with `entryCount === 0`, and `title.localeCompare(other.title)` within each group. Verify by calling the endpoint against seed data with a mix of populated and empty series and confirming populated ones all precede empty ones, each group alphabetical.
- [x] 1.3 Update `apps/api/tests/downloads.test.ts`: replace assertions that unbuilt/empty series are excluded with assertions that they're included with `entryCount: 0` and `lastModifiedAt: null`; add a case asserting `lastModifiedAt` is a non-null ISO timestamp for a non-empty series; add a case asserting populated-before-empty, alphabetical-within-group ordering. Verify by running the API test suite and confirming it passes.

## 2. Web

- [x] 2.1 Update the `apiGetDownloads` return type in `apps/web/src/lib/api.ts` to `{ slug: string; title: string; entryCount: number; lastModifiedAt: string | null }[]`. Verify by confirming `tsc`/the web typecheck passes.
- [x] 2.2 Update `apps/web/src/components/DownloadsPageContent.tsx`: remove the `dictionaries.length === 0` "no dictionaries" empty state (a real API response is never empty now that unbuilt series are included, but keep it defensively only if consistent with existing conventions elsewhere in the codebase), and render every dictionary returned in the order the API returns it (no client-side re-sorting). Verify by rendering the component with an API response containing a zero-series-count edge case and confirming no crash.
- [x] 2.3 In the same component, add a term-count cell to each row: the dictionary's `entryCount` if greater than zero, otherwise `—`. Verify visually (dev server) that a non-zero-entry dictionary shows its count and a zero-entry one shows a dash.
- [x] 2.4 Add a small local date-formatting helper (e.g. in `apps/web/src/lib/`): given an ISO string, build `MMM dd yyyy HH:mm` from `Date`'s local (non-UTC) getters — `getMonth`/`getDate`/`getFullYear`/`getHours`/`getMinutes`, zero-padded, joined with a fixed literal template — not `Intl.DateTimeFormat`, so the format is exact rather than locale-dependent. Verify with a couple of unit cases (e.g. single-digit day/hour/minute pad correctly).
- [x] 2.5 In `DownloadsPageContent.tsx`, add a last-modified cell to each row: the formatted `lastModifiedAt` when `entryCount > 0`, otherwise `—` (dash even if `lastModifiedAt` happens to be non-null). Verify visually that the displayed time matches the browser's local timezone (e.g. by comparing against the OS clock).
- [x] 2.6 In the same component, conditionally render the row's action and title styling: `entryCount > 0` keeps the existing "Download .epub" link (`/api/series/:slug/download`) with normal title styling; `entryCount === 0` instead renders "Contribute Now!" as a link to `/login`, with the row's title shown in a muted/dimmed style. Verify visually that both states render correctly for a mix of built, unbuilt, and empty-build dictionaries.

## 3. Verification

- [x] 3.1 Run the full test suite (`apps/api` and `apps/web`) and confirm all tests pass.
- [x] 3.2 Manually load `/downloads` and `/` in the dev server with seed data covering: a dictionary with entries, a dictionary with a successful but empty build, and a dictionary with no build at all — confirm each renders the correct count, last-modified value, title styling, and action per [specs/dictionary-management/downloads/spec.md](./specs/dictionary-management/downloads/spec.md), and confirm populated dictionaries are grouped above empty ones, alphabetical within each group.
