## 1. Frontend: page headings and browser tab title

- [x] 1.1 In `apps/web/src/routes/index.tsx`, change the homepage `<h1>` from "Planetos" to "eReader Dictionaries". Verify by visiting `/` and confirming the heading reads "eReader Dictionaries".
- [x] 1.2 In `apps/web/src/routes/login.tsx`, change the `<h1>` from "Planetos" to "eReader Dictionaries" and remove the `<p>Kindle Series Dictionaries</p>` tagline beneath it. Verify by visiting `/login` in each mode (`?mode=login`, `?mode=register`, `?mode=forgot-password`) and confirming the heading reads "eReader Dictionaries" with no tagline beneath it in any mode.
- [x] 1.3 In `apps/web/src/components/AppHeader.tsx`, change the header's brand link text from "Planetos" to "eReader Dictionaries". Verify by visiting any page and confirming the header's brand link reads "eReader Dictionaries", both logged out and logged in.
- [x] 1.4 In `apps/web/index.html`, change `<title>Planetos — Kindle Series Dictionaries</title>` to `<title>eReader Dictionaries</title>` (dropping the tagline, per the same reasoning as task 1.2). Verify by visiting any page and confirming the browser tab title reads "eReader Dictionaries".

## 2. Backend: password-reset email branding

- [x] 2.1 In `apps/api/src/lib/mailer.ts`, update the `sendPasswordResetEmail` from-name ("Planetos" → "eReader Dictionaries"), subject line ("Reset your Planetos password" → "Reset your eReader Dictionaries password"), and body text (both "Planetos" mentions → "eReader Dictionaries"). Leave the `no-reply@planetos.local` address's domain unchanged (internal placeholder, out of scope per the proposal). Update the matching assertions in `apps/api/tests/lib/mailer.test.ts`. Verify with `pnpm --filter api test`.

## 3. End-to-end verification

- [x] 3.1 Run `pnpm --filter api test`, `pnpm --recursive typecheck`, `pnpm --filter api lint`, `pnpm --filter web lint`, and `pnpm --filter web build`; confirm all succeed. api 241/241, typecheck clean across all 4 packages, both lints clean, web build succeeded (pre-existing chunk-size warning only, unrelated to this change).
- [x] 3.2 Manually verify against the real running dev stack: visit `/`, `/login` (all three modes), and confirm "eReader Dictionaries" appears with no leftover "Planetos" or redundant tagline anywhere in the rendered UI or browser tab title; trigger a real password-reset email via Mailpit and confirm its sender name, subject, and body all say "eReader Dictionaries". Also grep the real source tree (excluding `node_modules`, `dist`, `.playwright-mcp`, and archived `openspec/changes/archive/**`) for `Planetos` to confirm no other rendered-UI or email-content occurrence was missed. All verified live via Playwright: `/`, and `/login` in all three modes (`login`, `register`, `forgot-password`) show "eReader Dictionaries" as both the heading and browser tab title, no leftover "Planetos" or tagline anywhere. Triggered a real forgot-password request for `admin@example.com`; fetched the resulting Mailpit message directly and confirmed `From.Name: "eReader Dictionaries"`, `Subject: "Reset your eReader Dictionaries password"`, and body text "We received a request to reset your eReader Dictionaries password" — all correctly branded, address domain intentionally unchanged. Final grep sweep found only this change's own planning artifacts (expected — they describe the rename) and `README.md`'s heading, which is dev-facing documentation, not runtime UI, and was confirmed out of scope earlier.
