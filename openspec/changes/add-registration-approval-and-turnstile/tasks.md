## 1. Data model and migration

- [x] 1.1 In `apps/api/prisma/schema.prisma`, rename `User.displayName` to `username`; update the field's `@unique` and every relation/reference that names it.
- [x] 1.2 Add `usernameNormalized String @unique` to `User`.
- [x] 1.3 Add `reasonForJoining String?` to `User`.
- [x] 1.4 Add `enum UserApprovalStatus { PENDING APPROVED }` and `approvalStatus UserApprovalStatus @default(PENDING)` to `User`.
- [x] 1.5 Add the `TurnstileSettings` singleton model: fixed-literal `id`, `enabled Boolean @default(false)`, `siteKey String?`, `secretKeyEncrypted String?`, `updatedAt DateTime @updatedAt`, `updatedById String?` + relation to `User` (nullable, `onDelete: SetNull`).
- [x] 1.6 `prisma migrate dev` couldn't run non-interactively for this rename (Prisma's diff engine treats a field rename as drop+add when there's no interactive rename-detection prompt available); hand-wrote `20260825170000_add_username_approval_turnstile/migration.sql` instead and applied it via `prisma migrate deploy`. Verified: the column rename (+ index rename), the new unique `usernameNormalized` index with defensive collision-suffix backfill, a `DO $$ ... RAISE EXCEPTION` pre-check before the `LOWER(email)` bulk update, the `approvalStatus` column with its default plus the explicit backfill `UPDATE` to `APPROVED` for all 7 pre-existing rows (confirmed via direct query), and the new `TurnstileSettings` table. `TurnstileSettings` intentionally has no seeded row — `routes/turnstile.ts` will read-with-defaults / upsert-on-write instead, so a missing row is a normal, not an error, state.
- [x] 1.7 Update `apps/api/prisma/seed.ts` to set the seeded admin's `username`/`usernameNormalized` and explicitly `approvalStatus: APPROVED`.
- [x] 1.8 Verify `pnpm --filter api typecheck` passes with the regenerated Prisma client. Passes clean now that Groups 2-6's consumer updates (auth.ts, admin.ts, requireAuth.ts/requireAdmin.ts, entries.ts) are done; `pnpm --filter api test` also passes (115/115).

## 2. Shared package

- [x] 2.1 Add `.trim()` to `plainText()` in `packages/shared/src/validation.ts` (right after `z.string()`, before `.min()`/`.max()`); add a test confirming a whitespace-only input now fails the `min` check and a padded input is stored trimmed.
- [x] 2.2 Update `packages/shared/src/auth.ts`: rename `displayName` → `username` throughout `registerSchema`/`userDtoSchema`/`adminUserSchema`; add `usernameSchema` (via `plainText`) enforcing required/trimmed/whitespace-rejected; add `reasonForJoiningSchema` (via `plainText({ max: 2000 })`); add `approvalStatus: z.enum(["PENDING","APPROVED"])` to `userDtoSchema`/`adminUserSchema`; add a `reasonForJoining`/`turnstileToken` (optional) to `registerSchema`; rename `loginSchema`'s `email` field to `identifier`. Also added `pendingUserDtoSchema`/`PendingUserDto`. Updated `auth.test.ts` (18 tests, all passing) for the renamed/new fields.
- [x] 2.3 Add `packages/shared/src/turnstile.ts`: `turnstileConfigSchema` (public `{enabled, siteKey}`), `turnstileSettingsDtoSchema` (admin view: `{enabled, siteKey, secretConfigured, updatedAt}` — no secret value), `updateTurnstileSettingsSchema` (`{enabled, siteKey, secretKey?}`).
- [x] 2.4 Export the new/changed modules from `packages/shared/src/index.ts`; run `pnpm --filter shared build && pnpm --filter shared test`.

## 3. Backend: crypto and Turnstile modules

- [x] 3.1 Add `apps/api/src/lib/crypto.ts`: `encrypt(plaintext: string): string` / `decrypt(packed: string): string` using AES-256-GCM and `process.env.SETTINGS_ENCRYPTION_KEY`, packing IV + auth tag + ciphertext into one base64 string. Add unit tests: encrypt-then-decrypt round-trips; decrypting a tampered string throws. Key is derived via `sha256(SETTINGS_ENCRYPTION_KEY)` to get 32 bytes; `getKey()` throws if the env var is missing/short, matching design.md's "fail fast, don't silently run with an absent/weak key." Added `SETTINGS_ENCRYPTION_KEY` to local `.env` (dev-only value) so tests run; `.env.example` documentation is task 12.1.
- [x] 3.2 Add `apps/api/src/lib/turnstile.ts`: `verify(secretKey: string, token: string, remoteIp?: string): Promise<{success: boolean; errorCodes: string[]}>` (POSTs to `https://challenges.cloudflare.com/turnstile/v0/siteverify` via `fetch`); `isSecretKeyRecognized(secretKey: string): Promise<boolean>` (sends a deliberately-invalid dummy token and inspects `error-codes` for `invalid-input-secret` vs. other codes, per design.md). Add tests mocking `fetch` for: successful verify, failed verify, network error, malformed-secret detection, well-formed-secret detection.

## 4. Backend: registration and login

- [x] 4.1 Update `apps/api/src/routes/auth.ts`'s `POST /api/auth/register`: accept `{ username, email, reasonForJoining, password, turnstileToken? }`; lowercase `email` before lookup/storage; compute `usernameNormalized`; duplicate-check both (case-insensitive) via the existing `findFirst`/`OR` pattern plus the `P2002` fallback; when Turnstile is enabled (per `TurnstileSettings`), require and verify `turnstileToken` via `lib/turnstile.ts` before creating the account, rejecting with a clear validation error (no secret/implementation details) on failure or on missing configuration (logging a server-side config error in the latter case); explicitly set `role: "MEMBER"` and `approvalStatus: "PENDING"` (never from the request body); update `toUserDto` to return `username`/`approvalStatus` instead of `displayName`.
- [x] 4.2 Update `POST /api/auth/login`: rename the schema field to `identifier`; resolve via `findFirst({ where: { OR: [{ usernameNormalized: normalizeWord(identifier) }, { email: normalizeWord(identifier) }] } })`; keep the existing password-verify/`isActive`/session logic unchanged; keep the generic failure message.
- [x] 4.3 Update `GET /api/auth/me` and the `requireAdmin`/`requireAuth` plugins' inline types and `select` blocks to use `username` and include `approvalStatus`.
- [x] 4.4 Add `DUPLICATE_USERNAME`, `TURNSTILE_VERIFICATION_FAILED`, and `TURNSTILE_MISCONFIGURED` to the `Errors` registry in `apps/api/src/lib/errors.ts` (rename/reuse `DUPLICATE_DISPLAY_NAME` → `DUPLICATE_USERNAME` if it's the same case, matching the existing `DomainError` shape).

## 5. Backend: entry-creation gating

- [x] 5.1 Add `makeRequireApproved(prisma)` to `apps/api/src/plugins/requireAuth.ts`: same session/active checks as `makeRequireAuth`, then pass if `role === "ADMIN"`, else require `approvalStatus === "APPROVED"` (403 otherwise, matching the existing problem+json shape).
- [x] 5.2 In `apps/api/src/routes/entries.ts`, swap `POST /api/series/:slug/entries`'s `preHandler` from `requireAuth` to `requireApproved`; leave `GET /api/series/:slug/entries/words` on `requireAuth`.
- [x] 5.3 Extend `apps/api/tests/entries.test.ts` (or add a new test) covering: a Pending member's `POST /api/series/:slug/entries` is rejected; an Approved member's succeeds; an admin's succeeds regardless of the admin's own approval status. Also renamed `displayName`→`username` in this file's register payload/helpers and updated `setupMember` to explicitly approve the member (registrations now default to Pending) since Group 6's admin-approval endpoints don't exist yet for tests to use.

## 6. Backend: registration-approval endpoints

- [x] 6.1 In `apps/api/src/routes/admin.ts`, add `GET /api/admin/users/pending` (`requireAdmin`): list `approvalStatus: "PENDING"` users ordered `createdAt asc`, selecting `id, username, email, reasonForJoining, createdAt` only (no security fields).
- [x] 6.2 Add `POST /api/admin/users/:id/approve` (`requireAdmin`): 404 if missing, 409 (`ALREADY_REVIEWED`-style) if not Pending, else set `approvalStatus: "APPROVED"`, return the updated (non-Pending) user DTO.
- [x] 6.3 Add `POST /api/admin/users/:id/deny` (`requireAdmin`): 404 if missing, 409 if not Pending, else `prisma.user.delete()` (verify cascade/SetNull behavior on `revisions`/`createdSeries`/`submittedEntries`/`reviewedEntries` — all already nullable+SetNull per the existing schema), return `204`.
- [x] 6.4 Also update the existing `GET /api/admin/users` (all-users listing) to exclude `approvalStatus: "PENDING"` accounts, matching design.md's decision to keep the two tables separate. Also fixed the pre-existing `displayName`→`username` cascade in `admin.ts` (route selects/DTOs) that the Prisma rename had left broken.
- [x] 6.5 Add `apps/api/tests/adminRegistrations.test.ts` (or extend `admin.test.ts`) covering: pending list ordering/shape/exclusion-of-approved; 401/403 on all three endpoints; approve transitions and is safe on already-approved/nonexistent targets; deny deletes and is safe on already-removed/nonexistent targets; denied user's row disappears from the pending list; approved user's row disappears from the pending list and appears in the regular list. 14 tests, all passing. Also fixed `admin.test.ts`'s `displayName`→`username` field renames and its "user list" test (which now must explicitly approve its test member, since the list excludes Pending accounts).

## 7. Backend: Turnstile settings endpoints

- [x] 7.1 Add `apps/api/src/routes/turnstile.ts`: `GET /api/turnstile/config` (public — `{enabled, siteKey}` only, `siteKey: null` if disabled or unset); `GET /api/admin/turnstile` (`requireAdmin` — `{enabled, siteKey, secretConfigured, updatedAt}`); `PATCH /api/admin/turnstile` (`requireAdmin` — updates `enabled`/`siteKey` always, re-encrypts and overwrites `secretKeyEncrypted` only when a non-blank `secretKey` is submitted, sets `updatedById`); `POST /api/admin/turnstile/test` (`requireAdmin` — calls `isSecretKeyRecognized`, returns `{success: boolean}`, no secret in the response ever).
- [x] 7.2 Register `turnstileRoutes` in `apps/api/src/index.ts` and `apps/api/tests/helpers.ts`. Also added a `resetTurnstileSettings` test helper (deletes the singleton row) since the row persists across test files.
- [x] 7.3 Add `apps/api/tests/turnstile.test.ts`: config endpoint reflects enabled/disabled state; admin GET never includes a secret value; PATCH with blank `secretKey` doesn't change `secretKeyEncrypted`; PATCH with a new `secretKey` changes it; non-admin 403 on GET/PATCH/test; test-configuration endpoint behavior mocked against `lib/turnstile.ts`. 16 tests, all passing.

## 8. Web: registration and login

- [x] 8.1 Add `@marsidev/react-turnstile` to `apps/web/package.json`.
- [x] 8.2 Update `apps/web/src/routes/login.tsx`'s `SignInForm`: rename the `email` field to `identifier`, relabel "Username or Email", update the submit handler and error mapping accordingly (keep the generic-failure-message behavior).
- [x] 8.3 Update `RegisterForm`: add a required Username field (label "Username"), add a required multiline "Why are you requesting to join?" field (max 2,000, matches `reasonForJoiningSchema`); fetch `GET /api/turnstile/config` and conditionally render the `Turnstile` widget (Managed mode) using the returned `siteKey` when `enabled` is true, wiring its completion callback to a form field consumed on submit; block submission until a token is present when Turnstile is enabled; update the 409-error field-mapping regex/logic for the renamed `username` field; on successful registration, show a toast "Your registration request has been submitted and is awaiting administrator approval." before navigating to `/`.
- [x] 8.4 Update `apps/web/index.html`'s CSP: add `https://challenges.cloudflare.com` to `script-src` and `connect-src`; add a new `frame-src https://challenges.cloudflare.com` directive.
- [x] 8.5 Update `apps/web/src/lib/api.ts`: rename `apiLogin`'s payload field to `identifier`; update `apiRegister`'s payload type; add `apiGetTurnstileConfig`. Also added `apiGetPendingUsers`/`apiApproveRegistration`/`apiDenyRegistration`/`apiGetTurnstileSettings`/`apiUpdateTurnstileSettings`/`apiTestTurnstileConfig` in the same pass (tasks 9.4/10.4) since they're all in the same file. Also fixed a `displayName`→`username` reference in `routes/index.tsx` ("Signed in as...") discovered via typecheck.

## 9. Web: Admin Dashboard changes

- [x] 9.1 Update `apps/web/src/routes/admin.tsx`: rename the "Display Name" column/references to "Username"; add a "Pending Registrations" table section above the existing table (columns: Username, Email, Reason for Joining — truncated with a click-through details dialog mirroring the Approval Queue's entry-details modal pattern — Approve/Deny), fetched via a new `apiGetPendingUsers` query.
- [x] 9.2 Wire Approve to a `useMutation` (per-row busy state, matching the existing `admin.tsx`/`admin_.approval-queue.tsx` pattern): on success, invalidate the pending-users query, show a success toast; no optimistic removal.
- [x] 9.3 Wire Deny to a confirmation dialog (`ui/dialog.tsx`, wording per spec: "Are you sure you want to deny this registration? The user account will be permanently deleted.") + `useMutation` calling `apiDenyRegistration`; remove from the list only after success; success toast.
- [x] 9.4 Add `apiGetPendingUsers`, `apiApproveRegistration`, `apiDenyRegistration` to `apps/web/src/lib/api.ts`. (Done in task 8.5's pass.)

## 10. Web: Turnstile administration page

- [x] 10.1 Add `apps/web/src/routes/admin.turnstile.tsx` (flat-file naming — do NOT create an `admin/turnstile.tsx` nested file, per the routing lesson from the previous change) with an admin-only `beforeLoad` guard matching `admin_.approval-queue.tsx`'s pattern. Named it `admin_.turnstile.tsx` (matching the existing `admin_.approval-queue.tsx` file exactly, confirmed via `routeTree.gen.ts` to register as `/admin/turnstile` independent of `/admin`'s own route, not nested under it).
- [x] 10.2 Build the settings form: Enabled toggle (`ui/switch.tsx`), Site Key `Input`, Secret Key `Input type="password"` (never pre-populated), "Secret Key: Configured"/"Not configured" status text, last-updated timestamp; submit via `useMutation` calling `apiUpdateTurnstileSettings` (only include `secretKey` in the payload when the field is non-blank).
- [x] 10.3 Add a "Test Configuration" button calling `apiTestTurnstileConfig`, showing a success/failure toast per the response. Disabled until a secret is configured.
- [x] 10.4 Add `apiGetTurnstileSettings`, `apiUpdateTurnstileSettings`, `apiTestTurnstileConfig` to `apps/web/src/lib/api.ts`. (Done in task 8.5's pass.)

## 11. Web: navigation

- [x] 11.1 Update `apps/web/src/components/AppHeader.tsx`: add "User Management" (→ `/admin`) and "Turnstile" (→ `/admin/turnstile`) items to the Administration shelf, alongside the existing "Approval Queue" item.
- [x] 11.2 Update `apps/web/src/routes/entries/new.tsx`'s `beforeLoad` guard: redirect to `/` when the current user's `approvalStatus !== "APPROVED"` and `role !== "ADMIN"` (fetch via `apiMe`, matching the existing pattern), in addition to the existing unauthenticated → `/login` redirect. Also update any UI that hides/disables the Entries → Add item for Pending users if applicable — **discovery**: checked the live `navigation/app-menu` spec's "Add Action in Entries Shelf" requirement ("visible to all authenticated users regardless of role"), and this change's own delta (`specs/navigation/app-menu/spec.md`) only ADDs the two new Administration items — it does not MODIFY that requirement. So hiding the Add item for Pending users would silently narrow an unmodified, still-authoritative requirement. Left it visible unconditionally; redirect-on-navigate (this task) plus the server-side `requireApproved` guard (task 5.1/5.2) are the actual enforcement, matching design.md's stated approach.

## 12. Deployment configuration

- [x] 12.1 Add `SETTINGS_ENCRYPTION_KEY` to `.env.example` with guidance matching `SESSION_SECRET`'s wording (long random string, keep secret in production).
- [x] 12.2 Add a comment block to `.env.example` (or a short section in `SPEC.md`) documenting Cloudflare's published Turnstile test Site Key/Secret Key for local development, and that production credentials must never be committed.

## 13. SPEC.md

- [x] 13.1 Update `SPEC.md`: §1 access table (add registration-approval and Turnstile rows), §4 schema excerpt (`username`, `usernameNormalized`, `reasonForJoining`, `approvalStatus`, `UserApprovalStatus`, `TurnstileSettings`), §6 endpoint list (new auth/admin/turnstile routes), a short note on the registration-approval workflow, and a note on the required `SETTINGS_ENCRYPTION_KEY` env var — consistent with SPEC.md's own stated policy of staying authoritative. Also updated §8's route list for `/admin/turnstile`.

## 14. End-to-end verification

- [x] 14.1 Run `pnpm --recursive typecheck`, `pnpm --filter api lint`, `pnpm --filter web lint`, and `pnpm --filter web build`; confirm all succeed. All clean.
- [x] 14.2 Run `pnpm --filter api test`; confirm all new and existing tests pass. 131/131 passing (also `pnpm --filter shared test`: 36/36).
- [x] 14.3 Manually ran the full app (real Postgres/Redis, both dev servers, driven via Playwright browser automation): registered `WalkthroughUser` with a reason for joining, Turnstile disabled — confirmed no widget rendered, confirmed the pending-approval toast, confirmed `/entries/new` redirected to `/`, and confirmed a direct `POST /api/series/:slug/entries` call returned 403 with "Your account is pending administrator approval." Logged in as admin (via lowercase username `administrator`, confirming case-insensitive username login), saw the registration in Pending Registrations, opened the full reason via its details dialog, approved it, and confirmed the now-approved user could create an entry (201) without re-authenticating. Registered a second account (`DenyMeUser`) and denied it via the confirm dialog (exact spec wording verified); confirmed permanent deletion via a subsequent login attempt (401 invalid credentials). Enabled Turnstile with Cloudflare's published test Site Key/Secret Key; confirmed the widget rendered and auto-passed on `/login?mode=register`, and a real registration completed with a genuine token verified against Cloudflare's live siteverify endpoint; ran Test Configuration and got "Secret Key is recognized by Cloudflare." (never exposing the secret); saved settings again with a blank Secret Key field and confirmed `secretConfigured` stayed true (byte-level "unchanged" assertion already covered by the automated `turnstile.test.ts`).
