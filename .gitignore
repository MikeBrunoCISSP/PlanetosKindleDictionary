node_modules
.tanstack
.env

# Compiled output — every entry point (pnpm dev / build / build:railway /
# typecheck / test) rebuilds packages/*/dist first via `pnpm run build:packages`
/apps/api/dist/
/apps/web/dist/
/packages/*/dist/

*.log
.playwright-mcp/

# curl / libcurl cookie jars (never commit session cookies)
*cookies.txt
*.cookie-jar
*.cookies
cookies/

# AI
.claude/
.codex/
